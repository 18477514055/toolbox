# 桌面工具箱 · 第三方质检报告

> ## ⚠️ 更正声明（2026-09-17 增补，位置后于正文）
>
> **本报告 §四·B-1 里"100% 是孤儿"这个描述不准确。交付方在 `07-质检整改回复.md` §二指出，我接受该更正。**
>
> **我错在哪**：我只按*索引里的 `Volatile` 指针字段*统计引用，得出"13 个文件 100% 无引用"。
> 但实际上 `GetFullText()` 在降级分支里**会按 `entry.Hash` 去读 `volatile\<hash>.txt`** ——
> **普通长度的文本条目本来就依赖这些文件**。
>
> **这个错误描述的危险性**：若有人照"它们都是没人引用的垃圾"去修，最直觉的做法是**全删**，
> 那会把历史里**大部分条目的正文一起删掉**——比原 bug 严重得多。
>
> **正确的结论（原 bug 仍然成立）**：真正的缺陷只有一条 —— **删除路径回收不到这些文件**，
> 于是只增不减。成因不是"写进去没用"，而是"**删的时候找不到**"。
> 我给的修法（按 `entry.Hash` 推导文件名、删除时并删）方向是对的，但**理由是错的**。
>
> **教训（写给以后的自己）**：判断"有没有人用"不能只查索引里的指针字段，
> **必须追一遍实际读取路径**（谁在什么条件下会读它）。这次我把"结构化引用"当成了"全部引用"。

> 质检人：DeepSeek Harness（原项目交接方）
> 质检对象：接手平台交付的 `dist\Toolbox.exe` + 31 个源文件
> 质检方式：**独立复现为主**，不采信交付方自述文档
> 质检时间：2026-09-17
> **对应版本**：本报告针对**整改前**的版本；交付方的整改结果见 `07-质检整改回复.md`

---

## 一、总结论

**通过（有条件）**。交付物是**真实现**，不是空壳；我独立复现了它的核心断言，绝大部分成立。**主链路可以交付给用户试用了**，但有几处必须先修或至少先知情。

| 维度 | 结论 |
|---|---|
| 机器自检 | ✅ **我独立跑通 21/21**，0 失败（另有 2 项环境信息，明确标注不计入通过率——做法正确） |
| 代码真实性 | ✅ 全库 `TODO/FIXME/NotImplemented/占位` **零命中**；47 处 `return false/null` 逐条核对全在真实分支；无模拟按键抓取（`SendInput/keybd_event/SendKeys` 零命中） |
| 交接文档 4 个坑 | ✅ 坑 1/2 我在源码里逐字确认落实；坑 3/4 在工具箱场景不适用（无控制台退出/汇总文件） |
| 关键架构原则 | ✅ "通道 vs 档案"、"悬浮窗不抢焦点"（代码层面）、"中英双向写死方向" 全部落实 |
| 功能与文案一致性 | ❌ **有 2 处宣称与实现不符**（见问题 7、问题 8）——用户照验收单走会判不通过 |
| 资源管理 | ❌ **有 1 处无上限磁盘泄漏**（见问题 1） |
| 人工验收项 | ⚠️ **悬浮窗是否真的不抢焦点，仍未验证**（见 §五）——这是唯一的功能前提级未知 |

**AI 工具 8 条需求的实际达成度**（我与独立审计双方确认）：

| 需求 | 结论 |
|---|---|
| 1 整窗口 UIA 读全文 | ✅ 已实现（TextPattern + 三级降级） |
| 2 选中模式走 `GetSelection()`、不过剪贴板 | ✅ 已实现（`TextGrabber.cs` 全文零 Clipboard 引用） |
| 3 焦点窗口判定、不误记自家窗口 | ✅ 已实现（按**进程身份**过滤，天然覆盖全部自家窗口，比"列名单"更可靠） |
| 4 翻译当前页面 | ⚠️ **部分实现**（须"读取→翻译"两步，热键不会自动读）→ 问题 7 |
| 5 翻译选中文本 | ✅ 已实现（热键直达） |
| 6 翻译剪贴板 | ✅ 已实现，**确为只读**（不违反硬约束） |
| 7 中英双向判方向 + 提示词写死目标语言 | ✅ 已实现（CJK 占比阈值 0.30） |
| 8 分块 / 进度 / 流式 / 可中断 | ✅ 已实现（无汇总、无按段懒加载；悬浮窗点图标只换 ⏳ 不中断） |

剪贴板与格式转换 12 条需求：**9 条已实现、3 条部分实现、0 条未找到**。

---

## 二、我独立复现通过的（不是看它的文档，是我自己跑的）

### 2.1 机器自检：21/21

```
自检结果：21/21 通过 —— 全部通过（另有 2 项环境信息，不计入通过率）
```

值得肯定的细节：断言是**真断言**不是摆设——文本逐字一致、图片尺寸精确（120x80）、文件大小精确（4096 字节）、消息循环存活（坑 2 的防回归）、自污染标记命中且真的跳过归档、译文方向两个方向都测。

### 2.2 热键"被占用"的说法是真的（我自己写探针验的）

交付方称默认键 `Win+Shift+V/A/C` 被别的程序占用、自动降级。我写了个独立的 `RegisterHotKey` 探针（带对照组）实测：

| 热键 | 实测 |
|---|---|
| `Win+Shift+V` / `+A` / `+C` / `+Space` / `+T` | **确实被占用**（Win32 错误码 1409） |
| `Win+Shift+X` / `+E` | 空闲 |
| `Win+Alt+V` / `+C` / `+A` | 空闲（降级目标选得对） |
| 对照组 `Win+V` / `Win+Shift+S` | 预期失败，实测失败 ✅ 证明探针本身有效 |

**结论：它的热键探测没有假阳性，降级链是经过实测的。**

而且它们的记录显示，这一步做得**比我交接时更细**（`DECISIONS.md` §七）：

- 先在**工具箱进程完全退出后**再测一次，排除"自己注册两次"的干扰；
- 带对照组（`Ctrl+Shift+F9` 空闲）证明探针有效；
- 给出完整占用表：`Win+Shift+<字母>` 里 **A C F M P R S T V W 共 10 个被第三方程序占用**；
- 做了一条我没想到的产品决策：**降级链只对"工具自带默认键"生效，用户自己改过的键被占用时只报错、不自动换**（理由：悄悄换掉比报错更让人困惑）；
- 降级结果**写回 `settings.json`**，避免"设置里显示 Win+Shift+V、按了没反应"。
- 连"通知里带点的工具 id 找不到、会裸奔出英文"这种细节都修了（`DescribeTool()`）。

另外：我核对过它的 `00-先看我.md` / `05-验收清单.md` / `DECISIONS.md` 三处文档的热键说法**互相一致**，没有前后矛盾。

### 2.3 开机自启：真写进了注册表（我自己回读注册表验的）

不采信它的回报，直接读 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`：
写入后能看到值 `桌面工具箱` = 正确的 exe 全路径；关闭后**无残留**（这点很重要，说明它没留垃圾）。

### 2.4 文档转换：端到端真跑通（含中文路径）

```
txt → docx   10657 ms
docx → pdf    3821 ms
产物：中文名测试文档.pdf  22357 字节  文件头 = %PDF-   ✅
```

### 2.5 AI 翻译：两个方向都实测

直接打 Ollama（模拟它的调用方式）：

| 方向 | 耗时 | 结果 |
|---|---|---|
| 中 → 英 | 5.4 s | 方向正确 |
| 英 → 中 | 3.9 s | 方向正确（译文质量正常） |

### 2.6 端到端链路的关键实现，我逐字确认过

- `MessageWindow.cs` 用**显式 `CreateWindowExW` + `STATIC` + `HWND_MESSAGE`** ✅（坑 1 的正确写法）
- `MessageWindow.WndProc` **全包 try/catch**，注释写明"异常逃出去 = 消息循环线程死亡" ✅（坑 2）
- `FloatingWindow.xaml` 有 `ShowActivated="False"`，`.xaml.cs` 又补了 `WS_EX_NOACTIVATE` + `WS_EX_TOOLWINDOW` ✅
- `AiPanel.xaml.cs:126` 用 `_ctx.Focus.LastForeignWindow`，而 `FocusTracker` 用 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND, WINEVENT_SKIPOWNPROCESS)`——**由 Win32 自动排除工具箱自己的窗口**，比我交接时建议的"手动过滤"更稳 ✅
- `Prompts.cs` 把目标语言写成参数（"翻译成{targetLanguage}"），并明确要求"只输出译文""代码/专有名词/URL 原样保留" ✅
- `OllamaClient` 用 `Timeout = InfiniteTimeSpan`，超时交给 `CancellationToken` ✅（避免首次加载模型必然超时）
- 剪贴板读取顺序 **文件 → 图片 → 文本** ✅，且用 `GetFormats(false)` 只取名不逐个取值（不触发延迟渲染）✅
- 自污染用 **token + 内容哈希 + 10 秒 TTL** 三重判定，比我建议的单哈希更稳 ✅
- 配额 `MaxEntries=2000` / `MaxImageBytes=500MB`，LRU 淘汰、**置顶不淘汰** ✅

### 2.7 我自己的两个判断被证伪，如实纠正

| 我原本的怀疑 | 实测结论 |
|---|---|
| "长内容外置阈值可能只有几百字节，会把正常内容也摘要化" | **错**。阈值是 `LongTextThreshold = 10 * 1024`（字符），正常文本不受影响 |
| "`run-toolbox.cmd` 是 UTF-8 存的却写 `chcp 936`，中文注释应该坏了" | **错**。它是 **GBK 存储**，按 GBK 读中文完全正常，且它注释里自己写明"cmd 必须 GBK + chcp 936"——是我读取方式不对 |

---

## 三、交付方自己发现的坑（值得单独表扬）

在交接文档给的 5 个坑之外，它们**又实测出 7 个新坑**并落档（`DECISIONS.md` §六），其中几个只有真跑过才会知道：

| 坑 | 内容 |
|---|---|
| 6 | 本机构建环境缺 `APPDATA` 和 `ProgramFiles(x86)`，`dotnet build` 直接崩 |
| 7 | `UseWindowsForms=true` 会注入隐式 using，把 WPF 类型全变成"不明确的引用" |
| 9 | 自检里 `GetAwaiter().GetResult()` 等 `await` —— **必死锁（实测卡了 11 分钟）** |
| 10 | 中文 `.cmd` 用 UTF-8 无 BOM + `chcp 65001` 会让中文行被当成命令执行 |
| 11 | 自检"按事件等待"会误判成失败 → 必须"按内容等待" |
| 12 | 一次复制会产生**多条** `WM_CLIPBOARDUPDATE` |

---

## 四、发现的问题（按严重程度）

### 🟠 A-1：界面宣称"按热键就读当前页面"，但按热键**不会读**（功能级偏差）

**我逐行确认**：`AiTool.cs` 全文**只有** `GrabSelectionAsync`（`:89`、`:132`），**没有任何 `GrabWindowAsync` 调用**；`Invoke()` 只做 `SetCachedSelection` → `Show()` → `Activate()`。

而界面文案写的是：

```
AiPanel.xaml:68   <RadioButton x:Name="ModeWindow" Content="整窗口（按热键就读当前页面）"
```

**后果**：
- 按 AI 热键后，面板里文本框是**空的**，用户必须再点一下「读取」才填正文；
- 它们的 `05-验收清单.md` 第 3.5 节第 1 项写的是"按 `Win+Alt+A` 后面板里**已经填好了**这个页面的正文（不是空白）"——**照着自己的验收单走会直接判不通过**；
- 交接文档设计的独立入口「🌐 翻译当前页面」也没有（`AiTool.cs:38-41` 的额外热键里只有 `translateSelection`）。

**这不是"没实现功能"，而是"实现与文案/验收单不一致"**：能力（读整窗口）是有的，只是要两步操作。修法二选一：① 让 `Invoke()` 自动执行一次读整窗口（推荐，符合文案）；② 改文案 + 改验收单（承认是两步）。

---

### 🟠 A-2：「只取主内容区」是**空开关**

`TextGrabber.cs:199-204`：

```csharp
if (!string.IsNullOrWhiteSpace(best))
{
    var more = documents.Count > 1 && !mainContentOnly;
    return GrabResult.Ok(best, more ? "整窗口（主文档）" : "整窗口（主内容区）");
}
```

两个分支**返回的是同一个 `best`**，`mainContentOnly` 只改变了提示字符串。界面上那个勾选框：

```
AiPanel.xaml:77   Content="整窗口时只取主内容区（少带导航 / 广告 / 评论区噪声）"
```

**勾与不勾，读出来的正文完全一样。** 交接文档里反复强调的"整窗口会带导航/广告噪声，要正面处理"这个难点，实际上**没有落地**——真正生效的只有 `TextCleaner` 的压空行/去连续重复行。

**修法**：按 `documents` 里各 Document 的文本长度与位置做筛选（取最长且位于主区域的），或引入"忽略导航/页脚特征行"的清洗规则。若短期不做，**界面文案必须去掉这个承诺**。

---

### 🔴 B-1：`volatile\` 目录是无上限的磁盘垃圾场，而"删除"给了用户已清干净的错觉

**我亲手复现**：

```
索引条目数: 12
索引里带 Volatile 指针的条目数: 0
volatile 目录实际文件数: 13
其中【索引完全不引用】的孤儿文件: 13 个
```

> ⚠️ **就地更正（务必先读）**：上面这个统计**只反映"索引里的 `Volatile` 指针字段"**，
> **不等于"没有代码引用它们"**。`GetFullText()` 的降级分支会按 `entry.Hash` 读
> `volatile\<hash>.txt`，**普通长度文本条目的正文本来就靠这些文件**。
> ⇒ 正确结论是"**删除路径回收不到**（只增不减）"，**不是"这些都是没用的垃圾"**。
> 详细更正见文首的「更正声明」与 `07-质检整改回复.md` §二。
> **不要把这里的"孤儿"理解成"可以全删"。**

**根因**（三处代码合成一个 bug）：

| 位置 | 行为 |
|---|---|
| `HistoryStore.cs:235-237` | 文本正文**无条件**落盘到 `volatile\<hash>.txt` |
| `HistoryStore.cs:239-244` | **只有超长**（>10240 字符）才在条目上留指针 `entry.Volatile` |
| `HistoryStore.cs:455-458` | 删除时**只删有指针的**文件 |

→ 所有**正常长度**文本的正文文件都没有指针，于是**删除条目 / LRU 淘汰 / 清空历史（含"保留置顶"）统统回收不到它们**。`volatile\` 既无配额也无清理，只增不减。HTML 原文（`:251-263`）同理。

**影响**：用户点"删除"以为清掉了，磁盘上正文还留着；长期使用静默膨胀。**这是唯一一个我认定为必须修的缺陷。**

**修法**（很局部，我已验证可行）：文件名就是 `<完整hash>.txt`，**可直接由 `entry.Hash` 推导**——实测索引里 `Hash` 与 `volatile\` 下的文件名一一对应。所以只需在 `DeletePayload` / `EnforceQuota` / `ClearAll` 里按 hash 并删 `.txt` 和 `.html` 即可。

---

### 🟡 B-2（与 B-1 同源）：被去重顶掉的条目，其正文文件同样成为孤儿

实测中发现：索引里有 `Hash` 指向的 `.txt` 存在，但也有索引 `Hash` 在 `volatile\` 里**找不到对应文件**的情况。这与"同一内容去重后复用原条目"有关——去重路径不会重写正文文件，而删除路径又删不到它。修法与问题 1 合并处理即可（按 hash 为唯一键统一管理）。

---

### 🟡 B-3：`OfficeComConverter` 超时会留下僵尸 WINWORD

`OfficeComConverter.cs:163-166`：`thread.Join(5 分钟)` 超时后**直接返回失败，但不中断那条 STA 线程**——线程继续跑，且 finally 里的 `word.Quit()` 会因被放弃而失效 → 僵尸 `WINWORD.EXE` 持有源文件与输出文件，之后同一文件的转换会莫名失败，**报错完全指不到根因**。代码注释自己也承认"可能留下僵死的 WINWORD 进程"。

**影响**：Office COM 是**备选**后端（首选 LibreOffice），所以触发概率中等。但一旦触发，用户很难自查。

---

### 🟡 B-4：搜索框每敲一个字符就全表重载缩略图

`ClipboardPanel.xaml.cs:44` `SearchBox.TextChanged → RefreshList()` → `:116` 每条图片条目都 `LoadThumb` → `HistoryStore.cs:351-378` 逐张**读盘 + 解码 + Freeze**；上限 2000 条 / 500 MB。

**影响**：图片条目多时输入卡顿。`ListBox` 虽开了虚拟化，但 `MakeRow` 是**全部条目先建好**再交给列表，虚拟化救不了这一步。**修法很局部**：只为首屏/可见项取缩略图，或按 hash 做内存缓存。

---

### ⚪ C-1：UI 提示与实现不一致（用户会直接撞到）

`ClipboardPanel.xaml:147` 底部提示写 **`Ctrl+D = 删除`**，而实现是 **`Ctrl+Delete`**（`ClipboardPanel.xaml.cs:185` `case Key.Delete when Modifiers == Control`）。用户照提示按 `Ctrl+D` 毫无反应。**改一个字符串即可。**

### ⚪ C-2：拖入视频/音频是"静默跳过"而不是"明确拒绝"

`ConvertWindow.xaml.cs:185-206` 对不支持类型（mp4 等）直接丢弃，只在汇总行里说"跳过 N 个不支持的类型"。验收清单 3.4 第 5 项写的是"被**明确拒绝**"——实际是"不列入支持 + 汇总提示"，**算部分实现**。建议对 mp4/avi/mp3 给一条专门提示。

### ⚪ C-3：AI 读选区不校验焦点窗口是不是自己

`TextGrabber.cs:52` 直接取 `AutomationElement.FocusedElement`，**没有校验该窗口是否属于本进程**。边缘场景：工具箱某个会抢焦点的窗口（剪贴板面板 `Activate()` + `SearchBox.Focus()`）是前台时触发"翻译选中"，可能读到工具箱自己的控件文本而非用户选区。**修法**：加一次 `GetForegroundWindow` + `GetWindowThreadProcessId` 的归属校验，命中自己就返回明确提示。

---

### ⚪ C-4：几处未接线/死代码（低危）

- `AiPanel.xaml:75` 的 `TargetInfo` 从未被赋值 → 永远空白；"将要读哪个窗口"只在点「读取」后才写进 `StatusText`（应显示在"读哪个窗口"这件事上，用户更早需要知道）。
- `OllamaClient` 的 `systemPrompt` 恒为 `null`（唯一调用点传 null）→ 对应分支是死代码。**注意：这不影响翻译质量**，因为提示词里已经把规则写全了（我实测两个方向译文质量正常）。
- `AiPanel.xaml.cs:311` 探活调用传 `CancellationToken.None`，而 `OllamaClient` 的超时是 `InfiniteTimeSpan` → **这一次调用既无超时也不可取消**；且「停止」按钮要到 `:321` 才启用。若把 Ollama 地址改成不可达的黑洞地址，界面会永久卡在"正在检查 Ollama…"。对照：设置窗口里的探活是有界的（15s / 8s）。
- 每块翻译都 `new HttpClient` 且从不释放（8 段文章 = 9 个），长期使用是资源浪费。
- `_running` 重入守卫：检查在 `:300`、赋值在 `:319`，中间隔着一次 `await` → 该窗口期内再点按钮不会被拦，第二次会覆盖 `_cts`，导致「停止」只能取消后一条。**真实但难触发**。

---

## 五、尚未验证的（必须人工做，机器替不了）

### ⚠️ 最高优先：悬浮窗是否真的不抢焦点

`WS_EX_NOACTIVATE` 在源码里**设置正确**（我逐字确认过），但"点了之后焦点是否真的留在原窗口"**只有人手能验**。这是全项目功能正确性的前提——一旦失效，AI 的"读当前页面"会读到悬浮窗自己。

操作（30 秒）：开记事本打字让光标停在里面 → 用鼠标点悬浮窗任意图标 → **记事本应仍是前台窗口、光标没跳走**；再按 `Alt+Tab`，**悬浮窗不应在列表里**。

### 其余需人工验收的

| 项 | 为什么机器验不了 |
|---|---|
| 复制/截图/复制文件后历史里三种都在 | 需要真人操作真实程序 |
| 搜索框实时过滤、回车回填剪贴板 | 同上 |
| 裁剪拖框手感、125%/150% 缩放下是否对得上 | 涉及真实鼠标与 DPI |
| 拖 docx/xlsx 真实转换 + 长文 3000 字分块摘要进度 | 需要真实文件与等待 |
| 关掉 Ollama 是否给明确提示 | 需要人为制造异常 |
| 跑一整天：有没有漏、有没有卡、内存是否单调上涨 | **只有时间能验** |

**另需注意**：他们数据目录里已有 12 条真实历史（含 1 条 Files），说明**剪贴板主链路在真实使用中已经跑过**，这是好信号。

### 明确"没有证据"的部分（不等于有问题，但要知情）

| 项 | 状态 |
|---|---|
| **UIA 真机读取成功率** | 自检里**没有任何 UIA 用例**（只有纯逻辑 + 流式）。"读浏览器/Office 整页"能否成功、噪声多大，**无自动化证据**，只能真机试 |
| UIA 从线程池（MTA）线程调用的合规性 | 未确认。若出问题，表现为"读取偶尔失败"而非崩溃 |
| `Clipboard.GetText()` 对延迟渲染格式是否有副作用 | 未确认（这类副作用只在特定来源程序上出现） |
| 多显示器拔插后悬浮窗位置 | 代码按工作区百分比存（设计正确），但**未实测** |
| 长期稳定性（内存是否单调上涨） | 只有跑一天能验 |

### 另外两条我已确认的"轻微但真实"的假阳性风险

1. `ClipboardWriter.cs:27` 在写剪贴板**之前**就登记了哈希 → 若 `SetDataObject` 最终失败（`:106-109` 返回 false），接下来 10 秒内**用户自己**复制同样内容会被误判成 `Origin=tool` 而**跳过归档**。影响很小（10 秒窗口 + 内容完全相同），但属真实假阳性。
2. 语言方向检测阈值 `ChineseThreshold = 0.30`（`LanguageDetector.cs:18`）严格大于，且分母只算字母 → **中英混排且带大量代码的中文文档可能被判为"英文为主"而反向翻译**。建议真机用一篇技术文章试一次。

---

## 六、给接手平台的整改清单（按优先级）

| 优先级 | 事项 | 位置 | 工作量 |
|---|---|---|---|
| **P0** | 修 `volatile\` 孤儿文件：按 `entry.Hash` 一并清理 `.txt` / `.html` | `HistoryStore.cs` `DeletePayload` / `EnforceQuota` / `ClearAll` | 小 |
| **P0** | 人工验一遍悬浮窗不抢焦点（§五） | — | 30 秒 |
| **P0** | 「按热键就读当前页面」：要么让它真读，要么改文案与验收单（**否则用户照验收单走会判不通过**） | `AiTool.cs:45-65` / `AiPanel.xaml:68` / `05-验收清单.md:126` | 小 |
| **P1** | 「只取主内容区」空开关：落地降噪，或删掉这个承诺 | `TextGrabber.cs:199-204` / `AiPanel.xaml:77` | 中 / 极小 |
| **P1** | Office COM 超时改为可中断或杀进程兜底 | `OfficeComConverter.cs:163` | 中 |
| **P1** | 缩略图按需加载或加缓存 | `ClipboardPanel.xaml.cs:116` | 小 |
| **P1** | AI 探活加超时 + 允可取消（防黑洞地址永久卡住） | `AiPanel.xaml.cs:311` | 小 |
| **P2** | 改 UI 提示 `Ctrl+D` → `Ctrl+Delete` | `ClipboardPanel.xaml:147` | 极小 |
| **P2** | 视频/音频给专门拒绝提示 | `ConvertWindow.xaml.cs:185` | 极小 |
| **P2** | AI 读选区前校验焦点窗口归属 | `TextGrabber.cs:52` | 小 |
| **P2** | `HttpClient` 改为复用（单例或 `IHttpClientFactory`） | `OllamaClient.cs` / `AiPanel.xaml.cs:307,369` | 小 |
| **P2** | `_running` 守卫提前到 await 之前 | `AiPanel.xaml.cs:300` | 极小 |

---

## 七、质检方法说明（可复现）

1. 独立跑交付方二进制自检：`dist\Toolbox.exe --selftest`（读 `%LOCALAPPDATA%\桌面工具箱\selftest\selftest-report.json`）
2. 自写热键探针 `artifacts\qc\HotKeyProbe.cs`（含对照组，直接调 `RegisterHotKey`）
3. 直接读注册表核对开机自启，不采信程序回报
4. 命令行端到端跑 LibreOffice 转换，检查 PDF 文件头
5. 直接打 `127.0.0.1:11434/api/generate` 验证两个翻译方向
6. 逐字阅读核心源码（监听窗口、悬浮窗、焦点追踪、存储、提示词、超时）
7. **对交付方自述的每一条关键断言，优先找反例**；发现自己的假设错了就当场纠正

> 一条重要教训：本轮我最初把两个"只读核查"任务下发给本机 7B 模型，它返回的"原文引用"是**编造的**（例如声称某文件里有本来不存在的常量、还给出 `Debug.Assert(data.Virus)` 这种东西）。**已按护栏规则整份丢弃，改由我自己核。** 涉及结论与判断的活不下发给小模型。

---

*本报告只反映 2026-09-17 这一次质检的快照。*
