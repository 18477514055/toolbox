# 桌面工具箱 · 项目交接文档（自包含）

> ⚠️ **状态更新（2026-09-17，质检整改后）：本文档描述的需求已全部实现完毕，并已通过第三方质检整改。**
>
> 本文档是**动手前**的需求与设计底稿，保留原样作为需求依据。
> 想知道**现在是什么状态、怎么跑、怎么验收**，请看：
>
> | 想看什么 | 看哪份 |
> |---|---|
> | 现状速览 + 怎么上手 + 依赖分层（能不能分享给别人） | `00-先看我.md` |
> | 照着点一遍的验收清单 | `05-验收清单.md` |
> | 第三方质检报告（外部产出） | `06-第三方质检报告.md` |
> | 对质检报告的逐条整改回复 | `07-质检整改回复.md` |
> | 实施期踩的坑 / 实测数据 / 最终形态 | `DECISIONS.md` §六~§十一 |
>
> 交付物：`dist\Toolbox.exe`（单文件自包含 65.1 MB）；自检 `--selftest` **28/28** 通过，
> `--selftest --ai` **29/29** 通过。四个工具（剪贴板 / 裁剪 / 转换 / AI）均已实现，
> 另含无快捷键模式、剪贴板日期筛选、AI 远端后端（可分享）、开机自启加固。

> **给接手方的说明**：这份文档不需要任何历史对话就能读懂。它包含：项目要做什么、已经做到哪一步并**如何验证过**、环境事实、以及接下来三个新工具的要求与设计。
>
> 交接时间点：P0 阶段（抓取探针）已完成并通过验收。主程序尚未开写。
>
> 一句话背景：用户想要一个 Windows 桌面常驻的「工具箱」，里面挂若干小工具；第一个工具是快捷剪贴板，现在要再加三个。

---

## 〇、原始需求（用户原话整理）

> 一个桌面工具箱，方便我在电脑上使用，里面会有各种各样的工具。除了快捷剪贴板，还要有：
> 1. **图片裁剪工具**：两个功能——① 用切割的方法裁剪；② 改变图像的像素（尺寸）。
> 2. **文件格式转换工具**：类似格式工厂，但**更轻量、更便捷**。
> 3. **AI 工具**：快捷调起本地 AI（已部署千问 2.5 7b），进行**页面读取、总结和翻译**。
>
> 补充确认：AI 工具**要能整页读取，含滚动长页**；格式转换第一版**做图片 + 文档/PDF**，不含视频音频。

---

## 一、项目现状：什么能做、什么已验证

### 1.1 结论

**快捷剪贴板的核心风险已经排掉，不是纸上谈兵。** 三类内容均已在真机上验证抓取成功。

### 1.2 已完成的验证（可复现）

| 验证方式 | 命令 | 结果 |
|---|---|---|
| 自动化自检 | `ClipboardProbe.exe --selftest` | **3/3 通过**（文本/图片/文件各一项） |
| 活体验证 | 探针后台常驻，外部真实复制 | 中文+emoji 文本 31 字、2 个文件共 950 B，全部抓到，字节数精确 |

**实测证据**（取自 `artifacts\probe\history-*.jsonl`，真实输出）：

```json
{"Seq":1,"Time":"2026-09-17 09:02:21.724","Kind":"text","Formats":["UnicodeText"],
 "Hash":"d52b1fb2…","Bytes":51,"TextPreview":"验收文本：中文 English 混排 🎯 267876575",
 "TextLength":31,"Duplicate":false,"Attempts":1,"ReadMs":106}

{"Seq":4,"Time":"2026-09-17 09:02:22.741","Kind":"files","Formats":["FileDrop","FileNameW","FileName"],
 "Bytes":950,"Files":[{"Path":"C:\\Windows\\System32\\drivers\\etc\\hosts","Exists":true,"Size":858},
 {"Path":"C:\\Windows\\win.ini","Exists":true,"Size":92}],"Attempts":1,"ReadMs":109}
```

### 1.3 三类内容对应的 Windows 剪贴板格式

| 用户复制的东西 | 剪贴板格式 | 读取 API |
|---|---|---|
| 文本（含网页/Word 富文本） | `CF_UNICODETEXT` (+`HTML Format`+`Rich Text Format`) | `Clipboard.GetText()` |
| 图片（截图 / 复制图片） | `CF_BITMAP` / `CF_DIB` / `PNG` | `Clipboard.GetImage()` |
| 文件（资源管理器 Ctrl+C） | `CF_HDROP` (`FileDrop`/`FileNameW`) | `Clipboard.GetFileDropList()` |

监听机制：`AddClipboardFormatListener`（**系统级通知，非钩子、非注入**）+ 一个隐藏消息窗口收 `WM_CLIPBOARDUPDATE` (0x031D)。

---

## 二、环境事实（实测，不要凭猜）

| 项 | 值 | 影响 |
|---|---|---|
| 系统 | Windows 11 build 26100 | — |
| **.NET SDK** | **9.0.317 / 8.0.424** | 可直接编译 |
| **WindowsDesktop 运行时** | **9.0.19 / 8.0.30** | 可出**框架依赖小体积 exe**（约 1-2 MB，免安装） |
| **NuGet 本地缓存** | **不存在**（`~\.nuget\packages` 无） | ⚠️ **引任何第三方库都要联网下载**。现方案刻意做成**零第三方依赖** |
| Node / npm | v24.20.0 / 11.19.0 | 项目未使用 |
| Rust / cargo | **没有** | 排除 Tauri |
| WebView2 运行时 | 153.0.4234.32 | 有，未使用 |
| PowerShell | **5.1**（不是 7） | 脚本一律 `.cmd`（`.ps1` 被执行策略挡，实测） |
| **Ollama** | **运行中**，`127.0.0.1:11434` 监听（PID 3964） | AI 工具直接 HTTP 调用 |
| Ollama 模型 | `qwen2.5:7b`、`qwen2.5-coder:7b`、`batiai/qwen3-embedding:4b` | 用 `qwen2.5:7b` |
| **LibreOffice** | **已安装 25.2.7.2**（`soffice` 未进 PATH） | ✅ 文档格式转换的关键依赖 |
| Microsoft Office | 家庭版 2024（zh-cn），Click-to-Run | ✅ 可作为文档转换的备选（COM 自动化） |
| ffmpeg / ImageMagick / pandoc / tesseract | **全部没有** | 视频音频先不做；Markdown↔docx 需另想办法 |
| 截图工具 | `SnippingTool.exe` 存在（Win+Shift+S 可用） | 屏幕捕获可用 |

> ⚠️ **接手方注意**：上表是**本机**实测值。你的执行环境可能不同（比如没有 .NET SDK、没有 Ollama）。**先重新跑一遍探测**再动手，别照搬结论。

---

## 三、已有代码资产（可直接复用）

```
04-其他项目\桌面工具箱\
├─ 00-先看我.md                    入口文档（给人看的）
├─ DECISIONS.md                    ★ 决策与踩坑总账（动手前必读）
├─ 03-快捷剪贴板设计.md             P1 剪贴板工具设计稿
├─ 04-交接文档.md                  ← 本文件
├─ run-probe.cmd                   探针启动器
├─ src\ClipboardProbe\             ★ P0 探针（4 个源文件，全部可用）
│  ├─ Program.cs                   主流程：STA 线程 + 消息循环 + 抓取分类
│  ├─ ClipboardListener.cs         ★ 剪贴板监听（含关键踩坑修正）
│  ├─ ProbeStore.cs                JSONL 索引 + blobs 图片存储 + 滚动统计
│  ├─ SelfTest.cs                  ★ 自动化自检（3 个用例）
│  └─ Log.cs                       日志（控制台 + 文件双写，抗管道断开）
└─ artifacts\                      运行产出（探针生成的证据）
```

**这些东西的价值**：`ClipboardListener.cs` 和「读取顺序 + 重试」这套逻辑是**踩了三个坑换来的**，新工具应当**原样复用**，不要重写。详见下一节。

---

## 四、⚠️ 三个已踩过的坑（重写会再踩一次）

### 坑 1：用 WinForms `NativeWindow` + `Parent = HWND_MESSAGE` 建消息窗口，**收不到剪贴板通知**

- **现象**：`AddClipboardFormatListener` 返回 `true`，剪贴板内容也读得到（回读 24 字、格式 `UnicodeText` 全对），但**一条 `WM_CLIPBOARDUPDATE` 都收不到**。
- **结论**：这类系统广播通知**对窗口创建方式敏感**，别用框架封装的 `CreateParams`。
- **正确做法**：显式 `CreateWindowExW(class="STATIC", parent=HWND_MESSAGE, style=WS_POPUP)`，再 `SetWindowLongPtrW(GWLP_WNDPROC)` 子类化。见 `ClipboardListener.cs`。
- **自查手段**：注册监听后立刻 `PostMessage` 一条 `WM_APP+1` 自投消息。收得到 → 窗口和消息循环是活的，问题在别处；收不到 → 消息循环本身有问题，与剪贴板无关。

### 坑 2：异常从窗口过程逃出去会**打死整个消息循环**

- **现象**：图片用例抛 `NullReferenceException`，紧接着**后面所有步骤全部"零事件"**，看着像"监听坏了"。
- **根因**：异常冒泡出消息循环 → 线程死亡 → 之后不再有任何通知。**比丢一条记录严重得多。**
- **正确做法**（三条都要做）：
  1. 事件回调内**必须**全包 `try/catch`；
  2. `Clipboard.GetImage()` 在某些来源上会直接抛 NRE（.NET 内部实现问题），**必须单独特判并降级**，不能让异常冒泡；
  3. **把「消息循环线程是否存活」作为一项断言输出**。没有这条断言，这个坑会伪装成"监听失效"，能查一整天。

### 坑 3：`Console.ReadLine()` 收工 → 后台运行时探针**秒退**

- **现象**：前台跑正常；重定向 stdin/stdout 后台跑时，`Console.ReadLine()` 因 stdin 立刻 EOF 返回 `null`，**启动即退出**（总事件 0）。
- **连带问题**：stdout 被重定向后读者消失时，`Console.WriteLine` 抛 `IOException`，**直接打断事件处理**。
- **正确做法**：① 收工等 `Ctrl+C`（`ManualResetEventSlim` + `Console.CancelKeyPress`，`e.Cancel = true` 自控收尾顺序），**不依赖标准输入**；② 所有输出走 `Log.Line`，控制台失败自动降级为只写文件。

### 坑 4（验收时才发现的）：只在退出时写汇总文件 → **被强制结束就什么都没有**

- **现象**：`summary.json` 只在 Ctrl+C 正常退出时写。进程被硬杀（关窗口/注销/重启/任务管理器结束）时，退出钩子根本不执行，汇总文件不存在。
- **已修**：改为**每条事件后滚动写 `stats.json`**（先写 `.tmp` 再原子替换，避免写到一半留下半个 JSON）。
- **验证**：硬杀进程后 `stats.json` 仍在，内容完整（总事件数、更新时间、失败数都对）。
- **通用教训**：**任何"只靠退出钩子落盘"的状态都是不可靠的**，桌面常驻程序尤其如此。

---

## 五、新工具需求与设计

### 通用外壳：工具箱本体

外壳只做四件事：**托盘、主窗口/面板、热键分发、设置**。每个工具实现统一接口：

```csharp
interface IToolboxTool
{
    string Id { get; }             // "clipboard" / "image" / "convert" / "ai"
    string Name { get; }           // 中文名
    string Icon { get; }
    string DefaultHotKey { get; }  // 默认热键
    void Start(ToolboxContext ctx); // 注册监听、订阅热键
    void Stop();
    object CreatePanel();           // 面板 UserControl
}
```

`ToolRegistry` 启动时组装工具列表（P1 硬编码即可，工具多了再改反射扫描）。
**目的**：以后加工具只需新增一个 `Tools\Xxx\` 目录，**不动外壳一行代码**。

**热键分配建议**（避免冲突，全部可配置）：

| 工具 | 建议热键 | 理由 |
|---|---|---|
| **悬浮窗显示/隐藏** | `Win+Shift+Space` | ⭐ 悬浮窗是主要入口，要能随时收起/唤出 |
| 快捷剪贴板 | `Win+Shift+V` | 与系统 `Win+V` 明确区分 |
| 快捷 AI · 面板 | `Win+Shift+A` | 打开面板，再选模式 |
| 快捷 AI · **翻译选中** | `Win+Shift+T` | ⭐ **一键直达**：选中文字 → 按 → 直接出译文（最高频，值得单独占一个热键） |
| 图片裁剪 | `Win+Shift+X` | — |
| 格式转换 | `Win+Shift+C` | — |

> 热键数量已到 6 个：**必须做成可配置**，且注册失败（被别的程序占用）时要给明确提示，不能静默失效。

**⚠️ 热键注意**：`RegisterHotKey` 会**独占**热键，注册失败要给出明确提示（多半被别的程序占了），不能静默失效。

**通用交互模式**：无边框窗口、`Topmost`、失焦自动收起（`Deactivated`）、Esc 关闭、`ListBox` 开虚拟化。
**唤出位置**：鼠标所在屏幕的右下角附近（比居中更不容易挡视线）。

---

### 通用外壳 · 悬浮窗（用户明确要求的最终形态）

> 用户原话："最终做出来的工具箱应该有一个小悬浮窗，可以便捷使用功能。"

悬浮窗是整个工具箱的**主要入口**，不是附属品——把它当成"手机上的快捷面板"来做。

#### ⛔ 铁律：悬浮窗**绝不能抢焦点**

这一条不是体验偏好，是**功能正确性的前提**，比好看重要一百倍：

> AI 工具靠 UI Automation 读"当前窗口"。**只要悬浮窗抢了焦点，"读当前页面"就会读到悬浮窗自己**，而不是用户的网页。

所以：

| 要求 | 做法 |
|---|---|
| 不抢焦点 | WPF `Window` 设 **`ShowActivated = false`**，并在 `OnSourceInitialized` 里加扩展样式 **`WS_EX_NOACTIVATE` (0x08000000)**——`ShowActivated` 只保证"显示时不激活"，**点击仍会激活**，必须再加 `WS_EX_NOACTIVATE` 才能做到"点了也不抢" |
| 不进 Alt+Tab | 同时加 **`WS_EX_TOOLWINDOW` (0x00000080)** |
| 不打断用户 | 点击悬浮窗后，焦点仍在原程序窗口；用户手上正在编辑的东西不受影响 |

**配套要求**：维护"最近一个非本工具的焦点窗口"时，**必须排除悬浮窗和所有工具箱自己的面板**，否则记录里存的就是我们自己。

#### 外观与交互

```
    ┌──────┐
    │  ✥   │  ← 拖动柄（按住可移动整个悬浮窗）
    ├──────┤
    │  📋  │  剪贴板历史            Win+Shift+V
    │  🌐  │  翻译当前页面           Win+Shift+A
    │  ✎   │  翻译选中文本           Win+Shift+T
    │  ✂   │  图片裁剪              Win+Shift+X
    │  🔄  │  格式转换              Win+Shift+C
    ├──────┤
    │  ⚙   │  设置
    │  ⌃   │  收起 / 隐藏到托盘
    └──────┘
```

| 特性 | 说明 |
|---|---|
| **竖排小图标条** | 默认约 44×44 px 每格，紧凑；鼠标悬停显示 tooltip（含热键提示） |
| **半透明 + 悬停变实** | 平时半透明不挡视线（建议 0.6），鼠标移上去变不透明 |
| **可拖动 + 记忆位置** | 位置存到设置里；**多显示器要按"工作区百分比"存**，否则外接屏拔了会跑到屏幕外 |
| **可收起** | 收成一个小圆点/单图标，`Win+Shift+Space` 或点击展开 |
| **状态提示** | AI 正在流式生成时，对应图标显示进度环（**否则用户不知道它在干活**） |
| **长任务可取消** | 悬浮窗上点一下对应图标即可中断 |
| **高 DPI 安全** | 用 WPF 的 DPI 感知，别用硬编码像素；缩放 125%/150% 下不能错位 |

#### 悬浮窗与"结果展示"的分工

悬浮窗**只放动作入口，不放结果**。翻译/总结结果需要一个**独立的结果面板**（可调整大小、可复制、可编辑将要送出的文本）。理由：结果可能上万字，塞进悬浮窗会让它既难看又没法用。

#### 与其他工具的联动

- 打开**图片裁剪**或**格式转换**这种需要大窗口的工具时，**悬浮窗可以临时隐藏**（设置项，默认隐藏），避免挡着操作。
- 悬浮窗显示的按钮**可配置**（用户可以隐藏不常用的，比如只留翻译和剪贴板）。

---

### 工具 2：图片裁剪

#### 需求拆解

| 功能 | 用户原话 | 设计 |
|---|---|---|
| 切割裁剪 | "用切割的方法来裁剪" | 交互式框选区域 → 裁出该矩形；支持固定比例（1:1 / 16:9 / 自由） |
| 改变像素 | "改变图像的像素" | 改像素尺寸（宽/高），支持锁定宽高比、常用预设、插值质量选择 |

#### 技术方案（零第三方依赖）

WPF 自带即可完成，**不需要任何 NuGet 包**：

| 能力 | API |
|---|---|
| 解码图片 | `BitmapDecoder.Create` / `BitmapFrame` |
| 裁剪（切割） | `CroppedBitmap`，或 `WriteableBitmap` + `CopyPixels` 手动搬像素 |
| 改像素尺寸 | `TransformedBitmap(ScaleTransform)`（快、质量一般）或 `BitmapFrame` + `BitmapScalingMode.HighQuality` |
| 显示/框选 | WPF `Image` + `Canvas` / `Thumb` 拖拽命中，或 `InkCanvas` 做成"遮罩"效果 |
| 编码输出 | `PngBitmapEncoder` / `JpegBitmapEncoder` / `BmpBitmapEncoder` / `GifBitmapEncoder` / `TiffBitmapEncoder` |

**建议的输入来源**（很重要，决定好不好用）：

1. **剪贴板里的图片**——⭐ 最顺手：截图（Win+Shift+S）后直接按热键就能裁。
2. 文件拖拽进来。
3. "打开文件"对话框。

> **联动价值**：剪贴板工具已经在抓图片了，图片工具直接读剪贴板即可，用户零操作成本。**建议先做第 1 条。**

#### 验收标准

1. 从剪贴板拿到图片后，能框选区域并裁出正确尺寸（像素精确到 1px）。
2. 输入目标宽高后输出的实际像素与输入一致（不是"看起来差不多"）。
3. 支持 PNG/JPG/BMP 输出，保存后能正常打开。
4. 锁定宽高比时，改宽自动算高。
5. 原图不被修改（**永远另存，不覆盖**）。

#### 风险与坑

- **EXIF 方向**：手机照片带旋转元数据，不处理会出现"打开是竖的、裁出来是横的"。用 `BitmapFrame.Create` 默认会应用方向，但要留意 JPEG 的特殊情况。
- **超大图**：一张 8000×6000 的图 × 32bpp ≈ 192 MB 内存。要么限制尺寸，要么分块处理。
- **DPI**：96 vs 144 DPI 下 `PixelWidth` 与显示尺寸不同，裁剪必须按**像素**算而不是按显示尺寸算。

---

### 工具 3：文件格式转换

#### 需求拆解

"类似格式工厂，但**更轻量、更便捷**"。第一版范围（用户已确认）：**图片 + 文档/PDF，不含视频音频**。

#### 建议的转换矩阵（先做这些）

**图片**（WPF 自带编解码器，零依赖）：

| 从 | 到 | 说明 |
|---|---|---|
| PNG / JPG / BMP / GIF / TIFF | PNG / JPG / BMP / TIFF | 全部可做，且可顺带缩放和质量调节 |
| 任意 → PDF | — | 用 WPF 打出图片页再合成，或调 LibreOffice |
| HEIC / WebP | — | ⚠️ 需系统扩展或额外库，**建议标为"可选"** |

**文档**（本机有现成重炮，**不用自己写转换器**）：

| 路径 | 依赖 | 本机状态 | 说明 |
|---|---|---|---|
| **LibreOffice 无头转换** | `soffice.exe --headless --convert-to <fmt> --outdir <dir> <file>` | ✅ 已装 25.2.7.2（**未进 PATH，需定位安装目录**） | ⭐ 首选：一个命令搞定 docx/xlsx/pptx/odt/html → pdf/docx/txt 等，不开 GUI、可并发、免费 |
| Office COM 自动化 | `Word.Application` 等 | ✅ Office 2024 已装 | 备选：保真度更高（尤其 PDF→docx），但慢、会弹窗、有 COM 坑 |
| PDF → 文本/图片 | LibreOffice 的 PDF 导入能力有限 | — | ⚠️ `soffice --convert-to` **不能可靠读 PDF**。用 Office 打开 PDF 另存，或引入 PdfPig 类库（**注意本机无 NuGet 缓存，需要联网**） |
| Markdown ↔ docx | pandoc | ❌ **未安装** | 需要额外下载，**建议标为可选** |

> **设计建议**：转换后端做成**可插拔策略接口** `IConverter`，LibreOffice 与 Office 各实现一份，按文件类型自动挑。这样任一路径出问题都能切另一条，而不是把用户堵死。

#### 便捷性设计（"轻量便捷"的落点）

- **拖拽即转**：文件拖到托盘图标 / 面板里即开始。
- **右键菜单集成**（可选）：资源管理器右键"用工具箱转换"。
- **批量队列**：多文件排队，显示每条进度与结果。
- **记住上次输出格式与目录**。
- **转换前预检**：目标格式是否支持、依赖是否可用（比如 LibreOffice 没找到就明确提示，而不是转半天报个看不懂的错）。

#### 验收标准

1. 图片互转：PNG→JPG、JPG→PNG、BMP→PNG 均正确，输出可打开。
2. 文档转换：`.docx → .pdf`、`.xlsx → .csv`、`.pptx → .pdf` 至少各成功一次。
3. 批量 10 个文件不卡界面（后台线程 + 进度）。
4. 依赖缺失时有**明确的中文提示**，不是异常堆栈。
5. 输出文件不覆盖源文件（同名时自动加 `(1)`）。

#### 风险与坑

- **`soffice.exe` 并发限制**：同一用户下多实例可能冲突（用户配置锁），建议**串行或限制并发数**，并优先复用已运行的实例。
- **路径与编码**：中文路径跨进程传递有风险（本机已踩过 mangled），命令行参数要正确加引号，必要时用短路径或临时目录中转。
- **等待子进程退出**：`--convert-to` 是异步的，必须等进程结束再报成功，否则会出现"报成功但文件没生成"。
- **大文件超时**：给每个转换设超时，别让一个坏文件卡住整个队列。

---

### 工具 4：快捷 AI（本地千问）

#### 需求拆解

"快捷调起本地 AI（已部署千问 2.5 7b），进行**页面读取、总结和翻译**"。

**用户已确认的三条功能需求**（这是硬需求，不是建议）：

| # | 需求 | 用户原话 |
|---|---|---|
| 1 | 页面读取**两种方式都保留** | "这个读取页面，我希望两个方式都保留" |
| 2 | 翻译要支持**三个入口** | "翻译当前的这个页面 / 翻译我选中的文本 / 翻译剪贴板类的东西" |

**读取的两种方式（都要做）**：

| 模式 | 取什么 | 实现 | 特点 |
|---|---|---|---|
| **整窗口模式**（按热键就读） | 当前窗口/页面的全部文本 | UIAutomation TextPattern 取焦点窗口文档全文 | 省事，但会带上导航、广告、评论区等噪声 |
| **选中模式**（先选中再读） | 只有用户选中的那一段 | **UIAutomation `TextPattern.GetSelection()`**——这是"读选区"的正解，**不碰剪贴板** | 精准，适合"只总结这一段" |

> ⚠️ **整窗口模式的噪声问题要正面处理**，不能让用户自己去删广告：
> 1. **默认只取主内容区**（找页面里最大的文本块 / 主文档控件），界面上给一个 **"整页 / 仅主内容"开关**；
> 2. 送进模型前做**轻量清洗**（去掉连续空行、去重重复行、截掉明显是导航/页脚的行）；
> 3. **界面上显示"读到 N 字"并让用户能看到/编辑将要送出的文本**——用户能一眼发现"读多了"并手动删掉。这一条比任何自动清洗都可靠。

> ⚠️ **"当前窗口"的判定有个坑**：工具自己唤出面板后会**抢走焦点**，此时 UIA 读到的是我们自己的面板。
> → 必须在唤出前记下**上一个非本工具的焦点窗口**（维护一个"最近焦点窗口"记录），唤出后再用它来读，不能读当前焦点。

**翻译的三个入口（都要做）**：

| 入口 | 数据来源 | 产出 | 交互建议 |
|---|---|---|---|
| **① 翻译当前页面** | 整窗口模式（UIA 读全文） | 整页译文 | 热键 / 面板按钮；长文自动分块 |
| **② 翻译选中文本** | UIA `GetSelection()`（**零污染**） | 选中段译文 | **默认热键直达**：选中即按，出译文，全程不碰剪贴板 |
| **③ 翻译剪贴板内容** | **只读**当前剪贴板，**不写回、不改动** | 剪贴板内容译文 | 面板按钮。**安全**：因为只读不写，不违反"不污染"约束 |

> **关于入口 ③ 的一个重要澄清**：它**不会**污染剪贴板——我们只是**读**剪贴板的内容去翻译，一个字都不写回去。
> 真正会污染的是"把译文写回剪贴板"这个动作，那属于用户主动点的"复制结果"，按硬性规则③打标记、不进档案即可。
> **"读剪贴板"和"写剪贴板"必须区分对待**，不要把入口 ③ 误判成违规而砍掉。

**三种翻译的呈现**应统一：左原文 / 右译文（可按段落对齐），都带「复制译文」按钮。

**语言对：固定中英双向（用户已确认）**

> 用户原话："先固定中文和英文，双向吧，目前也只用这个。"

| 规则 | 说明 |
|---|---|
| 只做 **中文 ↔ 英文** | 不做语言选择器（省事且更可靠） |
| **自动判方向** | 检测输入文本以中文为主还是英文为主（按 CJK 字符占比，阈值建议 >30% 视为中文），据此决定译成英文还是中文 |
| **提示词必须写明方向** | ⚠️ **不要写"翻译成中文"这种含糊话**，要写全："把下面的内容翻译成**英文**，只输出译文，不要解释"。7B 模型对含糊指令会自己猜方向，猜错时质量明显下降 |
| **保留代码/术语** | 中英混排时，代码片段、专有名词、URL 应原样保留，提示词里要明确要求 |
| **未来扩展** | 若以后要加日语等，方向检测应做成可插拔的小函数，别把"中英"硬编码在调用处 |

> 顺带一提：**中译英**对 7B 模型比**英译中**更容易出错（术语、语气）。建议提示词里加一句"保持原文的语气与专业程度"，并允许用户在结果面板里手动改几个词再复制。

#### 技术方案

**调用本地模型**：Ollama 的 HTTP API，**零依赖**（`HttpClient` + `System.Text.Json` 即可）：

| 用途 | 接口 |
|---|---|
| 生成/总结/翻译 | `POST http://127.0.0.1:11434/api/generate`（流式）、`/api/chat`（对话式） |
| 模型清单 | `GET http://127.0.0.1:11434/api/tags` |
| 视觉模型（若上 OCR 替代方案） | `/api/generate` 传 `images` 字段（需另装 llava / qwen2-vl 等） |

> **不要硬编码 base url 和模型名**：做成设置项（默认 `127.0.0.1:11434` + `qwen2.5:7b`），这样以后换模型、换云 API 都不用改代码。

#### ⚠️ 「整页读取（含滚动长页）」—— 这是三个新工具里最难的一项，务必看清

##### 先说一条**全局约束**（用户明确提出的，违反它整个设计就错了）

> **不许污染剪贴板。** 用户原话："如果浏览器把整页文本放进剪贴板的话，那剪贴板会被污染啊。我要用的东西都被挤到下面去。"

这条约束背后是一个**架构原则**，比"用户体验"更根本：

> **剪贴板是"数据通道"，剪贴板历史是"用户档案"。两者必须分开。**
> 凡是往剪贴板写东西的操作，都会自动变成历史里的一条记录。所以**为了读数据而写剪贴板 = 往用户档案里塞垃圾**，还会把用户真正要用的内容挤下去。

**由此推出的三条硬性设计规则：**

| 规则 | 说明 |
|---|---|
| **① 尽量不要写剪贴板** | 读页面能不用剪贴板就不用（见下方路径 B）——这是唯一真正干净的方案 |
| **② 非要用，就必须还原** | 用之前备份原剪贴板内容，用完立刻放回去（见下方"路径 A 的正确用法"） |
| **③ 我们写的内容一律不进历史** | 工具箱自己写剪贴板时打标记（同一进程内可控），事件到达时识别并跳过归档 |

> ⚠️ **不要试图通过"来源进程"来过滤**：想判断剪贴板最后是谁写的，需要 `GetClipboardOwner`（常常返回 0）或 `OleGetClipboard` 拿 owner cookie，**第三方进程不可信**。跨进程识别不要做，只做"我们自己写的我们标记"。

##### 三条可选路径

| 路径 | 适用 | 污染剪贴板？ | 可靠性 |
|---|---|---|---|
| **A. 剪贴板**（用户自己全选复制后调起） | 网页、Word、PDF 阅读器、任何能选中的文本 | 会（除非按下面方式还原）；**但这是用户自己的主动操作，不是工具偷偷干的** | ⭐⭐⭐⭐⭐ |
| **B. UI Automation**：从焦点窗口读文档文本，**完全不碰剪贴板** | 浏览器（Chrome/Edge/Firefox）、Office、记事本等标准控件 | **零** | ⭐⭐⭐ 一般（能读到多少取决于程序） |
| **C. 屏幕捕获 + OCR** | 图片、扫描件、游戏、画布类应用、受保护内容 | **零** | ⭐⭐ 最脆弱 |

##### ⛔ 一条明确否掉的路：模拟 `Ctrl+A`/`Ctrl+C` 自动抓取

**不要做。** 它同时犯三个错：
1. 把整页内容塞进剪贴板（污染档案）；
2. **抢走用户当前复制的内容**；
3. 抢焦点、打断用户正在做的事。

> 这条是本文档早期版本的错误建议，**已作废**。看到旧版本请忽略。

##### 路径 A 的正确用法（如果要让用户复制，就必须做到这三点）

1. **必须是用户主动操作**，不能由工具模拟按键；
2. **调起时先把原剪贴板内容备份下来**，拿到文本后立刻还原——用户不该因为用了一次 AI 就丢掉手上正在粘贴的东西；
3. **还原也要有兜底**：还原失败（有些应用不允许）时，在界面上明确告诉用户"原剪贴板内容未能还原"，而不是静默吞掉。

> ⚠️ 还原的保真度限制：只备份纯文本 / 图片 / 文件列表，**HTML 与 RTF 通常无法还原**（会降级成纯文本）。备份要带格式清单，还原时降级不可怕，可怕的是**悄悄降级**。

##### 推荐的落地顺序

```
第一步：路径 B（UIAutomation 直读）—— 零污染，先把它做出来，覆盖浏览器与 Office
第二步：路径 A（用户主动复制 + 自动还原 + 不进历史）—— 兜底
第三步：路径 C（手动框选 + 捕获 + OCR）—— 只在明确需要时做，绝不自动滚动截图
```

##### 「滚动长页」怎么解决才不脏

**正解**：用户在浏览器里 `Ctrl+A`/`Ctrl+C` 时，浏览器会把**整页文本（包括可视区外的全部内容）**一次性放进剪贴板 —— 根本不需要截图拼接。配合上面的"还原 + 不进历史"两条，这条路既干净又可靠。

**反面教材**（真要做自动滚动截图时才会遇到的坑，能不做就别做）：
- 需要模拟滚动（`SendInput` 滚轮）或操作滚动条；
- 相邻两屏有重叠 → 必须去重拼接；
- 吸顶导航/浮动广告会被重复抓进去；
- 到达底部要能判断，否则死循环；
- 整个过程几十秒，必须有进度与可中断。

##### OCR 的实现选择（只在真需要路径 C 时才选）

1. **Windows 自带 OCR**（`Windows.Media.Ocr`）：免费离线。需把 TFM 设为 `net9.0-windows10.0.19041.0` 以用 WinRT 投影。⚠️ 中文准确率一般，且**必须先确认中文 OCR 语言包已安装**（本机用 PS 5.1 探测失败是因为 PS 5.1 无 WinRT 投影，**要在真实 .NET 项目里验证，别在 PS 里下结论**）。
2. **给 Ollama 装视觉模型**（llava / qwen2-vl 等）：质量好、能力统一。⚠️ 要再下几 GB，核显上很慢。
3. **Tesseract**：本机**未安装**，需额外下载 + 中文语言包。

#### 功能设计

**取文本（两种读取模式）**：

| 模式 | 做法 | 污染 |
|---|---|---|
| **整窗口** | 读"唤出前最后一个非本工具的焦点窗口"的 UIA 文档全文；默认只取主内容区，可切"整页" | 零 |
| **选中** | `TextPattern.GetSelection()` 读当前选区 | 零 |
| 降级（两者都失败时） | 提示用户"这个程序读不到，请选中后按 Ctrl+C 再试"——**由用户自己按**，工具不代按 | 无（但会改剪贴板，按规则②还原、按规则③不进档案） |

**AI 操作（几个入口并列）**：

| 操作 | 输入来源 | 做法 |
|---|---|---|
| **总结** | 整窗口 或 选中 | 分块 → `qwen2.5:7b` 逐块摘要 → 二次汇总（map-reduce）；短文本一步到位 |
| **翻译当前页面** | 整窗口 | 分块翻译 + 汇总；长文不要在界面上一次性铺开，按段懒加载 |
| **翻译选中文本** | 选中（UIA 直读） | 单段翻译，**默认热键直达**（选中→按→出译文，全程零污染） |
| **翻译剪贴板内容** | **只读**剪贴板 | 不写回、不改动；结果可选中复制 |
| **自由问答**（可选） | 用户输入 | 顺带支持，成本很低 |

> 以上三种翻译**统一为中英双向自动判方向**，不做语言选择器。

**三种翻译的呈现统一**：左原文 / 右译文，可按段落对齐；都带「复制译文」按钮。

**输出与剪贴板的关系（务必分清"读"和"写"）**：

| 动作 | 是否违反"不污染" | 处理 |
|---|---|---|
| 读剪贴板内容去翻译（入口③） | **不违反** | 纯读取，一个字都不写回去 |
| 把译文写回剪贴板（用户点"复制译文"） | 不违反（用户主动要） | **打 `Origin=tool` 标记，不进档案** |
| 工具自己偷偷写剪贴板搬数据 | **违反** | 禁止 |

**流式输出是必须的**：7B 模型在核显上首 token 要几秒到十几秒，不流式用户会以为卡死。用 `/api/generate` 的 `stream: true`。

**token 预算**：`qwen2.5:7b` 上下文 32K，但长输入显著变慢。建议单次上限约 3000-4000 字，超了分块。
⚠️ **"翻译当前页面"会轻易超过这个上限**（一篇长文上万字）→ 必须走分块，且**要显示进度**（"正在翻译 3/8 段"），否则用户以为死了。

#### 验收标准

1. 按热键唤出 AI 面板，若干秒内拿到回答（**流式可见**）。
2. **读取两种模式都要过**：
   - 整窗口模式：在浏览器里按热键，能读到当前页面正文，界面显示"读到 N 字"，且能查看/编辑将要送出的文本；
   - 选中模式：选中一段后触发，**只**得到这一段（没有把整页读进来）；
   - 两者都**不改变剪贴板**。
3. **翻译三个入口都要过**（**统一中英双向、自动判方向**）：
   - ① 翻译当前页面：一篇 2000 字以上页面能产出完整译文（分块链路通，有进度显示）；
   - ② 翻译选中文本：选中即按即得译文，质量可用（7B 水平）；
   - ③ 翻译剪贴板内容：翻译后**剪贴板内容不变**（这一条要专门测）；
   - ④ **方向判断要准**：中文输入 → 输出英文；英文输入 → 输出中文。**两个方向都要测，不能只测一个**。
4. 一篇 3000 字以上的文章能产出结构化摘要（分块→汇总链路通）。
5. **零污染验收**：用 AI 工具做完整整一轮读取+翻译后——
   - 剪贴板里**仍然是**用户调起前的原内容；
   - **剪贴板历史里不出现**这次操作产生的条目；
   - 用户手上正在粘贴的东西没有被破坏。
6. 读不到的程序（自绘 UI / 部分 Electron 应用）有**明确的中文降级提示**，不是静默失败。
7. **Ollama 没启动时给出明确提示**（"请先启动 Ollama"），不是超时卡死。
8. 长任务可中断（用户不想等了能停）。
9. **悬浮窗验收**：
   - 点击悬浮窗上的按钮后，**焦点仍停在原程序窗口**（不是悬浮窗）；
   - 悬浮窗**不出现在 Alt+Tab 列表**里；
   - 悬浮窗显示时，"读当前页面"**仍然读到的是用户的页面**，不是悬浮窗自己；
   - 拖动位置后重启程序，位置被记住；
   - 125% / 150% 显示缩放下不错位；外接屏拔掉后不会跑到屏幕外。

#### 风险与坑

- **模型服务未启动**：先探测 `11434`，给明确指引。
- **超时**：首次加载模型可能 10-30 秒，HTTP 超时要放宽（或靠流式判断活性）。
- **显存/内存**：7B 靠内存跑，**别在转换大文件时并行做长文本总结**。
- **隐私**：本地模型 = 数据不出机器，这是优点，值得在界面上写出来。
- **上下文超限**：长文本必须分块，否则模型静默截断（用户会以为"总结漏了"）。
- **中文提示词**：7B 对中文指令遵循一般，提示词要短、结构化、明确要求输出格式。
- **UIAutomation 的现实**：不同程序暴露的文本能力差别很大；某些应用（尤其自绘 UI、部分 Electron 应用）读不到，`GetSelection()` 也可能返回空。**必须有降级路径**，且降级时要告诉用户"这次是从剪贴板拿的"。
- **焦点窗口判定**：唤出面板会抢焦点，必须读"唤出前最后一个非本工具窗口"，否则读到的是我们自己的面板（详见 §五·工具4 需求拆解里的警告）。
- **整窗口模式的噪声**：导航栏/广告/评论会被一起读进去。界面必须让用户看到并编辑将要送出的文本，这是最可靠的兜底。

---

## 六、通用工程约定（沿用已验证的做法）

1. **编码**（Windows 中文环境下的坑）：
   - `.cs` / `.md` / `.csproj` = **UTF-8 无 BOM**；
   - `.cmd` = **GBK + CRLF**（本机现在这几个 `.cmd` 是 `chcp 65001` + UTF-8，实测可用；如果要写成 GBK 也行，**但不要混**）；
   - ⚠️ **绝不对含中文的文件做"读取→替换→写回"**——容易写坏中文。用编辑器的定点替换，或整份重写。
2. **路径要用绝对路径交付**：给用户的文件必须写完整路径（用户不知道到哪去点）。
3. **随手留证据**：每个结论写进 `DECISIONS.md`，带出处（`文件:行号` 或实测输出）。
4. **工作区纪律**：不为验证而删改用户已有文件；产出物集中在新目录里。
5. **验证要求**：**"编译通过" ≠ "功能验证过"**。每个工具都要有可重跑的验收步骤（参考 `SelfTest.cs` 的做法：程序自己制造输入、断言输出、输出通过率）。
6. **PWsh 是 5.1**：`[System.IO.File]::*`、`New-Object` 等在某些受限模式下会被禁；优先用 cmdlet。

---

## 七、建议的实施顺序

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P1** | 工具箱外壳（**悬浮窗（含 `WS_EX_NOACTIVATE`）+ 托盘 + 热键 + 工具注册**）+ 快捷剪贴板（复用探针逻辑 + 列表面板 + 搜索 + 回填）**+「我们自己写的不进历史」标记机制**（见 §五·工具4 的硬性规则③，这是 AI 工具的前置条件） | 能日常替代 Win+V |
| **P2** | 图片裁剪（先做"读剪贴板图片 → 框选 → 保存"） | 截图后按热键即可裁 |
| **P3** | 格式转换（图片互转 → LibreOffice 文档转换 → 批量队列） | 拖拽即转 |
| **P4** | 快捷 AI（**先做 UIAutomation 直读 → 再做剪贴板兜底 + 还原 → 最后才考虑 OCR**） | 按热键即得摘要/译文 |
| **P5** | 开机自启、设置界面（热键可改、按钮可配、路径可配）、发布打包 | 双击即用 |

**为什么这个顺序**：

- **悬浮窗放 P1 而不是最后**——它是主要入口，工具会一个个往上面挂；如果最后才做，前面每个工具都要返工接一遍。
- **`WS_EX_NOACTIVATE` 也必须在 P1 就做对**——它的正确性是 AI 读取功能的前提（见 §五·通用外壳·悬浮窗）。等 P4 才发现"悬浮窗一出现就读不到页面"，返工代价很大。
- 外壳和剪贴板是所有工具的地基（AI 要用剪贴板写入、图片要用剪贴板读取）；**「不进历史」的标记机制必须在 P1 做掉**，否则 P4 做 AI 时会先污染一批数据再返工。
- 图片最简单可快速建立信心；转换工具依赖外部进程、坑较多；AI 的"整页读取"最难，放最后且按污染最少到最多逐级降级。

---

## 八、待确认事项（动手前建议先和用户对齐）

### 已确认的（不要再问一遍）

- ✅ **不许污染剪贴板**——用户明确要求，见 §五·工具4 的全局约束。
- ✅ AI 工具要做**整页读取（含滚动长页）**。
- ✅ AI 读取**两种方式都保留**：整窗口模式 + 选中模式（用户明确："两个方式都保留"）。
- ✅ 翻译要有**三个入口**：翻译当前页面 / 翻译选中文本 / 翻译剪贴板内容（用户明确）。
- ✅ 翻译语言对**固定中英双向、自动判方向**，不做语言选择器（用户明确："先固定中文和英文，双向吧，目前也只用这个"）。
- ✅ **要有小悬浮窗**作为便捷入口（用户明确："最终做出来的工具箱应该有一个小悬浮窗，可以便捷使用功能"），且**绝不能抢焦点**。
- ✅ 格式转换第一版**不含视频/音频**（图片 + 文档/PDF）。
- ✅ 交接文档为**单份自包含**。
- ✅ 技术栈 **.NET 9 + WPF**；V1 粘贴行为**只回填剪贴板，用户自己 Ctrl+V**。

### 仍需确认的

1. **热键分配**是否符合用户习惯（尤其 `Win+Shift+V` 与系统 `Win+V` 的区别是否会让人困惑；以及 `RegisterHotKey` 失败时要给明确提示）。
2. 是否需要**开机自启**；数据存放位置建议 `%LOCALAPPDATA%\桌面工具箱\`（运行时数据，不放进项目目录）。
3. 剪贴板历史对**超长内容**的处理（建议：列表里只显示"文章（3200 字）· 时间"+ 临时文件指针，而不是把整篇塞进条目——**这条独立于 AI 工具，见 §五·工具4 硬性规则②的延伸**）。
4. 是否需要**多语言界面**（当前一切按中文单语言设计）。
5. 接手平台的环境是否与本机一致（**务必重新探测**：.NET SDK / Ollama / LibreOffice 都可能没有）。

---

## 九、快速上手（接手方第一件事）

```powershell
# 1. 看已有代码怎么跑
cd "C:\Users\24239\Desktop\DeepSeek-Workspace\04-其他项目\桌面工具箱"
.\run-probe.cmd                       # 启动探针（Ctrl+C 停止）

# 2. 跑自动化自检（30 秒，会临时改写剪贴板，测完自动恢复）
dotnet run --project "src\ClipboardProbe\ClipboardProbe.csproj" -c Release -- --selftest

# 3. 确认本地 AI 可用
curl http://127.0.0.1:11434/api/tags   # 应返回模型列表

# 4. 确认文档转换可用（先找 soffice.exe 的真实路径）
Get-ChildItem 'C:\Program Files\LibreOffice\program\soffice.exe' -ErrorAction SilentlyContinue

# 5. 动手前先读
#    DECISIONS.md（踩坑总账）→ 03-快捷剪贴板设计.md（P1 设计）→ 本文件
```

---

*本文档自包含：不依赖任何历史对话。若你是通过其他平台接手，请先完整读一遍 §二（环境）与 §四（三个坑）再动手。*
