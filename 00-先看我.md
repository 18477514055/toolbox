# 桌面工具箱 · 先看我

> 一句话：一个常驻托盘的「工具箱」，里面挂了 **12 个**小工具 ——
> **快捷剪贴板 / 图片裁剪 / 格式转换 / 快捷 AI / 快捷截图 /
> OCR 截图识字 / 批量重命名 / 哈希校验 / 二维码工具 / 窗口置顶 /
> 运行命令 / 批量打包**。
>
> 当前状态：**P1→P4 + 质检整改 + 独立验收 + 桌面图标与自启 + 可绿色分享
> + 12 个工具 + 功能开关体系 + 已发布到 GitHub，可交付**。
> 交付物：`dist\Toolbox.exe` —— 单文件、自包含、零运行时依赖，双击即用。
> 自检：`--selftest` **117/117 通过**（含真实模型调用时再多一项）。
>
> **⭐ GitHub**：https://github.com/18477514055/toolbox （公开，MIT）
> Release `v1.0` 里有打包好的 exe，别人可以直接下载。
>
> **本轮（模块化）新增**：
> **功能开关**（总开关 + 每工具开关，决定**是否运行**与**是否显示在悬浮窗**）/
> **热键完全可选**（设置里可一键清空全部热键）/
> **工具管理窗口**（按需下载通道：多镜像 + SHA-256 校验）/
> **已发布到 GitHub**。
> 设计与**能力边界**见 `13-模块化与按需下载.md`。
>
> 上一轮（加菜 5 个工具）：OCR 识字 / 批量重命名 / 哈希校验 / 二维码 / 窗口置顶。
> 每个工具目录下都有自己的 `README.md`（依赖 / 用法 / 已知限制 / 测试脚本）。
>
> 更早：便携模式 + 修掉便携模式暴露的真 bug（见 `12-分享给别人-依赖说明.md`）；
> 独立验收修掉 2 处缺陷（见 `09-独立验收报告.md`）。
>
> 再往前：无快捷键模式 / 剪贴板日期筛选 / AI 远端后端 / 开机自启加固 / 设置「浏览…」按钮……
> 整改明细见 `07-质检整改回复.md`。

---

## 一、最快上手（30 秒）

> ⭐ **本轮已装好桌面图标**：双击桌面上的 **`桌面工具箱`** 即可启动。
> 程序本体已装到 `%LOCALAPPDATA%\桌面工具箱\Toolbox.exe`（稳定位置，不怕工作区被整理）。
> 详见 `11-安装与自启修复说明.md`。

1. 双击桌面 **`桌面工具箱`** 图标（或 `run-toolbox.cmd`）—— 工具箱启动，
   右下角托盘出现图标，桌面上出现一条竖排的小悬浮窗。
2. 点悬浮窗上的图标就能用；或者按热键。

> 悬浮窗**不会抢焦点**（这是硬要求，见下）——你正在打字的窗口不会被打断。

**桌面还有一个 `桌面工具箱-直达\` 文件夹**，里面是各工具 + 设置的直达快捷方式
（记不住热键时用）。**托盘右键 →「所有工具」** 也能点开全部 10 个工具。

**退出**：右键托盘图标 → 退出。（关掉悬浮窗不等于退出程序。）

---

## 二、十个工具与热键

⚠️ **热键请以「设置」窗口里显示的为准**，下表是**本机实测实际生效**的组合键：

| 工具 | 本机实际热键 | 默认热键 | 干什么 |
|---|---|---|---|
| **快捷剪贴板** | `Win+Alt+V` | ~~Win+Shift+V~~ | 查历史复制的文本 / 图片 / 文件；**按日期或关键字筛选**、回车回填、Ctrl+P 置顶、Ctrl+D 或 Ctrl+Delete 删除 |
| **图片裁剪** | `Win+Shift+X` | `Win+Shift+X` | ①框选裁剪 ②改像素尺寸 ③另存（不覆盖原文件） |
| **格式转换** | `Win+Alt+C` | ~~Win+Shift+C~~ | 图片互转（png/jpg/bmp/tiff/gif）+ 文档/PDF（docx/xlsx/pptx/odt/html ↔ pdf/txt/csv） |
| **快捷 AI** | `Win+Alt+A` | ~~Win+Shift+A~~ | 读当前窗口 或 读选中文本 → 翻译 / 总结 / 提问 |
| └ **翻译选中** | `Win+Shift+E` | ~~Win+Shift+T~~ | 一键直达：选中 → 按 → 直接出译文 |
| **快捷截图** | `Win+Alt+S` | `Win+Alt+S` | 整屏冻结 → 拖拽框选 → 松手即存 PNG 并复制到剪贴板；右键 / Esc 取消 |
| **OCR 截图识字** 🆕 | `Win+Alt+J` | ~~Win+Alt+T~~ | 框选屏幕区域 → **识别文字**（中文可用）→ 自动复制到剪贴板；结果可编辑 |
| **批量重命名** 🆕 | `Win+Alt+I` | ~~Win+Alt+R~~ | 选目录 → 前缀/后缀/替换/序号/时间戳 → **先预览再执行**，**可一键撤销** |
| **哈希校验** 🆕 | `Win+Alt+H` | `Win+Alt+H` | 算 MD5/SHA1/SHA256/SHA512；**两个文件对比是否一致**；粘贴期望值比对 |
| **二维码工具** 🆕 | `Win+Alt+Q` | `Win+Alt+Q` | ①文字/链接 → 二维码图片（可存可复制）②框选屏幕二维码 → 读出内容 |
| **窗口置顶** 🆕 | `Win+Alt+P` | `Win+Alt+P` | 把当前窗口置顶；**再按一次取消**（toggle） |
| **运行命令** 🆕 | `Win+Alt+X` | `Win+Alt+X` | 输入命令交给 **cmd** 或 **PowerShell** 执行，输出当场显示（⚠️ 会真实执行，见下） |
| **批量打包** 🆕 | `Win+Alt+G` | `Win+Alt+G` | 批量选中文件 → 打包成 **ZIP**（零依赖）或 **RAR**（需装 WinRAR）；存放位置可选 |

> ⚠️ **「运行命令」是本工具箱里唯一能执行任意命令的功能** ——
> 输入什么就执行什么，能力边界就是你的账户权限。
> 它**绝不自动执行**（单独回车是换行，要按 **Ctrl+Enter**），
> 且执行前会把**最终拼给 shell 的命令行**原样显示出来供你核对。
> 它**不做**危险命令拦截 —— 黑名单永远是漏的，给人"它帮我挡住了"的错觉反而更危险。
> 详见 `src\Toolbox\Tools\CommandRunner\README.md`。
>
> 🆕 = 本批新增。**后 7 个工具各自目录下都有 `README.md`**，
> 写明依赖、用法、**已知限制**和最小测试脚本 ——
> 尤其「二维码工具」「OCR」「运行命令」的能力边界写得很清楚，用之前值得扫一眼。

### 📦 版本形态：5 个内置 + 7 个可下载插件

主程序**只内置 5 个常用工具**，其余 7 个是**可下载插件**
（设置 → 工具管理 → 安装 / 更新；每个 4~26 KB）：

| 内置（开箱即用） | 需下载 |
|---|---|
| 快捷剪贴板 / 图片裁剪 / 格式转换 / 快捷 AI / 快捷截图 | OCR 识字 / 批量重命名 / 哈希校验 / 二维码 / 窗口置顶 / 运行命令 / 批量打包 |

下载走多镜像（直连 / ghproxy.net / gh-proxy.com / ghfast.top），
**每个包都做 SHA-256 校验**，校验不过自动换下一个源。

### ⭐ 功能开关：默认只开 5 个

上表里带 ✅ 的 **5 个默认开启**（剪贴板/裁剪/转换/AI/截图），
其余 **7 个默认关闭** —— 在「设置 → 功能开关」里一键打开。

- **总开关**：一键让整个工具箱安静下来（所有功能停止，程序还在）
- **每个功能一个开关**：决定它**是否运行**、以及**是否显示在悬浮窗上**
- 改完**点保存立即生效**，不用重启
- 关掉的功能**真的会停止**（不注册热键、不监听剪贴板），不是只藏起来

**热键也是可选的** —— 设置里有「清空全部热键」按钮，
清空后工具箱不占用任何快捷键，全靠点悬浮窗，体验一样顺。

### 不想要热键？留空就行

**托盘图标 → 设置 → 热键 → 把那个输入框清空 → 保存。**

清空 = 这个工具**不占用任何热键**，改从悬浮窗或托盘打开。
如果你更习惯点面板而不是记快捷键，这是正规用法，不是"将就"。

- 留空 = 禁用（条目存成空串）
- 填「**默认**」两个字 = 恢复工具自带的键
- 两种意图分开存，所以"禁用"不会被悄悄退回成"用默认键"

### 为什么划掉的是默认键？

因为**它们在本机被别的程序占用了**。实测本机 `Win+Shift+<字母>` 有 10 个已被占：
`A C F M P R S T V W`（其中 S 是 Windows 自带的截图）。

工具箱的做法是：**默认键保留最顺手的那个，被占用时自动降级到备选键**，
并在托盘气泡里明确告诉你换成了什么。所以你会看到实际生效的是 `Win+Alt+V` 而不是 `Win+Shift+V`。

- 想固定成自己喜欢的组合键：**托盘图标 → 设置 → 热键**，改完点保存。
- 你自己改过的键如果也被占用，工具箱**只报错、不会偷偷换**——那是你的选择。
- 详细占用表和降级链见 `DECISIONS.md` §七。

### 💡 不用手敲模型名：点「查找模型」

**托盘图标 → 设置 → 快捷 AI**，模型名那一栏现在是**下拉框**，旁边有「查找模型」按钮。

填好 API 地址和 Key，点一下就能从服务商返回的列表里**直接选**，
不用去翻文档、也不用一个字一个字敲模型名。

| 你选的 | 程序实际做的 |
|---|---|
| 远端 API | `GET {你的地址}/models`（OpenAI 的约定） |
| 本地 Ollama | `GET {地址}/api/tags`（Ollama 自己的协议，与上面完全不同） |

**几个贴心的地方：**

- 地址写法很宽容 —— 带不带 `/v1`、末尾有没有斜杠、甚至把整条
  `/v1/chat/completions` 粘进来，都能拼对
- **模型名写错会直接告诉你该填什么**。比如填 `Qwen3-8B`，
  它会提示"是不是想填 `Qwen/Qwen3-8B`"（少了组织前缀）——
  而这种情况真去用只会得到一句看不出原因的 `400 错误的请求`
- 「测试连接」现在会**真的核对你填的模型名存不存在**：
  绿=一切正常；**黄**=连得上但模型名可疑（不拦你，只是提醒）；红=真不通
- 列表里向量/语音/图像模型（`embedding`、`whisper`、`bge`…）会被排到后面 ——
  但这是**按名字猜的，不保证准确**，拿不准就选一个试着测一次

> 下拉框**仍然可以直接打字**。有些网关不把全部模型列出来，
> 那种情况下手填是唯一出路，所以没把它堵死。

---

## 三、自检（不用你动手，30 秒）

```
双击 run-toolbox.cmd --selftest        ← 117 项，约 35 秒
双击 run-toolbox.cmd --selftest-ai     ← 118 项，多等约 40 秒（真实调用当前 AI 后端）
```

自检会自己往剪贴板放文本 / 图片 / 文件，验证能否正确抓回，**用完自动还原你原来的剪贴板内容**。
报告在 `%LOCALAPPDATA%\桌面工具箱\selftest\selftest-report.json`。

覆盖范围：剪贴板监听注册 / 三类内容抓取 / 消息循环存活 / 自写入标记 / 归档跳过 /
热键解析 / 中英双向判向 / 文本清洗 / 长文分块 / 悬浮窗扩展样式 /
裁剪像素精确 / 缩放像素精确 / 最近邻 / 缩略图 / 图片互转 / 不覆盖同名 / 转换矩阵路由 /
**日期筛选档位（跨月边界）** / **自启路径解析与比对** / **volatile 孤儿回收** /
**设置往返（空串热键 + AI 后端字段）** / **远端 API 端点拼接** /
**全屏 GDI 捕获 + 裁剪 + 保存 PNG** / **截图写入剪贴板（带自污染标记）**。

`--selftest --ai` 走的是**设置里当前选中的后端**，所以把后端切成远端再跑一次，
就等于验证了远端那条路径真的通。

---

## 四、能不能分享给别人？——依赖分层说明

> 你问的是：工具箱里有些工具依赖别的软件或本地模型，分享出去时对方是不是只能自己装一遍？

**不是。依赖是分层的，而且这一轮把最重的那一层拆掉了。**

### 4.1 哪些工具是"零依赖"的？

**快捷剪贴板、图片裁剪、格式转换（图片部分）、快捷截图** —— 这四个**完全不依赖任何外部东西**。

它们是纯 .NET 实现（剪贴板走 Win32、图片走 WPF 自带的编解码器、截图走 GDI 的 BitBlt），
`Toolbox.exe` 又是**自包含单文件**（运行时都打进去了）。
所以：**把这一个 exe 拷给对方，这四个工具立刻能用**，不需要装 .NET、不需要装任何东西。

### 4.2 哪些是"可选增强"？缺了会怎样？

| 功能 | 依赖 | 缺了之后 |
|---|---|---|
| 文档 / PDF 转换 | LibreOffice **或** Microsoft Office | 该格子不可用，**图片互转完全不受影响**；界面上会明确告诉你缺什么 |
| 快捷 AI | 一个 AI 后端（见下） | AI 面板打不开，其余四个工具不受影响 |

关键设计是：**依赖缺失时降级，而不是整个工具箱打不开**。
「格式转换」窗口里每一格的可选目标格式是**按当前机器实际能力算出来的** ——
没装 LibreOffice 就没有 `pdf` 这个目标，不会让你点了之后才报错。

### 4.3 AI 这一层，本轮已经改成两种后端可选

这正是你担心的那一点。以前 AI 硬绑本地 Ollama —— 对方要装 Ollama、再拉一个 4.7 GB 的模型，
对"分享一个工具箱"来说门槛太高。现在 **设置 → 快捷 AI → 后端** 有两个选项：

| 后端 | 对方需要做什么 | 数据流向 |
|---|---|---|
| **本地 Ollama**（默认） | 装 Ollama + 拉模型（约 4.7 GB） | **数据不出这台机器** |
| **远端 API**（OpenAI 兼容） | **什么都不用装**，填个地址 + Key | 待处理文本会**发到那台服务器** |

远端后端支持任何 **OpenAI 兼容**的 `/v1/chat/completions` 接口 —— 这几乎是现在的事实标准，
OpenAI / DeepSeek / 月之暗面 / 智谱 / 通义 / 自建网关（One-API、vLLM、LM Studio、
甚至 Ollama 自己）都提供。

几个刻意的设计：

- **地址填到哪都行**：填 `https://api.deepseek.com` 或 `https://api.deepseek.com/v1` 都能用，
  不带 `/v1` 会自动补 —— 不该让对方去记这种细节。
- **数据流向必须说清楚**：切到远端后，设置窗口的「关于隐私」和 AI 面板的状态行
  都会**明确写"待处理的文本会发送到这个远端服务"**。
  原文案写死的是"数据不出这台机器"，切到远端后那就是假话 —— 不能让人在不知情的情况下
  把一份内部文档传出去。
- **Key 明文存在本机 `settings.json`**，界面上也直说了，建议对方用自己的 Key。

**所以分享的方式是**：

1. 直接把 `Toolbox.exe` 给对方 —— 剪贴板 / 裁剪 / 图片转换立刻可用；
2. 对方想要 AI，就在设置里选「远端 API」，填自己的 Key（零安装）；
3. 对方想要文档转换，才需要装 LibreOffice 或 Office；
4. 对方在意隐私、想完全本地跑，才需要装 Ollama + 拉模型。

**每一步都是可选的，任何一步不做都不会让工具箱变成不能用的状态。**

---

## 四、文件在哪

| 用途 | 路径 |
|---|---|
| **启动工具箱**（双击桌面图标即可） | 桌面 `桌面工具箱` 快捷方式 |
| **程序本体（稳定安装位置）** | `%LOCALAPPDATA%\桌面工具箱\Toolbox.exe` |
| **功能总清单（逐项对照源码）** | `10-已实现功能总清单.md` |
| **安装 / 图标 / 自启修复说明** | `11-安装与自启修复说明.md` |
| **独立验收报告** | `09-独立验收报告.md` |
| 启动器（源码方式，开发者用） | `run-toolbox.cmd` |
| **交付的 exe（源码目录里这份）** | `dist\Toolbox.exe`（65.2 MB，单文件自包含，图标已内嵌） |
| **验收清单**（照着一项项点） | `05-验收清单.md` |
| **第三方质检报告的逐条整改回复** | `07-质检整改回复.md` |
| **加菜任务单（第 2 批需求：路径浏览按钮 + 快捷截图）** | `08-加菜任务单-第2批需求.md` |
| 决策与踩坑总账（**改动前必看**） | `DECISIONS.md` |
| 开发前的原始需求与设计 | `04-交接文档.md` |
| 第三方质检报告（外部产出） | `06-第三方质检报告.md` |
| P1 剪贴板设计稿 | `03-快捷剪贴板设计.md` |
| 源码 | `src\Toolbox\`（54 个 `.cs` + 8 个 `.xaml` + csproj/manifest/图标，约 10,500 行） |
| 数据目录（历史 / 图片 / 日志） | `%LOCALAPPDATA%\桌面工具箱` |

`Toolbox.exe` 还认几个启动参数，省得去托盘菜单里一层层点：

| 参数 | 作用 |
|---|---|
| `--settings` | 直接打开设置窗口 —— **验收热键和 AI 后端时最常用** |
| `--open <工具Id>` | 直接打开某个工具，Id 为 `clipboard` / `image` / `convert` / `ai` / `screenshot` |
| `--autostart status` | 查开机自启状态，**并打印注册表里实际那条命令 + 路径比对**（只读，无副作用） |
| `--autostart on` / `off` | 开 / 关开机自启（写、删 `HKCU\...\CurrentVersion\Run`） |
| `--selftest` | 跑自动化自检（117 项，约 35 秒） |
| `--selftest --ai` | 多跑一项真实模型调用（118 项），走当前选中的 AI 后端 |

例：`dist\Toolbox.exe --open clipboard` 直接弹出剪贴板历史，可以给它建桌面快捷方式。
`dist\Toolbox.exe --open screenshot` 直接拉起截图（等价于按热键 `Win+Alt+S`）。

`--autostart` 和 `--selftest` 是**纯命令行模式**：跑完就退，不会拉起托盘和悬浮窗。
写注册表这种改动系统状态的动作，用命令行做可脚本化、可回滚，每次都在日志里留痕。

> **`--autostart status` 值得单独说一句**：它会打印注册表里**实际写的那条命令**，
> 并和当前 exe 做路径比对。本轮就是靠它查出"自启指向的是 Debug 构建、不是交付的 exe"
> 这个隐藏了很久的问题 —— 在那之前，程序只会告诉你"已开启/未开启"，看不到它指向哪里。
> 详见 `07-质检整改回复.md` §七。

数据目录结构：

```
%LOCALAPPDATA%\桌面工具箱\
├─ clipboard\           index.jsonl（历史索引）+ blobs\（图片本体）+ thumbs\（缩略图）+ volatile\（超长正文）
├─ logs\                toolbox.log（运行日志，2 MB 轮转）
├─ selftest\            selftest-report.json + selftest.log
├─ startup-sentinel.log 启动哨兵：每次启动尝试都 append 一行（见下）
└─ settings.json        设置（热键、开机自启、AI 后端与地址等）
```

### 为什么要有 `startup-sentinel.log`

它是"开机自启到底有没有把进程拉起来"的**唯一可靠答案**。

主日志（`logs\toolbox.log`）要 `Log.Open` 之后才存在，而"启动失败"恰恰可能发生在它之前 ——
exe 根本没被拉起来、或者起来瞬间就崩了。那种情况下主日志一片空白，
看起来像"程序从没启动过"，排障时完全无从下手。

哨兵是**独立于主日志**的 append-only 文本，每次启动尝试都会留一行
（时间 / pid / 参数 / 工作目录），带 64 KB 上限、只保留最后 100 行。
开机后想确认自启有没有生效，看它就行：

```
2026-09-17 15:20:57.149	pid=20484	args=[--startup]	cwd=C:\...
```

`args=[--startup]` 就是"这次是开机自启拉起来的"。

---

## 五、三条必须知道的硬约束（不是偏好，是功能正确性的前提）

### 1. 悬浮窗绝不抢焦点

AI 工具靠 UI Automation 读「当前窗口」。**悬浮窗一旦抢了焦点，「读当前页面」就会读到悬浮窗自己。**

实现：`ShowActivated=false` **加上** 扩展样式 `WS_EX_NOACTIVATE`（0x08000000）。
只设前者不够——它只保证「显示时不激活」，**点击仍会激活**。
另加 `WS_EX_TOOLWINDOW`（0x00000080）让它不进 Alt+Tab。
日志里可见：`悬浮窗扩展样式已设置：NOACTIVATE + TOOLWINDOW（0x08080088）`。

### 2. 剪贴板是「通道」，历史是「档案」——不许为了读数据而写剪贴板

> 为了读数据而写剪贴板 = 往用户档案里塞垃圾，并把用户真正要用的内容挤下去。

- 读页面 → 走 UI Automation，**一个字都不写剪贴板**；
- 我们自己写的（比如「复制译文」）→ 打 `Origin=tool` 标记，**不进历史**；
- 明确否掉：模拟 `Ctrl+A` / `Ctrl+C` 自动抓取（同时犯三个错：污染档案、抢走用户正在复制的内容、抢焦点）。

### 3. 翻译方向必须写死在提示词里

中英双向、自动判方向（CJK 字符占比 > 30% 视为中文）。
提示词必须写明「翻译成**英文**」——含糊写「翻译成中文」会让 7B 模型自己猜方向，猜错时质量明显下降。

---

## 六、已知边界（不是 bug，是设计）

1. **重复复制不会新增条目**：按内容哈希去重，命中时把原条目顶到最前并更新时间戳。
2. **一次复制可能产生多条通知**：实测 OLE 设置剪贴板会触发 2-3 条 `WM_CLIPBOARDUPDATE`，日志里会看到多条「归档」行——**索引里只有一条**（实测 6 条记录 / 6 个唯一 hash）。
3. **超长文本只留摘要**：超过 10 KB 的正文外置到 `volatile\`，历史列表里显示「文章（N 字）」。
   正文文件在条目被删 / 被去重顶掉时会回收，载入时还会再扫一遍孤儿。
4. **密码管理器 / 部分远程桌面**：它们不往剪贴板放真实内容，抓到的会是空或占位。这是它们的设计，谁都抓不到。
5. **视频 / 音频转换不做**：本机没有 ffmpeg，用户已确认第一版不含。
   拖进来会被**点名拒绝**（不是静默跳过）：`拒绝 2 个视频/音频（a.mp4、b.mp3）—— 本版不做音视频转换`。
6. **PDF → 其他格式走 Office COM**，需要装 Office；LibreOffice 的 PDF 导入能力有限。
   COM 路径有 5 分钟超时；超时后只清理**本次新冒出来的** WINWORD 进程，不动用户自己开的 Word。
7. **AI 首字要等十几秒**（本机实测 17.9 秒）：7B 模型在核显上的正常水平。所以**必须流式**输出——不流式的话界面会像死了一样。
   （切到远端 API 后端后首字通常在 1 秒内，实测本机走远端路径首字 176 ms。）
8. **首次启动慢 1-2 秒**：单文件自包含 exe 要把运行时解压到临时目录，之后走缓存。
9. **自启模式会晚 8 秒初始化**：登录瞬间磁盘 / 注册表 / 剪贴板服务 / Shell 都在忙，
   这时候抢 `AddClipboardFormatListener` / `RegisterHotKey` 失败率明显高于平时。
   延迟后成功率高很多，代价只是托盘图标晚几秒出现。
10. **AI 面板的"自动读当前页面"有两条不做的情况**：当前是选中模式、或待处理文本里已有内容。
    前者会覆盖你真正想要的那段选区，后者会冲掉你自己改过的内容。
11. **「只取主内容区」是保守的**：它只切**首尾连续的短行块**（≤24 字且不以句末标点收尾），
    而且**如果切完只剩不到一半就放弃**（判定误判，原样保留）。
    正文里有大量短行（代码、清单、地址），误删正文比留一点噪声严重得多。

---

## 七、下一步

照 `05-验收清单.md` 一项项点。**重点看第 3 节那几项机器验不了的**：

1. **悬浮窗是否真的不抢焦点** —— 全项目功能正确性的前提；
2. **AI 读「当前页面」读得准不准** —— 本轮刚改了自动读取和降噪逻辑，**特别需要复验**；
3. **设置窗口里 AI 后端的切换与保存** —— 本轮新增的界面交互；
4. **开机自启的最终效果** —— 需要真的重启一次。

发现问题就告诉我具体是哪一项、什么现象。

---

*最后更新：独立验收复验并修掉 2 处缺陷（热键自动降级冒充用户意图 / 截图坐标单一比值在多屏混合 DPI 下算偏），
自检 30 项 → 32 项（`--ai` 33 项）全部通过、单文件 exe 重新发布（68,305,152 字节）那一轮。
完整验收过程见 `09-独立验收报告.md`。*
