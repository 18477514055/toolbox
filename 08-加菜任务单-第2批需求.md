# 桌面工具箱 · 加菜任务单（第 2 批需求）

> 交付方：接手平台（本单由原交接方撰写）
> 撰写时间：2026-09-17
> 前置：P1–P4 已交付并通过第三方质检（见 `06-第三方质检报告.md`）
>
> **本单只包含两个新需求**，都是用户明确提出的：
> **加菜 1：所有需要选存储位置的设置，加"拉起文件管理器"的按钮**
> **加菜 2：第 5 个功能「快捷截图」**

---

## 加菜 1：路径类设置加"浏览"按钮

### 1.1 用户的原始要求

> "所有需要选择文件存储地方的设置都加一个拉起文件管理器的按钮，方便我选择合适的储存地点，而不是手动输入地址。"

**要点**：**不想手输路径**。任何让用户填路径的地方，都要能点按钮去选。

### 1.2 先说清现状（我核过源码，避免你白干）

**目前真正的"路径输入框"只有一个**：

| 位置 | 控件 | 现状 |
|---|---|---|
| `Shell\SettingsWindow.xaml:157` | `SofficeBox`（LibreOffice 路径） | ⚠️ **纯手输文本框，没有浏览按钮** ← 主要整改点 |
| `Shell\SettingsWindow.xaml:165` | `DataPathText` | 只读显示，不是输入框 |
| `Tools\Convert\ConvertWindow.xaml.cs:268` | 输出目录 | ✅ 已有 `OpenFolderDialog`（可复用） |
| `Tools\ImageCrop\ImageCropWindow.xaml.cs:704` | 另存路径 | ✅ 已有 `SaveFileDialog` |

而且 `Settings.cs` 里目前**没有可配置的数据目录 / 截图保存目录**：

```
Shell\Settings.cs:85   public string? LibreOfficePath { get; set; }
Shell\Settings.cs:86   public string? LastConvertDir   { get; set; }
Shell\Settings.cs:91   public string? LastImageDir     { get; set; }
```

> ⚠️ **重要**：`Core\AppPaths.cs:15` 的 `Root` 是 `static { get; }`（**只读、无 setter**），
> 数据根目录写死在 `%LOCALAPPDATA%\桌面工具箱`。
> ⇒ "让用户选数据目录"**不是加个按钮的事，而是一项新设置 + 启动时重定向**（见 1.5 可选部分）。

### 1.3 要做的事

**① 做一个可复用的"路径框"控件（不要在每个窗口各写一遍）**

建议 `Shell\Controls\PathBox.xaml`（UserControl），同时支持两种模式：

| 模式 | 按钮行为 | 用于 |
|---|---|---|
| 目录模式 | `Microsoft.Win32.OpenFolderDialog` | 保存目录、输出目录 |
| 文件模式 | `Microsoft.Win32.OpenFileDialog`（可带过滤器，例如 `soffice.exe`） | LibreOffice 路径 |

控件内含：`TextBox`（**保留手输能力**，不要只允许选）+ **[浏览…]** + **[打开]**（用已有 `SettingsWindow.xaml.cs:375` 的 `OpenPath()` 直接复用，它已经会 `AppPaths.Ensure` 再 `explorer.exe`）。

**② 至少要覆盖这些位置**

| 位置 | 模式 | 说明 |
|---|---|---|
| 设置 → LibreOffice 路径（`:157`） | **文件**（过滤 `soffice.exe`） | 现在最难受的一个，必须改 |
| 设置 → **截图保存目录**（**新增**，见加菜 2） | 目录 | 新需求 |
| 设置 → 剪贴板图片保存目录（**新增，可选**） | 目录 | 若决定把 blob 外置给用户管理 |
| 转换窗口的输出目录（`ConvertWindow`） | 目录 | 已有对话框，统一成 PathBox 即可 |
| 裁剪窗口的另存目录 | 目录 | 同上 |

**③ 校验要跟上（光有按钮不够）**

- 目录：不存在 → 提示并**询问是否创建**（或静默创建，但要在界面上回显"已创建"）；
- 文件：不存在 / 不是文件 → 明确报错，并提示"点[浏览]重新选择"；
- **可写性检查**：选完立刻试写一个临时文件再删（否则用户选到只读盘，要到使用时才炸）；
- 路径回显时**不要截断**（长路径要能看全，或用省略号 + tooltip 给全文）。

**④ 记住"上次用的目录"**

`LastConvertDir` / `LastImageDir` 已有此模式，`OpenFolderDialog` 要传 `InitialDirectory`，否则每次都从"文档"开始，用户得重新点一路。

### 1.4 坑（已知的，别踩）

1. **中文 + 空格的路径**：传给外部进程（`soffice.exe`）时必须用 `ArgumentList`（`LibreOfficeConverter.cs:114-119` 已经这么做了，**保持**），不要手拼命令行。
2. **WPF 原生对话框**：`Microsoft.Win32.OpenFolderDialog` 在 **.NET 8+** 才有；本项目是 `net9.0-windows`（`Toolbox.csproj`），**可以用**，而且 `ConvertWindow.xaml.cs:125` 已经在用 → 全项目统一用它，**不要混入 WinForms 的 `FolderBrowserDialog`**（那个是老式树形 UI，风格也不一致）。
3. **`OpenFolderDialog` 的 owner 要传**（`dlg.ShowDialog(this)`），否则对话框可能被你的 Topmost 悬浮窗盖住。
4. **悬浮窗的 `WS_EX_NOACTIVATE` 不会影响对话框**——对话框是独立窗口，正常激活即可。

### 1.5 可选（建议单独立项，不要混进本单）

把**数据根目录**做成可配置（`AppPaths.Root` 加 setter，启动时读设置）。
**难点要先说清**：`Root` 是 `static { get; }`，且 `settings.json` **就存在 Root 下面** —— 改目录后设置文件本身也要搬家，还要处理"新旧目录同时存在"的迁移。属于结构性改动，**建议等用户明确要求再做**。

### 1.6 验收标准

1. 设置里点 LibreOffice 路径的 [浏览…]，能选出 `soffice.exe`，回显正确。
2. 选完立刻显示"✓ 可用"或"✗ 不可用 + 原因"。
3. 手输一个不存在的路径，保存时报错而不是静默存下。
4. 所有路径框都能点 [打开] 直接在资源管理器里定位（沿用已有 `OpenPath`）。
5. 对话框在**悬浮窗存在**的情况下也正常弹在前台。
6. 中文名目录（例如 `D:\我的 截图`）能正常选、能正常回显、能正常用于保存。

---

## 加菜 2：第 5 个功能 ·「快捷截图」

### 2.1 用户的原始要求（逐字）

> "第5个功能快捷截图，允许截图并且截出来的图放到设置内指定的地址。截图的大小和位置由鼠标拉取决定。呼出截图工具之后，鼠标右键取消。"

拆成硬需求：

| # | 要求 |
|---|---|
| 1 | 是工具箱的**第 5 个工具**（挂在悬浮窗 + 托盘 + 热键体系里） |
| 2 | 截出来的图**保存到设置里指定的目录** |
| 3 | **截图区域由鼠标拖拽决定**（不是固定全屏、不是固定比例） |
| 4 | **呼出后按鼠标右键 = 取消**（不保存、退出截图模式） |

### 2.2 建议的基线行为（照抄 Windows 自带截图的习惯，用户不用重新学）

| 操作 | 行为 |
|---|---|
| 拖拽 | 画出选区，实时显示尺寸（如 `820 × 460`） |
| 松开左键 | 立即截图并保存，退出截图模式 |
| **点一下右键** | **取消**，不保存、退出（**用户明确要求**） |
| `Esc` | 同样取消（顺手加上，成本为零） |
| 拖拽中 | 允许拖出**反方向**选区（从右下往左上），要正确归一化 |
| 极小选区（如 < 4×4 像素） | 视为误触，取消并提示，不要存一个 1×1 的垃圾文件 |
| 双击 / 单击空白 | 建议：取消（与 Windows 截图一致），不要做成"全屏截图"以免误存大文件 |

**保存规则**：

- 目录来自设置（见加菜 1）；**目录不存在时自动创建**，创建失败则明确报错并给出"去设置里改"的入口。
- 文件名建议 `截图_yyyyMMdd_HHmmss.png`（**用 PNG 无损**；若要 JPG，需在设置里给质量选项，别默认 JPG）。
- **同名不覆盖**：沿用已有 `FileNaming.UniquePath`（`Tools\Convert\FileNaming.cs:9-34`，自动加 ` (1)`）。
- 保存后给**明确反馈**：托盘气泡或屏幕角落的小提示，写明**保存到哪个路径** + 是否成功。

**⚠️ 一个必须提醒的产品决策**：截图**要不要同时写进剪贴板**？
用户的原始要求里**没提剪贴板**，而剪贴板有"不许污染"的硬约束（见 `04-交接文档.md` §五·工具4）。
⇒ **默认只存文件、不写剪贴板**。若以后要加，必须走 `ClipboardWriter`（带自污染标记）+ 在设置里给开关。

### 2.3 技术实现要点

| 要点 | 做法 |
|---|---|
| 全屏选区层 | **无边框全屏 WPF 窗口**，`WindowStyle=None`、`AllowsTransparency=True`、`Topmost=True`、`ShowInTaskbar=False` |
| 半透明遮罩 | 覆盖层本身半透明（如 `#33000000`），选区内**透亮**（用 `OpacityMask` 或四条 Border 拼出"挖洞"效果） |
| 多显示器 | 覆盖所有屏幕：用 `GetSystemMetrics(SM_XVIRTUALSCREEN/SM_YVIRTUALSCREEN/SM_CXVIRTUALSCREEN/SM_CYVIRTUALSCREEN)`，**注意副屏在主屏左边时 X 是负数** |
| 截取像素 | `System.Drawing.Graphics.CopyFromScreen(x, y, 0, 0, size)`（WinForms 已引用，`TrayIcon.cs` 就在用）；先 `Hide()` 覆盖层并 `Thread.Sleep(~120ms)` 等它真的消失，**否则会把自己的遮罩拍进去** |
| ⚠️ **DPI 必须按物理像素算** | 本项目 DPI 是 **PerMonitorV2**（`app.manifest`）。WPF 的坐标是 DIP，`CopyFromScreen` 要的是物理像素 → 必须用 `PresentationSource.FromVisual(...).CompositionTarget.TransformToDevice` 或 `GetDpiForWindow` 换算。**这是本项目最容易出错的一处**（同类的坑在裁剪工具里已经处理过，可以让它复用同一套换算） |
| 隐藏自己 | 呼出前先 `FloatingWindow.Suppress()`（已有 `IDisposable` 机制，`FloatingWindow.xaml.cs:334`），**否则悬浮窗会被拍进图里** |
| 焦点 | 选区窗口**需要**拿到焦点（要收键盘 `Esc`），所以它**不**用 `WS_EX_NOACTIVATE` —— 但因此它是"会抢焦点"的窗口，**必须**在"最近外部窗口"记录里被排除（`FocusTracker` 用的是 `WINEVENT_SKIPOWNPROCESS`，本进程窗口天然被排除 ✅ 无需额外处理） |
| 光标 | 选区窗口上 `Cursor = Cursors.Cross` |
| Esc/右键后 | 正常退出，不动剪贴板、不产生任何历史条目 |

### 2.4 热键（**有硬约束，见下**）

**实测**（我扫了三个族，工具箱运行时）：

| 组合族 | 空闲 | 已被占用 |
|---|---|---|
| `Win+Shift+<字母>` | B D G H I J K L N O Q U Y Z | **A C E F M P R S T V W X** |
| `Win+Alt+<字母>` | E H I J L N O P Q S U X Z | A B C D F G K M R T V W Y |
| `Ctrl+Alt+<字母>` | B D E G H I J K M N P Q S U V W Y | A C F L O R T X Z |

> 注：`Win+Shift+X` 现在是被占的，因为**工具箱自己正在运行并持有它**（实测 PID 31916）——不是冲突。

**建议默认键：`Win+Shift+Q`**（Q = 截图，语义好，本机空闲）。
**建议备选链**：`Win+Shift+Q` → `Win+Alt+Q`（本机**被占**，跳过）→ `Ctrl+Alt+Q`（本机空闲）。
⚠️ **`Win+Shift+<字母>` 族已被占掉 11 个，快用完了** —— 以后加工具要把备选链**扩到 `Win+Alt` / `Ctrl+Alt` 族**，不要只在这个族里找（`App.xaml.cs:379` 的 `HotKeyFallbacks` 建议按"跨族"来配）。

### 2.5 与现有架构的接法（照现有约定做，别另起一套）

1. 实现 `IToolboxTool`（`Shell\IToolboxTool.cs`）：`Id = "screenshot"`、`Name = "快捷截图"`、`Glyph`、`DefaultHotKey = "Win+Shift+Q"`、`OpensBigWindow = true`（这样悬浮窗会自动临时隐藏）。
2. 注册进 `ToolRegistry` / `App.xaml.cs` 的热键表与备选链。
3. 设置里加一项"截图保存目录"（用加菜 1 的 `PathBox`，目录模式）。
4. 走 `BusyState`（若截取耗时）与 `Log`（`Log.Line` 记录"截图已保存：<路径>"）。
5. **补自检用例**（见 2.6）。

### 2.6 自检用例（这个功能必须自检，因为人工验"截得对不对"太麻烦）

| 用例 | 断言 |
|---|---|
| 选区 → 像素映射 | 给定 DIP 矩形 `(100,100,300,200)`，在 **100% / 125% / 150%** 三种缩放下，换算出的物理像素必须与期望值**完全一致**（1px 不差） |
| 反方向拖拽 | 从 (400,400) 拖到 (100,100) 与反过来拖，产出的矩形完全相同 |
| 极小选区 | `3×3` 被判定为误触 → 返回取消、不写文件 |
| 保存与命名 | 写入指定目录、文件名形如 `截图_20260917_221530.png`、**同名时自动加 ` (1)`** |
| 目录不存在 | 自动创建成功；若创建失败返回明确错误 |
| 右键取消 | 调用取消路径后**不产生任何文件**、不写剪贴板 |

### 2.7 验收标准

1. 按热键 → 屏幕变暗、可拖框；**悬浮窗不在截图里、选区遮罩不在截图里**。
2. 拖框松手 → 文件出现在**设置里指定的目录**，且用看图软件打开尺寸与选区一致。
3. **点右键 → 立刻退出，不产生任何文件**（用户明确要求）。
4. `Esc` 同样取消。
5. 输出目录改成 `D:\我的 截图`（中文+空格）后仍能保存成功。
6. 缩放 125% / 150% 的显示器上，截出来的像素尺寸与选区一致（**不是"看起来差不多"**）。
7. 多显示器（含副屏在主屏左侧）能跨屏框选并正确保存。
8. 截图后：**剪贴板内容没有被改动**，剪贴板历史里**不新增条目**。

---

## 三、优先级建议

| 顺序 | 内容 | 理由 |
|---|---|---|
| 1 | 加菜 1 的 `PathBox` 控件 + LibreOffice 路径改造 | 小、独立、立刻改善体验；也是加菜 2 的前置（截图目录要用它） |
| 2 | 加菜 2 的「快捷截图」 | 用户明确要的第 5 个功能 |
| 3 | （可选）数据根目录可配置 | 结构性改动，等用户明确要求 |

> **建议**：加菜 1 只要先把 `PathBox` 做出来，加菜 2 的"截图保存目录"就是顺手的事——**两单有共用件，建议一起排期**。

---

*本单所有"现状/行号"均为 2026-09-17 对 `src\Toolbox\` 实测所得。动手前请重新核一遍行号，代码可能已变动。*
