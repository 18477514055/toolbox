# 桌面工具箱 · 已实现功能总清单

> **共 12 个工具**（前 5 个 — 剪贴板/裁剪/转换/AI/截图 — 见 §二~§六；
> 后 5 个 — OCR/重命名/哈希/二维码/置顶 — 见 §六·补）。
> 每个后 5 个工具的目录下都有自己的 `README.md`（依赖 / 用法 / 已知限制 / 测试脚本）。

> 本清单**逐项对照源码核实**（不是照抄文档），每条都注明实现位置。
> 核实时间：2026-09-18 晚
> 对应版本：`dist\Toolbox.exe`（68,367,429 字节，已嵌入图标）

---

## 〇、总览：一个外壳 + 五个工具 + 一套设置

```
                    ┌─────────────────────────┐
                    │   托盘图标（常驻）        │
                    │   + 竖排小悬浮窗（不抢焦点）│
                    └───────────┬─────────────┘
                                │  热键 / 点击 / 托盘菜单
   ┌────┬────┬────┬────┬────┬────┬────┬────┼────┬────┬────┬────┐
   ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼    ▼
  ①   ②   ③   ④   ⑤   ⑥   ⑦   ⑧   ⑨   ⑩   ⑪   ⑫
 剪贴板 裁剪 转换 AI 截图 OCR 改名 哈希 二维码 置顶 运行命令 打包
```

| # | 工具 | 默认热键 | 本机实际生效 | 大窗口 |
|---|---|---|---|---|
| ① | 快捷剪贴板 | `Win+Shift+V` | **`Win+Alt+V`** | 否（面板） |
| ② | 图片裁剪 | `Win+Shift+X` | `Win+Shift+X` | 是 |
| ③ | 格式转换 | `Win+Shift+C` | **`Win+Alt+C`** | 是 |
| ④ | 快捷 AI | `Win+Shift+A` | **`Win+Alt+A`** | 是 |
| ④b | └ 翻译选中 | `Win+Shift+T` | **`Win+Shift+E`** | 是 |
| ⑤ | 快捷截图 | `Win+Alt+S` | `Win+Alt+S` | 是 |
| ⑥ | OCR 截图识字 🆕 | `Win+Alt+T` | **`Win+Alt+J`** | 是 |
| ⑦ | 批量重命名 🆕 | `Win+Alt+R` | **`Win+Alt+I`** | 是 |
| ⑧ | 哈希校验 🆕 | `Win+Alt+H` | `Win+Alt+H` | 是 |
| ⑨ | 二维码工具 🆕 | `Win+Alt+Q` | `Win+Alt+Q` | 是 |
| ⑩ | 窗口置顶 🆕 | `Win+Alt+P` | `Win+Alt+P` | 否（纯动作） |
| ⑪ | 运行命令 🆕 | `Win+Alt+X` | `Win+Alt+X` | 是 |
| ⑫ | 批量打包 🆕 | `Win+Alt+G` | **`Win+Shift+I`** | 是 |

> **为什么划掉的是默认键**：本机 `Win+Shift+<字母>` 已被占用 10 个
> （`A C F M P R S T V W`，其中 `S` 是 Windows 自带截图），
> `Win+Alt+<字母>` 也有 10 个被占（`B D F G K M R T W Y`）。
> 工具箱**默认键被占用时自动降级**到备选键，并在托盘气泡里明确告诉你换成了什么。
>
> **热键可在「设置」里改**；留空 = 该工具不占热键（从悬浮窗 / 托盘打开）。
>
> ⚠️ **占用表是"当时快照"**：加新工具前要用 `artifacts\probe-hotkey\` 重测一次，
> 而且**必须在工具箱自己没运行时测**（它注册的键也会显示为"被占用"）。
> 本轮就因此发现 `Win+Alt+T/R` 其实已被占，从而调整了 OCR 与重命名的默认键。

---

## 一、通用外壳能力

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 常驻托盘（右键菜单：各工具 / 暂停监听 / 显示悬浮窗 / 设置 / 退出） | ✅ | `Shell\TrayIcon.cs` |
| **竖排半透明悬浮窗**，悬停变实、可拖动、**可收起** | ✅ | `Shell\FloatingWindow.xaml(.cs)` |
| ⭐ **悬浮窗绝不抢焦点**（`ShowActivated=false` + `WS_EX_NOACTIVATE` + `WS_EX_TOOLWINDOW`） | ✅ | 同上，日志可见 `0x08080088` |
| 悬浮窗不进 Alt+Tab | ✅ | `WS_EX_TOOLWINDOW` |
| 悬浮窗位置按**工作区百分比**记忆（多屏/拔屏不会跑出屏外） | ✅ | `Settings.FloatingX/Y` |
| 打开大窗口工具时悬浮窗**临时躲开**（避免挡操作） | ✅ | `IDisposable Suppress()` |
| **全局热键**，含被占用检测 + 自动降级链 + 明确提示 | ✅ | `Shell\HotKeyManager.cs`、`App.xaml.cs` |
| **无热键模式**（留空 = 不占热键，与"没配过"分开存） | ✅ | `App.xaml.cs` `RegisterOne` |
| **热键来源可区分**（用户填的 vs 自动降级的） | ✅ | `Settings.HotKeyOrigins`（本轮新增） |
| 单实例（重复启动只唤起已有实例） | ✅ | `App.xaml.cs` Mutex |
| 开机自启（注册表 Run 键）+ **每次启动路径自愈** | ✅ | `Shell\AutoStart.cs` |
| **启动哨兵** `startup-sentinel.log`（独立于主日志，排障用） | ✅ | `AppPaths.StartupSentinelFile` |
| **设置窗口**：热键 / 悬浮窗 / 容量 / AI 后端 / 各路径 | ✅ | `Shell\SettingsWindow.xaml(.cs)` |
| 设置里路径用 **「浏览…」按钮**选（不用手敲） | ✅ | 同上 `BrowseFile` / `BrowseFolder` |
| 运行日志（2 MB 轮转）+ 未处理异常兜底（不崩） | ✅ | `Core\Log.cs`、`App.OnUnhandledException` |
| 命令行参数：`--open <id>` / `--settings` / `--autostart` / `--selftest` / `--startup` | ✅ | `App.xaml.cs` |
| **自动化自检 32 项**（`--ai` 33 项） | ✅ | `SelfTest.cs` |
| 程序图标（多分辨率 16~256，已嵌入 exe） | ✅ | `Assets\toolbox.ico`（本轮新增） |

---

## 二、① 快捷剪贴板

**核心**：监听剪贴板 → 归档 → 随时翻出来。

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 监听 `WM_CLIPBOARDUPDATE`（系统级通知，非钩子非注入） | ✅ | `Core\ClipboardListener.cs` |
| 抓取**文本 / 图片 / 文件**三类 | ✅ | `Core\ClipboardReader.cs` |
| 读取顺序固定 **文件 → 图片 → 文本**（避免逼 shell 现场生成缩略图） | ✅ | 同上 |
| 读取带**指数退避重试**（应对延迟渲染） | ✅ | 同上 |
| **不枚举全部格式**（避免触发全部延迟渲染卡死） | ✅ | 用 `GetFormats(false)` |
| 内容级 **SHA-256 去重**（重复复制顶到最前，不新增） | ✅ | `HistoryStore.Add` |
| **搜索框实时过滤**（命中 X / Y 条） | ✅ | `ClipboardPanel` |
| **日期筛选**：全部 / 今天 / 昨天 / 最近7天 / 最近30天 | ✅ | `DateRangeFilter.cs` |
| 缩略图**内存缓存**（敲字不再全表重载） | ✅ | `_thumbCache` |
| 缩略图长边 320，图片有缩略图、文件显示文件名 | ✅ | `HistoryStore.MakeThumbnail` |
| 回车 = 复制回剪贴板（**不自动粘贴**，你自己 Ctrl+V） | ✅ | `ClipboardPanel` |
| `Ctrl+P` = 置顶（**置顶条目永不淘汰**） | ✅ | `HistoryStore` |
| `Ctrl+D` **和** `Ctrl+Delete` = 删除（两个都收） | ✅ | `ClipboardPanel.xaml.cs` |
| 面板失焦**自动隐藏** | ✅ | 同上 |
| 时间列：今天显示 `HH:mm:ss`，更早显示 `MM-dd HH:mm` | ✅ | `ClipboardPanel` |
| **超长文本只留摘要**（>10 KB 外置到 `volatile\`，列表显示「文章（N 字）」） | ✅ | `HistoryStore.SavePayload` |
| 配额：默认 2000 条 / 500 MB，**LRU 淘汰** | ✅ | `EnforceQuota` |
| **删除时按 Hash 回收正文文件**（修掉过磁盘泄漏） | ✅ | `DeletePayload` |
| **载入时清扫孤儿正文文件**（兜底，防无限增长） | ✅ | `SweepOrphanPayloads` |
| 托盘「暂停监听」/「恢复」（暂停期间不进历史） | ✅ | `App.xaml.cs` |
| ⭐ **我们自己写的不进档案**（token + 内容哈希 + 10 秒 TTL 三重判定） | ✅ | `Core\SelfWriteGuard.cs` |

---

## 三、② 图片裁剪

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **框选裁剪**（鼠标拖框 + 四个角手柄，像素精确到 1px） | ✅ | `ImageCropWindow.xaml.cs` |
| **改像素尺寸**，支持锁定宽高比 | ✅ | 同上 |
| 插值质量可选（含**最近邻**） | ✅ | `ImageOps.cs` |
| 保存格式 PNG / JPG / BMP / TIFF / GIF | ✅ | WPF 内置编码器 |
| **永远另存，绝不覆盖原文件**（同名自动加 `(1)`） | ✅ | `FileNaming.UniquePath` |
| 图片来源：**剪贴板图片**（截图后直接裁）/ 拖拽 / 打开文件 | ✅ | 同上 |
| 缩放 125% / 150% 显示器下选区不偏 | ✅ | 按像素算，不按显示尺寸 |

---

## 四、③ 格式转换

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **图片互转** PNG / JPG / BMP / TIFF / GIF（**零依赖**，WPF 内置） | ✅ | `ImageConverter.cs` |
| **文档/PDF 转换**（docx / xlsx / pptx / odt / html ↔ pdf / txt / csv） | ✅ | `LibreOfficeConverter.cs` |
| 备选后端 **Office COM**（保真度更高，尤其 PDF→docx） | ✅ | `OfficeComConverter.cs` |
| **可插拔 `IConverter` 策略**，按文件类型自动挑后端 | ✅ | `IConverter.cs` |
| **批量队列**（多文件排队，逐条状态，成功/失败都说清） | ✅ | `ConvertWindow.xaml.cs` |
| 拖拽即转 | ✅ | 同上 |
| **按当前机器实际能力算可选目标格式**（没装 LibreOffice 就不列 pdf） | ✅ | `IConverter` 能力探测 |
| 依赖缺失给**明确中文提示**（不是异常堆栈） | ✅ | 同上 |
| **视频/音频被点名拒绝**（不是静默跳过） | ✅ | `拒绝 N 个视频/音频（a.mp4）—— 本版不做` |
| 中文路径 / 带空格路径正常 | ✅ | 用 `ArgumentList`，不手拼命令行 |
| 可取消，不卡界面 | ✅ | 后台线程 + `CancellationToken` |
| Office COM 超时**只杀本次新冒出来的 WINWORD**（不动你开着的 Word） | ✅ | `OfficeComConverter.cs` |
| 记住上次输出目录 / 格式 | ✅ | `Settings.LastConvertDir` |
| 转换临时目录用纯 ASCII（避免中文跨进程问题） | ✅ | `FileNaming.CreateTempDir` |

---

## 五、④ 快捷 AI

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **读取方式一：整窗口**（按热键**自动读**当前页面） | ✅ | `AiTool.Invoke` → `AutoGrabWindowAsync` |
| **读取方式二：选中文本**（`TextPattern.GetSelection()` 直读，**不碰剪贴板**） | ✅ | `TextGrabber.GrabSelectionOrNull` |
| ⭐ **全程零剪贴板污染**（读页面走 UI Automation，一个字都不写剪贴板） | ✅ | `TextGrabber.cs` 全文无 Clipboard 引用 |
| 「将要读取：<窗口标题>」**在点读取之前**就显示 | ✅ | `panel.RefreshTargetInfo()` |
| **「只取主内容区」真的生效**（切掉首尾导航/页脚短行） | ✅ | `TextGrabber.TrimNavAndFooter` |
| └ 带**「切太多就放弃」保底**（切完不足一半 → 原样返回） | ✅ | 同上 |
| 面板里**能看到并编辑**将要送出的文本（最可靠兜底） | ✅ | `AiPanel` |
| 文本轻量清洗（压空行 + 去连续重复行） | ✅ | `TextCleaner.cs` |
| **翻译**（中英双向，**自动判方向**，提示词写死目标语言） | ✅ | `LanguageDetector` / `Prompts` |
| **总结**（长文分块 → 逐块摘要 → 二次汇总，带进度） | ✅ | `AiPanel.xaml.cs` |
| **提问**（自由问答） | ✅ | 同上 |
| **流式输出**（逐字冒出，不等全部生成完） | ✅ | `/api/generate` `stream:true` |
| **可中断**（生成中点取消立刻停） | ✅ | `CancellationTokenSource` |
| **连点两次被拦**（不会两路输出交错） | ✅ | 置忙在第一个 `await` 之前 |
| 三个翻译入口：当前页面 / 选中文本 / 剪贴板内容 | ✅ | —— |
| └ **翻译选中一键直达**（独立热键 `Win+Shift+E`） | ✅ | `AiTool.InvokeExtra` |
| **两种后端可选**（分享给别人的关键）： | ✅ | `AiClientFactory` |
| └ 本地 **Ollama**（数据不出本机，默认） | ✅ | `OllamaClient.cs` |
| └ **远端 OpenAI 兼容 API**（零安装，填地址+Key 即可） | ✅ | `RemoteAiClient.cs` |
| └ 地址不带 `/v1` **自动补全** | ✅ | 同上 `Endpoint()` |
| └ **隐私文案跟着后端变**（切远端必须明说数据会外发） | ✅ | `UpdatePrivacyText()` |
| 探活有**界超时**（不会永久卡在"正在检查…"） | ✅ | 本地 12s / 远端 20s / 设置 15s |
| `HttpClient` 静态共享（不会耗尽 socket） | ✅ | 同上 |
| 读不到的程序给**明确降级提示**（教你自己 Ctrl+C 再翻译剪贴板） | ✅ | `TextGrabber` 失败分支 |
| 焦点在我们自己窗口时**明确提示**（不给假译文） | ✅ | `IsOwnProcess` |

---

## 六、⑤ 快捷截图

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **整屏冻结**（先拍静态图再框选，不闪、不漏帧） | ✅ | `ScreenCapture.CaptureFullScreen`（GDI `BitBlt`） |
| **鼠标拖拽框选**，反方向拖也对（坐标归一化） | ✅ | `ScreenshotWindow` |
| 选区**外变暗、内透亮**（`CombinedGeometry` 挖洞） | ✅ | 同上 |
| 松手即**存 PNG** 并**同时复制到剪贴板** | ✅ | `Finish()` |
| **右键 / Esc 取消**（用户明确要求） | ✅ | 同上 |
| **极小选区视为误触**（< 3×3 不存垃圾文件） | ✅ | `MinSize` |
| 保存目录**可配置**，留空落到数据目录 `screenshots\` | ✅ | `Settings.ScreenshotOutputDir` |
| 目录不存在**自动创建**，失败退回默认目录 | ✅ | `ResolveOutputDir` |
| 同名**不覆盖**（自动加 `(1)`） | ✅ | `EnsureUnique` |
| 保存后**明确反馈**（通知里写清文件名） | ✅ | `_ctx.Notify` |
| 截图**不进剪贴板历史**（带自污染标记，但能直接 Ctrl+V） | ✅ | `ClipboardWriter.SetImage` |
| 已有遮罩时再按热键**不叠加**第二个 | ✅ | `ScreenshotTool.Invoke` |
| 悬浮窗**不会截进图里**（`OpensBigWindow` 自动躲开） | ✅ | 同上 |
| ⭐ **坐标按显示器真实 DPI 换算**（多屏混合 DPI 也不偏） | ✅ | `Core\ScreenCoordinateMapper.cs`（本轮修复） |
| 越界选区**自动夹取**（不会让 `CroppedBitmap` 抛异常） | ✅ | 同上 |

---

## 七、设置项一览

| 分类 | 可配置项 |
|---|---|
| **热键** | 5 个工具 + 「翻译选中」，全部可改；留空=禁用；填「默认」= 恢复自带键 |
| **通用** | 开机自启、暂停监听 |
| **悬浮窗** | 显示/隐藏、打开工具时是否躲开、透明度、记忆位置、可收起 |
| **剪贴板** | 最大条数、图片总量上限、是否显示自己写入的条目 |
| **AI** | 后端（本地/远端）、Ollama 地址与模型、远端地址/Key/模型、单次分块字数、是否只取主内容区 |
| **格式转换** | LibreOffice 路径（**浏览…**按钮）、上次输出目录/格式 |
| **图片** | 上次另存目录 |
| **快捷截图** | 截图保存目录（**浏览…**按钮，可留空） |

---

## 八、已知边界（是设计，不是 bug）

1. **重复复制不新增条目** —— 按内容哈希去重并顶到最前。
2. **一次复制可能产生多条通知** —— 实测 OLE 写剪贴板会触发 2-3 条 `WM_CLIPBOARDUPDATE`；索引里只有一条。
3. **超长文本只留摘要** —— >10 KB 正文外置，列表显示「文章（N 字）」。
4. **密码管理器 / 部分远程桌面抓不到** —— 它们不往剪贴板放真实内容，谁都抓不到。
5. **不做视频/音频转换** —— 本机无 ffmpeg；拖进来会被**点名拒绝**。
6. **PDF → 其他走 Office COM**，需要装 Office；有 5 分钟超时。
7. **AI 首字要等十几秒**（本地 7B 在核显上，本机实测 17.9~23.3 秒）→ 所以必须流式。切远端后首字约 176 ms。
8. **首次启动慢 1-2 秒**（单文件 exe 要解压运行时到临时目录，之后走缓存）。
9. **自启模式晚 8 秒初始化**（避开登录瞬间的资源争抢）。
10. **AI「自动读当前页面」有两条不做**：当前是选中模式、或文本框里已有内容（避免覆盖你的东西）。
11. **「只取主内容区」是保守的** —— 只切首尾连续短行块，且切完不足一半就放弃。
12. **中英判向阈值 30%** —— 中英混排且带大量代码的技术文档**可能被判成英文为主**（已知假阳性，建议真机试一篇技术文章）。

---

## 九、验证状态

| 项 | 结果 |
|---|---|
| 自动化自检 | **32/32** 通过（`--ai` **33/33**，含真实模型调用） |
| 编译 | 0 错误 0 警告 |
| 独立验收夹具（验收方自写，不在产品内） | 28/28 通过 |
| 三种启动方式实测 | 桌面快捷方式 / 直达入口 / `--startup` 自启 **全部通过，零错误** |

---

---

## 六·补、本轮新增的 5 个工具

### ⑥ OCR 截图识字（`ocr`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 热键触发 → 整屏冻结 → 拖拽框选 → 识别文字 | ✅ | `Tools\Ocr\OcrTool.cs` / `OcrRegionWindow` |
| **自动复制到剪贴板** + 弹出结果窗口 | ✅ | `OcrResultWindow` |
| 结果**可编辑**（OCR 有错字时直接改） | ✅ | 同上 |
| **中文识别**（本机 zh-Hans-CN 实测可用） | ✅ | `OcrEngineService` |
| **零依赖、离线**（用 Windows 自带 OCR） | ✅ | 同上 |
| 引擎不可用时**在框选之前**就明确提示 | ✅ | `IsAvailable(out reason)` |
| 复用截屏与 DPI 换算 | ✅ | `ScreenCapture` / `ScreenCoordinateMapper` |
| 空白图**明确失败**（不静默返回空） | ✅ | `OcrResult.Fail` |

### ⑦ 批量文件重命名（`rename`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **前缀 / 后缀 / 替换 / 序号 / 时间戳 / 扩展名小写** | ✅ | `RenamePlanner`（纯函数） |
| **先预览再执行**（预览与执行**共用同一份计算**） | ✅ | 同上 |
| **一键撤销**（账本落盘，重启后仍可撤） | ✅ | `RenameJournal` |
| **重名自动加序号**，绝不覆盖已有文件 | ✅ | `RenamePlanner` 冲突处理 |
| **两阶段改名**（先改临时名 → 再改目标名，避免交换名字时撞车） | ✅ | `BatchRenameWindow.Apply` |
| 非法字符被拦下并说明原因 | ✅ | `RenamePlanner` |
| **不递归子目录**（避免误伤） | ✅ | 有意为之 |
| 执行前二次确认 | ✅ | `BatchRenameWindow` |

### ⑧ 哈希校验（`hash`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| MD5 / SHA1 / SHA256 / SHA512 | ✅ | `HashService` |
| **两个文件对比是否一致** | ✅ | `CompareAsync` |
| 大小不同**直接判不同**（省一次完整计算） | ✅ | 同上 |
| 与**期望值比对**（粘贴下载站给的校验值） | ✅ | `Verify` + 按长度识别算法 |
| **大文件流式分块**（内存占用恒定，带进度、可取消） | ✅ | `ComputeAsync` |
| 文件不存在 / 空文件 / 边界都正确 | ✅ | 同上 |
| 界面明确标注 **MD5/SHA1 不能用于防篡改** | ✅ | `HashCheckWindow.xaml` |

### ⑨ 二维码工具（`qrcode`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **生成**二维码（文字 / 链接 → 图片） | ✅ | `QrEncoder`（自实现） |
| 四个纠错等级 L/M/Q/H | ✅ | 同上 |
| 保存 PNG / 复制到剪贴板 | ✅ | `QrWindow` |
| **扫描**：框选屏幕 或 选图片 → 读出内容 | ✅ | `QrScanner`（自实现） |
| 扫出后**自动复制** | ✅ | `QrWindow.DoScan` |
| 内容超长**明确报错并给建议**（不产出坏码） | ✅ | `QrEncoder.Encode` 返回 null |
| **零第三方依赖**（编码器 + 解码器都是自实现） | ✅ | 同上 |
| 渲染留静区、模块严格等宽 | ✅ | `QrRenderer` |
| **外部独立验证**（ZXing 当裁判，24/24） | ✅ | `artifacts\probe-qr\` |

### ⑩ 窗口置顶（`topmost`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 热键把**当前活动窗口**置顶 / 取消（toggle） | ✅ | `TopmostService` / `WindowTopmostTool` |
| 托盘气泡告知**操作了哪个窗口**、当前状态 | ✅ | `WindowTopmostTool.Invoke` |
| 状态**真的去查系统**（不自己记 bool，能跟随别的工具改动） | ✅ | `IsTopmost` 读 `WS_EX_TOPMOST` |
| **不抢焦点**（`SWP_NOACTIVATE`） | ✅ | `SetTopmost` |
| 最小化的窗口给明确提示 | ✅ | 同上 |
| 排除工具箱自己的窗口与 shell 窗口 | ✅ | `WindowTopmostTool` |
| 无效句柄返回失败而不抛异常 | ✅ | 同上 |

---

---

## 七·补、本批再新增的 3 个功能

### ⑪ 运行命令（`run`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| 输入命令 → 交给 **cmd** 或 **PowerShell** 执行 | ✅ | `Tools\CommandRunner\CommandRunner.cs` |
| **输出完整回显**（stdout + stderr + 退出码） | ✅ | 同上 |
| **不自动执行**（回车 = 换行，Ctrl+Enter 才执行） | ✅ | `CommandWindow.xaml.cs` |
| **执行前原样显示最终命令行**供核对 | ✅ | `DescribeCommandLine` |
| 超时后**真的杀掉进程树** | ✅ | `RunAsync` |
| **非 0 退出码如实回报**（不误判成工具故障） | ✅ | 状态行区分"命令自身失败"与"工具出错" |
| **中文输出不乱码**（按实际代码页解码） | ✅ | `ResolveConsoleEncoding` |
| 可取消 / 可复制输出 | ✅ | `CommandWindow` |
| **不做危险命令拦截**（有意为之，如实告知） | ✅ | README 里有专门一节 |

### ⑫ 批量打包（`archive`）

| 功能 | 状态 | 实现位置 |
|---|---|---|
| **ZIP**（零依赖，用 .NET 自带能力） | ✅ | `Tools\Archiver\ArchiveService.cs` |
| **RAR**（调用用户装的 WinRAR） | ✅ | 同上；本机实测产物文件头为 `Rar!` |
| 找不到 WinRAR 时**明确说明原因 + 建议用 ZIP** | ✅ | `IsAvailable(out reason)` |
| **输出位置两个选项**（同目录 / 设置里指定的目录） | ✅ | `ArchiveDestination` |
| 可「设为默认」把目录存进设置 | ✅ | `Settings.ArchiveOutputDir` |
| 拖拽 / 多选 / **从剪贴板载入**（配合"框选后 Ctrl+C"） | ✅ | `ArchiveWindow` |
| **绝不覆盖同名压缩包**（自动换名） | ✅ | 复用 `Convert.FileNaming.UniquePath` |
| ZIP 条目名用**相对路径**（解压不带一长串目录） | ✅ | `AddDirectoryToZipAsync` |
| 中文文件名正常 | ✅ | 端到端用例覆盖 |

### 剪贴板体验优化（用户要求的改动）

| 改动 | 说明 | 实现位置 |
|---|---|---|
| **右键 = 直接复制** | 点哪一行就复制哪一行（右键会先同步选中项）；点空白处不动作 | `ClipboardPanel.OnListRightClick` |
| **文件显示原位置** | 单文件→所在目录；单文件夹→它自己的完整路径；多文件→同一目录或"等 N 个位置" | `DescribeLocation` |
| **图片显示原位置** | 显示工具箱保存的那份原图路径（`blobs\<hash>.png`），点开即可看 | `GetAbsoluteBlobPath` |
| 右键菜单补充 | 置顶 / **打开所在位置**（资源管理器里定位）/ 删除 | `BuildContextMenu` |
| 底部提示同步更新 | 写明「右键 = 直接复制」 | `ClipboardPanel.xaml` |

> ⚠️ **关于"图片原位置"要如实说明**：剪贴板里的图片**没有"来源文件路径"**这个信息 ——
> 剪贴板传的是像素数据（CF_BITMAP / CF_DIB / PNG），不像文件（CF_HDROP）那样天然带路径，
> 系统 API 根本不提供"这张图从哪个文件复制来的"。
> 所以这里显示的是**工具箱把它存到了哪里** —— 那是它在磁盘上真实、可点开的位置，
> 也正是用户真正需要的："我复制过的那张图，现在在哪儿能找到"。

---

## 九、验证状态（更新）

| 项 | 结果 |
|---|---|
| 自动化自检（源码目录） | **117/117** 通过 |
| 自动化自检（安装版） | **111/111**（8 项需源码树的降级为信息项，不计入通过率） |
| 编译 | **0 错误 0 警告** |
| 12 个工具逐个冒烟 | ✅ 全部启动正常 |
| 独立验收夹具（验收方自写） | 28/28 通过 |
| 二维码外部独立验证（ZXing） | 24/24 通过 |

---

*本清单由验收方逐项对照源码核实。如与 `00-先看我.md` 表述冲突，以源码为准并请指出。*
