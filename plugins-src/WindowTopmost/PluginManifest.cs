// ═══════════════════════════════════════════════════════════════════════
// 插件清单 —— 主程序**读这个特性**来了解插件，然后才决定要不要实例化它。
//
// 为什么不靠"扫 IToolboxTool 实现"就完事：
//   主程序需要在**实例化之前**知道 Id / 版本 / 契约版本，才能：
//     · 和 tools.json 对账（这个插件是不是官方发布的、版本对不对）；
//     · 判断兼容性并**明确拒绝**（而不是实例化到一半崩掉）；
//     · 在没有 UI 的场景下列出插件。
//   实例化是有代价的（要 STA 线程、要依赖就绪），所以"先读清单"是必要的。
//
// ⚠️ 这个特性的参数必须与 tools.json 里的 id / version 一致，
//    否则主程序会在对账时判定为"清单与实现不符"而拒绝加载。
// ═══════════════════════════════════════════════════════════════════════

using Toolbox.Shell;

[assembly: ToolboxPlugin(
    "topmost",
    "窗口置顶",
    Version = "1.0.0",
    ContractsVersion = "1.0")]
