# 桌面工具箱 · 验收清单

> 用法：从第 1 节开始往下点。第 1、2 节是机器已经验过的（你只需确认结果）；
> **第 3 节必须你亲手点**——那几项机器验不了，也正是最可能出问题的地方。
>
> 发现不对就记下「哪一项 + 什么现象」，告诉我即可。

---

## 0. 准备

| 步骤 | 操作 |
|---|---|
| 0.1 | 确认 `dist\Toolbox.exe` 存在（约 65 MB）。**不需要装任何东西**——.NET 运行时已经打进 exe 里了 |
| 0.2 | 如果之前跑过测试，先确认没有残留进程：任务管理器里搜 `Toolbox`，有就结束掉 |
| 0.3 | 记一下**当前剪贴板**里有什么（自检会临时借用并还原，但先记着更保险） |

---

## 1. 机器自检（自动，30 秒）

**操作**：双击 `run-toolbox.cmd`，或者在命令行里执行 `run-toolbox.cmd --selftest`

| # | 检查项 | 通过标准 | 结果 |
|---|---|---|---|
| 1.1 | 自检结论 | 打印 `自检结果：117/117 通过 —— 全部通过`（另有若干项环境信息，不计入通过率） | ☐ |
| 1.2 | 剪贴板三项 | `纯文本复制` / `纯图片复制` / `多文件复制` 全部 `[通过]` | ☐ |
| 1.3 | 消息循环存活 | `消息循环线程存活（坑 2 的防回归断言）` = `是（正常）` | ☐ |
| 1.4 | 自污染标记 | `工具箱自己写入的内容被识别为 Origin=tool` + `Origin=tool 的条目确实不进档案` 都通过 | ☐ |
| 1.5 | 图片像素精确 | `裁剪输出像素精确` = `裁 120x90 → 实际 120x90`；`改尺寸` = `777x333 → 777x333` | ☐ |
| 1.6 | **日期筛选档位** | `剪贴板日期筛选档位（含跨月 / 时间戳损坏）` = `10 条边界用例全部正确` | ☐ |
| 1.7 | **自启路径解析与比对** | `自启命令的路径解析` 4 种写法正确；`自启路径比对` 认得出"不是当前 exe" | ☐ |
| 1.8 | **volatile 孤儿回收** | `volatile 孤儿正文文件在载入时被回收（B-1 防回归）` = `造了一个孤儿文件，载入后被清掉` | ☐ |
| 1.9 | **设置往返** | `空串热键存活…；AI 后端 4 个新字段完整往返` | ☐ |
| 1.10 | **远端 API 端点拼接** | `两种情况都拼成 .../v1/models`；未填地址时给出明确中文提示 | ☐ |
| 1.11 | **截图捕获+保存** | `快捷截图：捕获+裁剪+保存 PNG` = `N 字节；源图 W×H`；`快捷截图：写入剪贴板（标记自污染）` = `[通过]` | ☐ |
| 1.12 | 环境信息两项 | `LibreOffice` 找到 soffice.exe；`Ollama` 报「连接正常，模型 qwen2.5:7b 已就位」 | ☐ |
| 1.13 | 剪贴板已还原 | 打印 `[自检] 已恢复原剪贴板：…`，且你的剪贴板内容没丢 | ☐ |
| 1.14 | **热键来源分离**（新增） | `热键「自动降级」与「用户指定」被分开记录` = `auto/user 两种来源可区分且能往返；老设置文件缺该字段时为空格而非 null` | ☐ |
| 1.15 | **截图坐标 DPI 换算**（新增） | `截图坐标换算按显示器真实 DPI` = `100%:✓；125%:✓；150%:✓；200%:✓；越界夹取✓；非法系数兜底✓` | ☐ |
| 1.16 | **便携模式判定**（新增） | `数据目录模式：常规 vs 便携（--portable）判定正确` = `常规→%LOCALAPPDATA%\桌面工具箱；便携→exe 旁边的 data\` | ☐ |
| 1.17 | **全新环境也清扫孤儿**（新增） | `索引文件不存在时也清扫 volatile 孤儿` = `没有 index.jsonl 的全新环境里，孤儿正文文件同样被回收` | ☐ |
| 1.18 | **OCR 引擎可用** 🆕 | `OCR 引擎可用（Windows 自带，零依赖）` = `可用语言：zh-Hans-CN…` | ☐ |
| 1.19 | **OCR 端到端** 🆕 | `OCR 端到端：造图 → 识别 → 文字对得上` = 三项准确率 ≥ 90% | ☐ |
| 1.20 | **OCR 空白图** 🆕 | `OCR 空白图给出明确失败（而非静默返回空）` | ☐ |
| 1.21 | **批量改名规则** 🆕 | `批量改名：规则计算（前缀/后缀/序号/替换/时间戳/扩展名）` | ☐ |
| 1.22 | **批量改名重名保护** 🆕 | `批量改名：同批次重名自动加序号 + 非法字符被拦下` | ☐ |
| 1.23 | **批量改名端到端** 🆕 | `批量改名：端到端（真实改名 → 撤销还原 → 重名自动加序号）` | ☐ |
| 1.24 | **哈希测试向量** 🆕 | `哈希：已知测试向量（"abc" 的 MD5 / SHA1 / SHA256）` 与公开标准值一致 | ☐ |
| 1.25 | **哈希比对** 🆕 | `哈希：相同文件判一致，改 1 个字节判不一致` | ☐ |
| 1.26 | **二维码结构** 🆕 | `二维码：编码基本结构（尺寸/三个定位图案）` | ☐ |
| 1.27 | **二维码往返** 🆕 | `二维码：自扫描往返（编码 → 渲染 → 识别）` = 三个用例全 ✓ | ☐ |
| 1.28 | **窗口置顶往返** 🆕 | `窗口置顶：切换后状态真的改变（真实窗口往返）` | ☐ |
| 1.29 | **工具注册完整性** 🆕 | `工具注册完整性（10 个工具都在注册表里）` = 10 个 Id 一个不缺 | ☐ |
| 1.30 | **默认热键无重复** 🆕 | `默认热键无重复（工具之间不互相抢键）` | ☐ |
| 1.31 | **工具注册完整性** 🆕 | `工具注册完整性（12 个工具都在注册表里）` = 12 个 Id 一个不缺 | ☐ |
| 1.32 | **忘了接线护栏** 🆕 | `工具发现：所有工具类都在 App 里注册了` = 工具类数与注册行数一一对应 | ☐ |
| 1.33 | **降级链跨工具无冲突** 🆕 | `热键降级链：跨工具无冲突（且不抢别人的默认键）` | ☐ |
| 1.34 | **运行命令实跑** 🆕 | `运行命令：cmd 实跑` + `PowerShell 实跑` 都退出码 0 | ☐ |
| 1.35 | **中文不乱码** 🆕 | `运行命令：中文输出不乱码（按实际代码页解码）` | ☐ |
| 1.36 | **超时中止** 🆕 | `运行命令：超时后真的中止（不会留挂着的 shell）` | ☐ |
| 1.37 | **ZIP 端到端** 🆕 | `批量打包：ZIP 端到端（建包 → 读回 → 内容逐字一致）` | ☐ |
| 1.38 | **RAR 端到端** 🆕 | `批量打包：RAR 端到端（用 WinRAR 建包，产物文件头为 Rar!）` | ☐ |
| 1.39 | **剪贴板原位置** 🆕 | `剪贴板：文件/文件夹显示原位置（单文件/单文件夹/多文件）` | ☐ |

**可选加测**（多等 30 秒，会真实调用当前 AI 后端）：
`run-toolbox.cmd --selftest-ai` → 应变成 `118/118 通过`，并打印后端名、首字耗时与译文。

- 本地 Ollama 后端实测：首字 17868 ms，共 17 段 / 64 字，译文正确。
- 远端 API 后端实测：首字 **176 ms**，共 17 段 / 64 字，译文正确。

---

## 2. 启动与常驻

| # | 检查项 | 操作 | 通过标准 | 结果 |
|---|---|---|---|---|
| 2.1 | 启动 | 双击 `run-toolbox.cmd` | 托盘出现图标，桌面出现竖排小悬浮窗，**没有报错弹窗** | ☐ |
| 2.2 | 单实例 | 再双击一次 `run-toolbox.cmd` | **不会出现第二个图标**，而是把已有的悬浮窗显示出来 | ☐ |
| 2.3 | 热键注册 | 看托盘气泡提示 | 若默认键被占用，会明确告诉你「已自动改用 XXX」。**本机预期**：剪贴板→`Win+Alt+V`、转换→`Win+Alt+C`、AI→`Win+Alt+A`、翻译选中→`Win+Shift+E`，裁剪保持 `Win+Shift+X` | ☐ |
| 2.4 | 日志 | 打开 `%LOCALAPPDATA%\桌面工具箱\logs\toolbox.log` | 末尾有 `启动完成。`，且**没有** `[错误]` 行 | ☐ |
| 2.5 | 启动哨兵 | 打开 `%LOCALAPPDATA%\桌面工具箱\startup-sentinel.log` | 最新一行的时间戳与刚才启动的时刻一致，`args=[]`（手动启动） | ☐ |
| 2.6 | 开机自启（可选） | 托盘 → 设置 → 勾选「开机自启」→ 保存 | 保存成功。注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 下出现值名 `桌面工具箱` | ✅ **已代验**（见下） |
| 2.7 | 关闭自启 | 取消勾选 → 保存 | 上面的注册表值消失 | ✅ **已代验**（见下） |
| 2.8 | ⭐ **自启指向的是哪个 exe** | 命令行跑 `dist\Toolbox.exe --autostart status` | `路径比对` 显示 **`一致 —— 自启会启动当前这个 exe`** | ✅ **已代验**（见下） |

> **2.6 / 2.7 已用命令行实跑验过**，不必你再点。命令是 `Toolbox.exe --autostart on|off|status`，
> 实测完整往返：
>
> ```
> 初始      : 未开启
> on        : 已写入启动项，下次登录会自动启动。
> 回读 status: 已开启（下次登录会自动启动）   ← 证明真的写进注册表了，不只是 API 没报错
> off       : 已删除启动项，不再自动启动。
> 回读 status: 未开启                        ← 恢复原状，没留残留
> ```
>
> 日志留痕（`toolbox.log`）：
> `命令行 --autostart on：写入启动项。` → `开机自启已开启："...\dist\Toolbox.exe"`
> → `命令行 --autostart off：删除启动项。` → `开机自启已关闭。`

### ⭐ 2.8 自启指向了哪个 exe —— 本轮查出来的真问题

**这是本轮最有价值的一条发现**：注册表里的自启项原来指向的是

```
"…\桌面工具箱\src\Toolbox\bin\Debug\net9.0-windows\Toolbox.exe" --startup
```

—— **Debug 构建**，不是交付的 `dist\Toolbox.exe`。

它"能跑"，所以看起来一切正常，但拉起的是源码目录里的开发版；
一旦源码目录被清理、挪走，或者 `dotnet clean` 一次，自启就**静默死亡**，
而且因为进程根本没起来，主日志里连一行都不会有。

**已修**：只要自启是开着的，**每次启动**都会把注册表校正成当前这个 exe（改写了会用托盘气泡告知一次）。
实测：

```
修正前 → 注册内容 : "...\src\Toolbox\bin\Debug\net9.0-windows\Toolbox.exe" --startup
         路径比对 : 不一致 ← 注册的是别的路径，自启可能起不来或启动了旧版本

（启动一次 dist\Toolbox.exe）

修正后 → 注册内容 : "...\dist\Toolbox.exe" --startup
         路径比对 : 一致 —— 自启会启动当前这个 exe
```

**你可以自己再确认一次**：命令行跑 `dist\Toolbox.exe --autostart status`（只读，无副作用）。
如果你想真的验证开机自启，需要**重启一次**，然后看 `startup-sentinel.log` 里有没有
`args=[--startup]` 那一行 —— 那是"开机自启确实把进程拉起来了"的唯一可靠证据。

---

## 3. ⭐ 必须你亲手点的（机器验不了）

### 3.1 悬浮窗不抢焦点 —— **最重要的一项**

> 这一项如果错了，AI 工具的「读当前窗口」会读到悬浮窗自己，整个功能就废了。

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 打开记事本，随便打几个字，**光标停在里面** | — | ☐ |
| 2. 用鼠标点一下悬浮窗上的任意图标 | 记事本**仍然是前台窗口**（标题栏仍是激活色），你刚才打的字**没有被中断** | ☐ |
| 3. 再点悬浮窗的空白/拖动区 | 同上，焦点不跑到悬浮窗 | ☐ |
| 4. 按 `Alt+Tab` | 悬浮窗**不在**列表里 | ☐ |

### 3.2 快捷剪贴板

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 随便复制几段文字（Ctrl+C） | — | ☐ |
| 2. 截个图（`Win+Shift+S`） | — | ☐ |
| 3. 在资源管理器里选中 2 个文件 Ctrl+C | — | ☐ |
| 4. 按 `Win+Alt+V`（或设置里显示的实际热键） | 面板弹出，**列表里三种内容都在**：文本有预览、图片有缩略图、文件显示文件名 | ☐ |
| 5. 在搜索框输入刚才某段文字里的关键词 | 列表实时过滤，条数变成「命中 X / Y 条」 | ☐ |
| 6. **把右上角的日期下拉框切到「今天」** | 只留下今天复制的内容；切回「全部时间」恢复 | ☐ |
| 7. **切到「昨天」** | 今天的不显示、昨天的显示（**边界**：昨天 23:59 算昨天，今天 00:00 不算） | ☐ |
| 8. **切到「最近 7 天」**，同时**在搜索框输个关键词** | 含今天往前数 7 天；两个条件是「与」的关系（日期筛完再按关键字筛） | ☐ |
| 9. **搜索框敲字时列表滚动是否流畅** | 缩略图不该每敲一个字符就全部重载（本轮加了缓存） | ☐ |
| 10. 选中一条按 `Enter` | 该内容回到剪贴板（面板关闭，你自己 Ctrl+V 验证） | ☐ |
| 11. 选中一条按 `Ctrl+P` | 标为置顶 | ☐ |
| 12. 选中一条按 `Ctrl+D` **和** `Ctrl+Delete` | 两个键**都能**删除（提示里写的是 `Ctrl+D / Ctrl+Delete`） | ☐ |
| 13. 面板打开时点别的窗口 | 面板**自动隐藏** | ☐ |
| 14. 复制一段很长的文章（比如网页整篇） | 历史里显示成「文章（N 字）」**一行摘要**，不是一大坨 | ☐ |
| 15. 时间列的显示 | 今天的条目显示 `HH:mm:ss`，更早的显示 `MM-dd HH:mm` | ☐ |

### 3.3 图片裁剪（`Win+Shift+X`）

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 先复制一张图片，再按热键 | 窗口打开，**左侧自动载入了刚才那张图** | ☐ |
| 2. 在图上拖框 | 出现选区 + 四个角手柄 | ☐ |
| 3. 拖角手柄调整 | 选区跟手，不跳、不飘 | ☐ |
| 4. 切到「调整尺寸」，输入 800×600 | 预览按比例变化 | ☐ |
| 5. 点保存 | 保存成功，**原文件没被覆盖**；同名时自动加 `(1)` | ☐ |
| 6. 打开保存出来的文件 | 尺寸与设定**完全一致**（右键属性看像素） | ☐ |
| 7. 在**缩放 125% / 150% 的显示器**上重做第 2 步 | 选区与鼠标位置**仍然对得上**（DPI 感知） | ☐ |

### 3.4 格式转换（`Win+Alt+C`）

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 按热键打开 | 窗口出现，显示依赖检查结果（LibreOffice / Office 是否可用） | ☐ |
| 2. 拖 2-3 个文件进去（混着放 png 和 docx） | 队列里出现，每个任务的「目标格式」下拉框**只列它支持的格式** | ☐ |
| 3. 选好目标格式，开始转换 | 逐条执行，每条有状态；**成功/失败都说清楚**，不静默 | ☐ |
| 4. 转完后打开产物 | 能正常打开，格式正确 | ☐ |
| 5. 拖一个 `.mp4` 进去 | 被**明确拒绝**，而且**点名**：`拒绝 1 个视频/音频（xxx.mp4）—— 本版不做音视频转换` | ☐ |
| 6. 拖一个带**中文名**的文件进去 | 转换成功，**不因中文路径失败** | ☐ |
| 7. 点取消 | 当前任务停止，不卡住 | ☐ |

### 3.5 ⭐ 快捷 AI（`Win+Alt+A`）—— 第二重要

> **本轮改过这里，请重点复验。**
> ① 按热键现在会**自动读当前窗口**（以前不读，"按热键就读当前页面"是句空话）；
> ② 「只取主内容区」以前是个空开关，现在真的会切掉首尾的导航/页脚短行。

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 打开一个**英文网页**，让它是前台窗口，然后按 `Win+Alt+A` | 面板打开，「将要送出的文本」里**已经自动填好了这个页面的正文**（不是空白，也不是悬浮窗的内容） | ☐ |
| 2. 看面板顶部那行提示 | 应写着 `将要读取：<窗口标题>` —— 让你在点「读取」**之前**就知道会读哪个窗口 | ☐ |
| 3. 检查这段文本 | 能看出是**网页正文**；首尾的导航/页脚短行应已被切掉，正文里如果有短行（清单、代码）**应该还在** | ☐ |
| 4. **手动改一下**「将要送出的文本」，再按一次 `Win+Alt+A` | 你改过的内容**不会被覆盖**（自动读取只在文本框为空时生效） | ☐ |
| 5. 点「翻译」 | 输出区**逐字流式冒出**译文（不是等半天后一次性出现） | ☐ |
| 6. 检查译文 | 是中文，方向正确（英文→中文） | ☐ |
| 7. 换成中文网页重做 1-6 | 译文是英文，方向正确（中文→英文） | ☐ |
| 8. **选中**网页里的一段文字（不复制），按 `Win+Shift+E` | 直接出译文，且**只翻这一段**（选中模式不会去读整窗口） | ☐ |
| 9. 上一步之后检查剪贴板 | **剪贴板里还是你原来复制的东西，没被改动**（这是硬约束） | ☐ |
| 10. 点「总结」送一段长文（3000 字以上） | 出现「正在分段摘要 N/M 段…」的进度，最后给出汇总 | ☐ |
| 11. 点「复制结果」 | 译文进剪贴板；**打开剪贴板历史，里面不该多出这条**（我们自己写的进不了档案） | ☐ |
| 12. 生成过程中点「取消」 | 立刻停止，界面不卡死 | ☐ |
| 13. 生成过程中**连点两下「翻译」** | 第二次应被拦下（提示"上一件事还没做完"），**不会两路输出交错在一起** | ☐ |
| 14. 打开**微信 / 记事本**这类不是浏览器的窗口，按 `Win+Alt+A` | 要么读到能用的文字，要么给明确提示；**不该读到工具箱自己界面的文字** | ☐ |

### 3.6 ⭐ 无快捷键模式（本轮新增）

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 托盘 → 设置 → 热键，把「快捷剪贴板」那一行**清空** → 保存 | 状态显示中性的 `— 已关闭（不占热键）`（**不是**橙色的 ⚠ 警告） | ☐ |
| 2. 按原来的 `Win+Alt+V` | 没有反应（热键确实被释放了） | ☐ |
| 3. 从悬浮窗 / 托盘打开剪贴板 | **照常能用** —— 这是正规用法，不是降级 | ☐ |
| 4. 看日志 | 有 `热键已禁用（用户设置为空）：clipboard —— 请从悬浮窗或托盘打开。` | ☐ |
| 5. 看日志的汇总段 | 应该是 `热键未注册（用户已关闭）：快捷剪贴板 —— …`，**不该**被归到「已被别的程序占用」那一类里 | ☐ |
| 6. 在那个空框里填「**默认**」两个字 → 保存 | 恢复成工具自带的键（或自动降级后的键），状态变 `✓ 已生效` | ☐ |
| 7. 把另一个热键清空后**重启程序** | 那个热键**仍然是关闭的**（空串被存下来了，没被悄悄退回成"用默认键"） | ☐ |
| 8. 打开 `settings.json` 看 | 被禁用的那个是 `"clipboard": ""`（空串），不是条目消失 | ☐ |

### 3.7 ⭐ AI 后端切换（本轮新增，也是"分享给别人"的关键）

> 目的：让对方**不用装 Ollama、不用拉 4.7 GB 模型**也能用 AI。

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 托盘 → 设置 → 快捷 AI，看「后端」下拉框 | 默认选中「本地 Ollama（数据不出这台机器）」，下面只显示 Ollama 的地址/模型 | ☐ |
| 2. 切到「远端 API（OpenAI 兼容，零安装）」 | 下面的输入框**换成** API 地址 / API Key / 模型名三项（两组不会同时显示，避免搞不清在用哪个） | ☐ |
| 3. 看「关于隐私」那一段 | 文案**跟着后端变**：切到远端后必须明确写「待处理的文本会通过网络发送到你在设置里填的那台服务器」 | ☐ |
| 4. 填一个远端地址（例：`https://api.deepseek.com/v1` ＋ `deepseek-chat` ＋ 你的 Key），点「测试连接」 | 给出 `[远端 API · deepseek-chat] 连接正常，远端提供 N 个模型` 之类的结果 | ☐ |
| 5. **地址故意不带 `/v1`**（例：`https://api.deepseek.com`），再测一次 | **仍然能通**（会自动补 `/v1`） | ☐ |
| 6. 故意填一个错的地址，点「测试连接」 | 给出明确中文提示，**不会永久卡在"正在测试…"**（有超时） | ☐ |
| 7. 点保存，然后按 `Win+Alt+A` 翻译一段 | 能出译文；面板状态行应写明当前后端，远端时提示数据会外发 | ☐ |
| 8. 切回「本地 Ollama」→ 保存 → 再翻译一次 | 恢复正常 | ☐ |
| 9. **可选**：命令行跑 `--selftest --ai` | 应打印 `AI 流式生成（真实调用「远端 API · xxx」，--ai）`，且 `31/31 通过` | ☐ |

> **第 4/5/9 项我已代验过**（用本机 Ollama 自带的 `/v1` 兼容接口当靶子）：
> 地址填 `http://127.0.0.1:11434`（**故意不带 `/v1`**）→ 自动补全 → 首字 176 ms、共 17 段 / 64 字、
> 译文 `The weather is very nice today, let's go for a walk in the park.` 完全正确，`31/31 通过`。
> 所以远端这条代码路径是**真的跑通过**的，不只是"看起来对"。
> 你那边只需要确认「用你自己的 Key 能连上」这一步。

### 3.8 ⭐ 快捷截图（`Win+Alt+S`）—— 本轮新增

> **交互**：按热键或点悬浮窗 → 整屏**冻结**成背景 → 鼠标拖拽框选 → 松手即存 PNG 并复制到剪贴板；
> 右键 / Esc 取消。截图**不进剪贴板历史**（带自污染标记），但能直接 Ctrl+V 粘贴。
>
> 自检已经验证了「GDI 全屏捕获 + 裁剪 + 保存 PNG + 写剪贴板」这条链路不崩、能出文件；
> 下面几项是要你确认**交互手感**和**坐标没偏**。

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 按 `Win+Alt+S` | 屏幕冻结成一张静态图，全屏蒙版、提示条「拖拽框选区域 · 右键 / Esc 取消」 | ☐ |
| 2. 鼠标从左上往右下拖出一块区域 | 出现蓝色描边选区，选区**外**变暗、选区**内**透出明亮背景 | ☐ |
| 3. 松手 | 选区存成 PNG（文件名 `截图_日期_时间.png`），**同时**复制到剪贴板（可直接 Ctrl+V） | ☐ |
| 4. 打开刚才保存的 PNG | 尺寸 = 你拖出来的那块区域，**内容正好是你框选的那块**（坐标没偏、没黑边） | ☐ |
| 5. 再按一次，刚拖一半时**按右键**（或 Esc） | 取消，不保存、不弹错 | ☐ |
| 6. 在**缩放 125% / 150% 的显示器**上重做第 1~4 步 | 截出来的图位置和大小**仍然对得上**（DPI 感知，运行时按显示宽度量换算系数） | ☐ |
| 7. 悬浮窗 → 设置 → 快捷截图，**把「截图保存目录」用「浏览…」选一个文件夹** → 保存 | 下次截图存进你选的目录；留空则存数据目录下的 `screenshots\` | ☐ |
| 8. 截图时悬浮窗是否躲开 | 截图遮罩是 Topmost 全屏，悬浮窗不会截进背景里 | ☐ |
| 9. 已有一个截图遮罩在框选时，**再按一次** `Win+Alt+S` | 不叠加第二个遮罩（直接忽略第二次触发） | ☐ |

> 关于"框选坐标会不会偏"：截图换算现在取**窗口所在显示器的真实 DPI**
> （`Core\ScreenCoordinateMapper.cs`，`GetDpiForWindow` 逐级兜底），
> 并由自检在 100% / 125% / 150% / 200% 四档上做**精确断言**（1px 不差）。
>
> ⚠️ **修掉的旧缺陷**：以前用的是「位图像素宽 ÷ 背景图显示宽」**一个全局比值**。
> 单屏下等价于 `dpi/96` 所以看不出问题，但**多屏且各屏缩放不同**时必然算错
> （一个比值表达不了两个比例）。本机是单屏 100%，所以这个缺陷在本机从未显形。
> 详见 `DECISIONS.md` 坑 26。
>
> 因此下面第 6 步（125%/150% 显示器）现在**有自动化证据支撑**，
> 但"多屏混合 DPI"仍建议有条件时手验一次。

### 3.9 边界与异常

| 步骤 | 通过标准 | 结果 |
|---|---|---|
| 1. 托盘 → 暂停监听 → 复制几段文字 → 恢复监听 | 暂停期间的复制**不进历史**；恢复后正常 | ☐ |
| 2. 托盘 → 显示/隐藏悬浮窗 | 切换正常 | ☐ |
| 3. 托盘 → 设置，改一个热键为 `Ctrl+Alt+F9`，保存 | 状态显示 `✓ 已生效`；按新热键能用 | ☐ |
| 4. 改一个热键为**已被占用**的组合（比如 `Win+Shift+S`），保存 | 显示 `✗ 已被别的程序占用`，**并且不会偷偷换成别的** | ☐ |
| 5. 改回默认，保存 | 若默认键被占用，显示 `⚠ 默认键…已被占用，已自动改用…` | ☐ |
| 6. 关掉 Ollama（用本地后端时），按 `Win+Alt+A` 点翻译 | 给出**明确提示**（「连不上本地 Ollama」之类），不是让你对着转圈发呆 | ☐ |
| 7. **把 Ollama 地址改成不可达的黑洞地址**（如 `http://10.255.255.1:11434`），点翻译 | **12 秒内**给出超时提示 —— 不该永久卡在"正在检查…"（本轮修的就是这个） | ☐ |
| 8. 托盘 → 退出 | 进程真的退出（任务管理器里没了），悬浮窗和托盘图标都消失 | ☐ |

---

## 4. 跑一天（最真实的验收）

机器自检只能证明「链路通」，证明不了「用一整天会不会出问题」。建议：

1. 让工具箱开着，**正常用电脑一整天**（复制文字、截图、复制文件，都来）。
2. 第二天看两件事：

| 看什么 | 在哪 | 通过标准 |
|---|---|---|
| 有没有漏 | 剪贴板历史 | 你印象里复制过的东西，基本都能找到 |
| 有没有卡 | 体感 + 日志 | 有没有哪个程序复制/粘贴时变卡甚至卡死；`toolbox.log` 里 `[错误]` 行是否明显增多 |
| 内存有没有涨 | 任务管理器 | 不该持续单调上涨（正常在 150-250 MB 量级波动） |

---

## 5. 记录结果

把下面这张表填一下，连同你发现的问题一起给我：

| 节 | 项数 | 通过 | 不通过 | 备注 |
|---|---|---|---|---|
| 1. 机器自检 | 39 | | | 预期 117/117 |
| 2. 启动与常驻 | 8 | | | 2.6/2.7/2.8 已代验 |
| 3.1 悬浮窗不抢焦点 | 4 | | | **最重要** |
| 3.2 快捷剪贴板 | 15 | | | 含日期筛选 |
| 3.3 图片裁剪 | 7 | | | |
| 3.4 格式转换 | 7 | | | |
| 3.5 快捷 AI | 14 | | | **本轮改过，重点复验** |
| 3.6 无快捷键模式 | 8 | | | 本轮新增 |
| 3.7 AI 后端切换 | 9 | | | 本轮新增，4/5/9 已代验 |
| 3.8 快捷截图 | 9 | | | 本轮新增 |
| 3.9 边界与异常 | 8 | | | |

---

## 6. 本轮（质检后）改了什么

如果只想快速了解这一轮动了哪些地方：

| 改动 | 验收项 |
|---|---|
| **开机自启加固**：启动哨兵日志 + 每次启动校正注册表路径 + 延迟 8 秒初始化 | 2.5 / 2.8 |
| **修掉一个隐藏很久的问题**：注册表指向 Debug 构建而非交付的 exe | 2.8 |
| **无快捷键模式**（留空 = 不占热键） | 3.6 |
| **剪贴板日期筛选**（今天/昨天/最近 7 天/最近 30 天）+ 缩略图缓存 | 3.2 第 6~9 项 |
| **AI 远端后端**（OpenAI 兼容，零安装，可分享） | 3.7 |
| **AI 按热键自动读当前页面** + 「只取主内容区」真正生效 | 3.5 第 1~4 项 |
| **修 volatile 孤儿文件无限占磁盘**（质检唯一"必须修"） | 1.8 |
| 视频/音频拖入改为**点名拒绝** | 3.4 第 5 项 |
| `Ctrl+D` 现在真的能删（以前只有 `Ctrl+Delete`） | 3.2 第 12 项 |
| 探活加超时（以前黑洞地址会永久卡死） | 3.8 第 7 项 |
| 连点两次不再产生两路交错输出 | 3.5 第 13 项 |
| Office COM 超时后清理僵尸 WINWORD；不动用户自己开的 Word | 3.4（难手动触发，已知边界第 6 条） |

逐条整改明细（含质检报告里一处不准确判断的说明）见 `07-质检整改回复.md`。

### 再上一轮（2026-09-18）：设置「浏览…」按钮 + 第 5 个工具「快捷截图」

| 改动 | 验收项 |
|---|---|
| **设置里的路径不再手敲**：LibreOffice 路径加了「浏览…」选文件；新增「快捷截图」区，保存目录用「浏览…」选文件夹（WinForms 的 FolderBrowserDialog） | 3.8 第 7 项 |
| **第 5 个工具：快捷截图**（GDI BitBlt 全屏冻结 + 拖拽框选 + 裁剪 + 存 PNG + 复制剪贴板，带自污染标记不进历史） | 1.11 / 3.8 |
| 截图保存目录可配置；留空落到数据目录 `screenshots\` | 3.8 第 7 项 |
| 截图热键 `Win+Alt+S` 加了降级链（被占用时降级到 `Win+Alt+Z` / `Win+Alt+Q`） | 3.9 第 4 项 |

---

## 8. 最新一轮（可分享性）

> 用户问："能不能让它不依赖我电脑上的东西？要么一起打包，要么用别人电脑天生就有的。"

| 改动 | 说明 | 验收项 |
|---|---|---|
| **便携模式**（新增） | exe 旁边放 `portable.txt`，或加 `--portable` ⇒ 数据全在 exe 同级的 `data\`，删文件夹即彻底卸载。给"拷给同事试用"用 | 1.16 |
| **修掉一个真 bug**（便携模式暴露） | `HistoryStore.Load()` 在"索引文件不存在"时 `return`，把写在后面的 `SweepOrphanPayloads()` 一起跳过了 ⇒ **全新安装时孤儿文件永远不清**（只增不减）。已修 + 补回归用例 | 1.17 |
| 自检 32 → **34 项**（`--ai` 33 → 35） | 两条新增均为防回归断言 | 1.16 / 1.17 |

**依赖核实结论**（实测，详见 `12-分享给别人-依赖说明.md`）：

- .NET 9 运行时**已打进 exe**（`includedFrameworks` 实测确认）⇒ 对方**什么都不用装**；
- 把 `dotnet` 从 PATH 彻底拿掉后跑自检 ⇒ **34/34 全过**；
- 零第三方库（全库 `using` 只有 .NET/Windows 命名空间，`csproj` 零 `PackageReference`）；
- 缺 LibreOffice / Office 时**只降级不崩**（11/11 实测）；
- AI 用「远端 API」后端可**零安装**使用（实测 33/33，译文正确）。

**分享方式**：把 `share\` 文件夹整个发过去即可 ——

```
04-其他项目\桌面工具箱\share\
├─ 桌面工具箱.exe    ← 程序本体（65.2 MB，运行时+图标都已内嵌）
├─ toolbox.ico       ← 图标（可删，exe 已内嵌一份）
├─ portable.txt      ← 有它 = 便携模式
└─ 本说明.txt         ← 给对方看的说明
```

---

## 9. 最新一轮：新增 5 个工具

> 用户加菜：一次只做一个、每个独立目录、自带 README 与最小测试。全部完成。

| 工具 | 热键（本机实际） | 一句话 | 自检项 |
|---|---|---|---|
| **OCR 截图识字** | `Win+Alt+J` | 框选屏幕区域 → 识别文字 → 自动复制 | 1.18~1.20 |
| **批量重命名** | `Win+Alt+I` | 前缀/后缀/替换/序号/时间戳，**可预览可撤销** | 1.21~1.23 |
| **哈希校验** | `Win+Alt+H` | MD5/SHA256 + 两文件对比 | 1.24~1.25 |
| **二维码工具** | `Win+Alt+Q` | 生成 + 扫描（自实现编解码） | 1.26~1.27 |
| **窗口置顶** | `Win+Alt+P` | 当前窗口置顶 / 取消（toggle） | 1.28 |

**每个工具目录下的 README** 都写了：依赖、用法、**已知限制**、最小测试脚本。
尤其这两个的能力边界值得看：

- `src\Toolbox\Tools\QrCode\README.md` —— 扫描**不做透视校正、不做纠错译码**，
  只支持"自己生成的、正对屏幕的、完整的"二维码；拍照的请用手机扫。
- `src\Toolbox\Tools\Ocr\README.md` —— Windows OCR 对某些字形有固有弱点
  （实测同一句话在不同字号下"别"字会被认成"另刂"）。

**本轮修掉的 3 个问题**（都记进了 DECISIONS）：

| 问题 | 为什么危险 |
|---|---|
| **二维码工具忘了注册** | 代码都在、编译不报错，但没热键、悬浮窗看不到、托盘也没有。新增"工具注册完整性"用例钉住 |
| **新工具降级链互相撞车** | 凭旧占用表推的热键，实际 `Win+Alt+T/R` 已被占用。重测占用表后重挑，并加"默认热键无重复"断言 |
| **托盘缺少直达全部工具的入口** | 工具涨到 10 个后，托盘加「所有工具」子菜单作为兜底入口 |

**外部独立验证**（二维码编码器的硬证据，**不依赖自检的自洽性**）：

```
cd artifacts\probe-qr
dotnet run -c Release      → ZXing 独立验证：24/24 通过
```

> 用 ZXing.Net（完全独立的实现）解码本工具生成的二维码：
> 6 种内容 × 4 个纠错等级 = 24 组，全部正确。
> ZXing **只在验证工具里用，不进产品** —— 产品保持零第三方依赖。

---

*本清单基于 2026-09-18 的实现状态编写。自检数据、热键占用表、踩坑记录见 `DECISIONS.md`。*

---

## 7. 最新一轮（2026-09-18 晚）：独立验收 + 两处缺陷修复

> 由接手方（DeepSeek Harness）**独立复验**所得，不采信交付方自述。
> 完整过程与证据见 `09-独立验收报告.md`。

| 改动 | 性质 | 验收项 |
|---|---|---|
| **修掉：热键「自动降级」冒充用户意图** —— 降级结果写回 `HotKeys` 后被当成"用户改过的键"，导致默认键重新空闲后也永远回不去、且备选键再被占用时只报错不降级 | 真缺陷（用户可见） | 1.14 / 3.6 |
| **修掉：截图坐标用单一全局比值** —— 多屏且各屏缩放不同时必然算偏（实测反例偏 30 px）。改为取窗口所在显示器的真实 DPI | 潜伏缺陷（本机单屏 100% 未显形） | 1.15 / 3.8 |
| 新增 `Core\ScreenCoordinateMapper.cs`：换算抽成纯函数，补上越界夹取与非法系数兜底 | 加固 | 1.15 |
| 自检 **30 → 34 项**（`--ai` 31 → 35），新增两条均为**针对上述缺陷的防回归断言** | — | 1.14 / 1.15 |
| 独立验收夹具 + 变异测试落在 `artifacts\verify\`（**不属于产品**，是验收证据） | — | 见 `09-独立验收报告.md` |

**复核命令**（一条就能复现验收方的结论）：

```
dist\Toolbox.exe --selftest          → 预期 117/117
dist\Toolbox.exe --selftest --ai     → 预期 33/33
```

用旧算法反证用例有鉴别力（变异测试）：

```
cd artifacts\verify && dotnet run --project mutrun -c Release
```
