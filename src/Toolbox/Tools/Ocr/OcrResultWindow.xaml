<Window x:Class="Toolbox.Tools.Ocr.OcrResultWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 截图识字 · 识别结果"
        Width="640" Height="480"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        FontFamily="Microsoft YaHei UI, Microsoft YaHei, Segoe UI"
        Background="#FFF6F7F9">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 状态行：识别语言 + 字数 -->
        <TextBlock x:Name="StatusText" Grid.Row="0"
                   Foreground="#FF1F2329" FontSize="13" TextWrapping="Wrap"
                   Text="正在识别…" />

        <TextBlock Grid.Row="1" Margin="0,8,0,4"
                   Foreground="#FF8A94A6" FontSize="12"
                   Text="识别结果（可以直接在这里修改，改完再点「复制」）" />

        <!-- 结果文本框：可编辑，这是最可靠的兜底 -->
        <TextBox x:Name="ResultBox" Grid.Row="2"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Microsoft YaHei UI, monospace"
                 FontSize="14"
                 Padding="8"
                 BorderBrush="#FFD5D9E0" BorderThickness="1" />

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="SaveHint" VerticalAlignment="Center"
                       Foreground="#FF12A150" FontSize="12" Margin="0,0,12,0" />
            <Button x:Name="CopyBtn" Content="复制" Width="88" Height="30" Margin="0,0,8,0" />
            <Button x:Name="CopyCloseBtn" Content="复制并关闭" Width="104" Height="30" Margin="0,0,8,0" />
            <Button x:Name="CloseBtn" Content="关闭" Width="88" Height="30" />
        </StackPanel>
    </Grid>
</Window>
