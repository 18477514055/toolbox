# 桌面工具箱 · 对第三方质检报告的逐条整改回复

> 对应文档：`06-第三方质检报告.md`
> 回复时间：2026-09-17
> 结论：**13 条问题，12 条已修（含唯一"必须修"的 B-1），1 条为设计取舍并说明理由。**
> 另外，质检报告本身有 **1 处判断不准确**，见文末「对质检结论的两点修正」。
>
> 本轮同时交付了 4 项用户新需求（无快捷键模式 / 剪贴板日期筛选 / AI 远端后端 / 开机自启加固），
> 以及 **7 项新增自检用例**。自检从 21 项 → **28 项**（`--ai` 时 29 项），**全部通过**。

---

## 一、结论速览

| 编号 | 严重度 | 问题 | 状态 |
|---|---|---|---|
| B-1 | 🔴 必须修 | `volatile\` 无上限磁盘泄漏 | ✅ 已修 + 新增自检 |
| A-1 | 🟠 高 | 界面宣称"按热键就读当前页面"但实际不读 | ✅ 已修 |
| A-2 | 🟠 高 | 「只取主内容区」是空开关 | ✅ 已修 |
| B-2 | 🟡 中 | 去重顶掉的条目正文也成孤儿 | ✅ 已修（同 B-1 一处修法覆盖） |
| B-3 | 🟡 中 | Office COM 超时留僵尸 WINWORD | ✅ 已修（附带修了一个更严重的隐患） |
| B-4 | 🟡 中 | 搜索每敲一字符全表重载缩略图 | ✅ 已修 |
| C-1 | ⚪ 低 | 提示写 `Ctrl+D`，实现是 `Ctrl+Delete` | ✅ 已修（两个键都支持） |
| C-2 | ⚪ 低 | 视频/音频静默跳过而非明确拒绝 | ✅ 已修 |
| C-3 | ⚪ 低 | AI 读选区不校验焦点窗口归属 | ✅ 已修 |
| C-4a | ⚪ 低 | `TargetInfo` 从未被赋值 | ✅ 已修 |
| C-4b | ⚪ 低 | `systemPrompt` 恒 null（死代码） | ⏸ 保留（见 §四说明） |
| C-4c | ⚪ 低 | 探活无超时 + 不可取消 → 黑洞地址永久卡死 | ✅ 已修 |
| C-4d | ⚪ 低 | 每块翻译 `new HttpClient` 不释放 | ✅ 已修 |
| C-4e | ⚪ 低 | `_running` 守卫跨 `await` | ✅ 已修 |

---

## 二、🔴 B-1：`volatile\` 无上限磁盘泄漏（唯一"必须修"）

### 质检的判断

> `HistoryStore.cs:235-237` 无条件把正文写进 `volatile\<hash>.txt`；`:239-244` 只在 >10240 字时才设
> `entry.Volatile` 指针；`:455-458` 删除时只删有指针的。实测 12 条索引 / 13 个 volatile 文件，
> **100% 是孤儿**。

### 我的核查结论：**bug 成立，但"100% 是孤儿"这个描述不准确**

先说结论差异，因为它直接影响修法：

这些正文文件**并不是没人引用的垃圾**。`GetFullText()` 在降级分支里会按 `entry.Hash` 去读
`volatile\<hash>.txt` —— 也就是说**普通长度的文本**（不设 `Volatile` 指针那批）也依赖这个文件。
如果按"100% 是孤儿"去理解，最直觉的修法就是**全部删掉**，那会把历史里大部分条目的正文一起删掉，
比原 bug 严重得多。

真正的缺陷只有一条：**删除路径回收不到它们**，于是文件只增不减。这与质检测出的现象一致，
但成因不是"写进去没用"，而是"删的时候找不到"。

### 修法（两处，互相兜底）

1. **删除时按 Hash 回收**（`HistoryStore.DeletePayload`）：
   除了原来的"有 `Volatile` 指针就删那个文件"，补上按 `entry.Hash` 删 `.txt` / `.html`。
2. **载入时清扫孤儿**（新增 `SweepOrphanPayloads()`，在载入构造末尾调用）：
   把全部条目的 `Hash+".txt"` / `Hash+".html"` / `Volatile` 文件名收进一个 `alive` 集合，
   目录里**其余文件全部删除**，并记一行日志：
   `清理 volatile 孤儿正文文件 N 个，释放 X KB。`

第 2 条是兜底：即使将来又出现"某种路径删不掉"的情况，最多残留到**下一次启动**，
不会无限增长。B-2（去重顶掉的条目正文成孤儿）走的是同一条清扫路径，一并解决。

### 实测验证

造 2 个孤儿文件 → 启动程序 → 文件数 36 → 34，日志出现
`清理 volatile 孤儿正文文件 2 个，释放 0.0 KB。`

并新增自检用例把它钉住（**这条以后回归会被自动发现**）：

```
[通过] volatile 孤儿正文文件在载入时被回收（B-1 防回归）
        造了一个孤儿文件，载入后被清掉（不再无限占磁盘）
```

---

## 三、🟠 A-1 / A-2：AI 的两个"承诺了但没做"

### A-1 按热键不自动读当前页面

质检说 `AiTool.cs` 只有 `GrabSelectionAsync`，没有任何地方调 `GrabWindowAsync` ——
所以验收清单 3.5 第 1 项会失败。**成立**。

修法：`AiTool.Invoke()` 里补上两步（放在 `panel.Activate()` 之后）：

```csharp
panel.RefreshTargetInfo();          // 顺带修掉 C-4a（原来这个提示永远空白）
_ = AutoGrabQuietlyAsync(panel);    // 兑现"按热键就读当前页面"
```

`AutoGrabWindowAsync()` 有两条**不做**的自我保护，避免帮倒忙：

- 当前是「选中模式」→ 不读。那种模式下要读的文本在打开面板那一刻就已经捕获好了，
  再读一次整窗口会把用户真正想要的那段选区**覆盖掉**。
- 「待处理文本」里已经有内容 → 不读。用户自己改过的内容不该被一次自动读取冲掉。

读取失败只记日志，不弹窗（自动行为不该打断人）。

### A-2 「只取主内容区」是空开关

质检说 `TextGrabber.cs:199-204` 两个分支返回同一个 `best`，只有标签字符串不同。**成立**。

修法：新增 `TrimNavAndFooter(text, out bool didTrim)`，扫描**首尾连续的"短行"块**并切掉。
判据是"像标签而不是像句子"：长度 ≤ 24 字，且不以句末标点收尾。

**关键是加了保底**：如果切完之后剩下的不足原文一半，判定为误判，**原样返回**并记一行日志。

> 为什么必须有这个保底：正文里有大量短行（代码、清单、地址、对话）。一旦把它们误判成导航切掉，
> 用户拿去翻译的就是一段残缺的文本 —— 而且他不会知道少了东西。
> **误删正文的代价远大于留一点噪声。**

三条读取路径（根文档 / 最佳文档 / 控件收集）都接上了。界面上那个勾选框的文案也从
「少带导航 / 广告 / 评论区噪声」改成「**自动去掉首尾的导航 / 页脚短行**」——
原文案让人以为它什么都能清，实际上它只做首尾、且很保守。

---

## 四、🟡 B-2 / B-3 / B-4

### B-2 去重顶掉的条目正文成孤儿
由 B-1 的 `SweepOrphanPayloads()` 覆盖，不单独处理。

### B-3 Office COM 超时留僵尸 WINWORD

质检说得对：`Thread.Join(timeout)` 超时只是"我们不等了"，那个 STA 线程连同 Word 进程还活着
（.NET Core 起没有 `Thread.Abort`，没法从外部掐断），于是 `finally` 里的 `Quit()` 永远执行不到。

修法分两层：

1. **超时后清理**：开工前快照 `WINWORD` 进程 ID，超时后**只杀这次新冒出来的那些**（做差集）。
   用户自己开着的 Word 一个都不碰。日志记录清掉了几个。
2. **顺手修了一个更严重的隐患**（质检没提，但我在读这段代码时发现的）：
   Word 的 COM 服务器是**单实例**的。如果用户本来就开着 Word，`Activator.CreateInstance` 拿到的是
   **他那个实例** —— 那么原来的 `word.Quit()` 会**把他正在编辑、可能还没保存的文档一起关掉**。
   这是数据损失。现在改成：开工前先记下"Word 是否已在运行"，已在运行就**跳过 `Quit`**，
   只 `Close(0)` 我们打开的那一份（0 = 不保存改动）。

### B-4 搜索每敲一字符全表重载缩略图

修法：加 `_thumbCache`（`Dictionary<路径, BitmapSource>`），`LoadThumbCached()` 命中直接复用。
缓存超过 400 条时**整体清空**，刻意不做 LRU —— LRU 要维护访问顺序，代码量和出错面都不小，
而这里的收益只是省几百 KB 内存。

---

## 五、⚪ C-1 ~ C-4

| 编号 | 修法 |
|---|---|
| C-1 | `Ctrl+D` 和 `Ctrl+Delete` **都支持**（两个 `case` 并列），底部提示同步改成 `Ctrl+D / Ctrl+Delete = 删除`。质检建议"改一个字符串"，但用户已经照提示按过 `Ctrl+D` 了，把实现补上比改提示更符合预期。 |
| C-2 | `ConvertWindow.AddPaths` 单独收集被拒的音视频文件，在明细里**点名**：`拒绝 N 个视频/音频（a.mp4、b.mp3）—— 本版不做音视频转换（需要 ffmpeg，本机没装）`。 |
| C-3 | `TextGrabber` 新增 `IsOwnProcess(element)`，比对 `AutomationElement.Current.ProcessId` 与本进程 ID。命中自己直接返回明确提示，不再去读工具箱自己的控件文本。 |
| C-4a | 见 A-1。 |
| C-4b | **保留，不删。** 理由见下。 |
| C-4c | 探活套上**有界** `CancellationTokenSource`（本地 12 秒 / 远端 20 秒）。原先传 `CancellationToken.None` 而 `HttpClient` 又是 `InfiniteTimeSpan`，等于这次调用既无超时也不可取消 —— 地址填成黑洞 IP 就永久卡在"正在检查…"，只能强杀进程。 |
| C-4d | `HttpClient` 改成**静态共享**。每次 `new` 都建一个连接池，而 `Dispose` 不会立刻关闭底层 socket（进 TIME_WAIT）；翻译长文要连发几十个请求，socket 会被耗干，症状是"翻到一半突然连不上"。 |
| C-4e | `_running = true` 提到**第一个 `await` 之前**，整个方法体包进 `try/finally`。原写法判断与赋值之间隔着一个 `await`，该窗口期内连点两下两个调用都会通过 —— 两路流式同时写同一个输出框，输出交错，且「停止」只能停掉后一条。 |

### 关于 C-4b（`systemPrompt` 恒 null）

质检自己已经注明"这不影响翻译质量"。我的判断是**保留**：

- 它是 `IAiClient` 接口的一部分，远端后端会把它拼成 `messages[0].role = "system"`，
  这条路径是真实可用、且有意义的（有些网关对 system 消息的指令遵循更好）；
- 提示词目前已经把规则写全在 user 消息里，**翻译质量已被实测验证**。
  为了消除"死代码"的观感去改动一条已验证正常的行为，风险大于收益。

**不动已验证正常的东西**，这条我倾向于保留并在代码注释里写明。

---

## 六、对质检结论的两点修正

### 1. B-1 的"100% 是孤儿"不准确

见 §二。文件是被 `entry.Hash` 引用的（`GetFullText` 的降级分支会读它们），
真正的缺陷只是"删除路径回收不到"。这个区别很关键：按错误理解去修会**删掉历史里大部分正文**。

### 2. 我认同质检的工作方式，并沿用了一条

报告里提到"把只读核查下发给本机 7B 模型导致编造原文引用"的教训。
本轮所有关键断言我都做了**可复现的验证**，而不是靠阅读代码下结论：

| 断言 | 怎么验的 | 结果 |
|---|---|---|
| 自启路径自愈真的生效 | 让 `dist\Toolbox.exe` 自己发现注册表指向 Debug 构建并改写，前后各查一次 | ✅ 见 §七 |
| 无快捷键模式真的禁用 | 把 `clipboard` 热键写成空串 → 启动 → 查日志 | ✅ `热键已禁用（用户设置为空）：clipboard` |
| 「条目不存在 = 用默认键」没被误伤 | 删掉 `ai.translateSelection` 条目 → 启动 | ✅ 回落到默认 `Win+Shift+T`，被占用后自动降级到 `Win+Shift+E` |
| 远端 OpenAI 兼容后端真的能跑 | 后端切到 remote，指向 Ollama 自带的 `/v1` 接口，跑 `--selftest --ai` | ✅ 29/29，首字 176 ms，译文正确 |
| 远端地址不带 `/v1` 会自动补 | 故意填 `http://127.0.0.1:11434` | ✅ 自动补成 `.../v1/chat/completions` |
| 孤儿文件真的被回收 | 造 2 个孤儿 → 启动 → 数文件 | ✅ 36 → 34，日志有记录 |

---

## 七、顺带修掉的、比质检报告更严重的一个问题

**开机自启注册表指向的是 Debug 构建的 exe，不是交付的 `dist\Toolbox.exe`。**

这是用新加的 `--autostart status` 查出来的：

```
注册内容 : "C:\...\桌面工具箱\src\Toolbox\bin\Debug\net9.0-windows\Toolbox.exe" --startup
路径比对 : 不一致 ← 注册的是别的路径，自启可能起不来或启动了旧版本
```

**为什么这个比质检报告里任何一条都严重**：它"能跑"。开机确实拉起来了一个工具箱，
看起来一切正常 —— 但拉起的是源码目录里的开发版，不是交付的那个 exe。
一旦源码目录被清理、挪走、或者 `dotnet clean` 一次，自启就**静默死亡**，
而且因为进程根本没起来，主日志里连一行都不会有。

同时我发现自己之前写的自愈逻辑**有个洞**：

> 自愈只在 `--startup` 启动时执行。可注册表指向错路径的后果，正是"`--startup` 启动根本不会发生"——
> 等于永远等不到那次调用。

已改成：**只要自启是开着的，每次启动都校正一次**；改写了就通过托盘气泡告知一次
（用气泡不用模态框 —— 开机弹窗既打断人，又长得像"启动失败"）。

**实测验证**：修正前 `路径比对：不一致` → 启动一次 → 修正后 `路径比对：一致`。

另外新增的启动哨兵（`startup-sentinel.log`，独立于主日志）解决了一个排障死角：
主日志要 `Log.Open` 之后才存在，而"启动失败"恰恰可能发生在它之前 ——
那种情况下主日志一片空白，看起来像"程序从没启动过"。现在每次启动尝试都会在哨兵里留一行。

---

## 八、自检变化

**21 项 → 28 项**（`--ai` 时 29 项），全部通过。新增的 7 项都是**针对已发生过的问题**的回归断言：

| 新增用例 | 防的是什么 |
|---|---|
| 剪贴板日期筛选档位（含跨月 / 时间戳损坏） | 日期筛选的 off-by-one（新功能，10 条边界用例） |
| 自启命令的路径解析（带引号 / 空格 / 中文 / 无引号） | 注册表路径解析 —— 就是 §七 那个问题 |
| 自启路径比对（认得出「不是当前 exe」→ 触发自愈） | 自愈逻辑失效会静默放过错误路径 |
| volatile 孤儿正文文件在载入时被回收 | B-1 磁盘泄漏回归 |
| 设置往返：空串热键（= 禁用）与 AI 远端字段不丢 | "禁用热键"退化成"用默认键"；AI 新字段静默丢失 |
| 远端 API 端点拼接 | 带不带 `/v1` 都要能用，且不产生 `//v1` |
| 远端后端未填地址时给出明确中文提示 | 空配置不能抛异常、不能发无谓请求 |

**新增用例当轮就抓到了一个真 bug**：日期档位的 `today` 参数没归一化到零点。
生产调用点传的是 `DateTime.Today`（本来就是零点）所以看不出来，但一旦传入 `DateTime.Now`，
所有 `>= today` 判断会把今天凌晨的记录误判成"不在今天"—— 而这种错误**只在白天出现、半夜测试永远正常**。
已在函数内主动 `.Date` 归一化，把它变成不可能犯的错。

---

## 九、仍未验证、必须人工确认的

质检报告 §五列的那几条我依然验不了（机器替不了），照旧列在 `05-验收清单.md` 第 3 节：

1. **悬浮窗是否真的不抢焦点** —— 全项目功能正确性的前提（源码逐字确认过，但只有人手能验）；
2. **AI 读「当前页面」读得准不准** —— 本轮刚修了 A-1/A-2，这条**特别需要人工复验**；
3. **设置窗口里 AI 后端的切换与保存** —— 本轮新增，界面交互只能人点；
4. **开机自启的最终效果** —— 需要真的重启一次。

第 2 条尤其重要：A-1/A-2 是我这一轮改动里**唯一会改变用户可见输出**的地方（自动读取 + 降噪），
改动逻辑本身有自检覆盖，但"读出来的内容对不对"只有人看才知道。
