# B 阶段：工具插件化（可下载工具）

> 本轮把「工具」真正拆成了**可独立下载的插件**。
> 用户要的「被分享的用户可以按需下载里面的工具」—— **已经能用了**。

---

## 一、结论先讲：现在能做到什么

| 场景 | 结果 |
|---|---|
| 只装主程序 | **5 个内置工具**：剪贴板 / 裁剪 / 转换 / AI / 截图 |
| 装齐 7 个插件 | **12 个工具**，插件工具与内置工具**完全同权** |
| 删掉某个插件 | 那个功能**彻底消失**（不是隐藏，是真的没有），且不报错 |

**7 个可下载插件**（共 109 KB）：OCR 识字 / 批量重命名 / 哈希校验 /
二维码工具 / 窗口置顶 / 运行命令 / 批量打包。

**实测证据**（不是推测）：

```
① 清空插件目录后启动       → 工具已启动 = 5
   （clipboard / image / convert / ai / screenshot）
② 从 Release 下载并装齐 7 个 → 工具已启动 = 12，插件已加载 = 7
③ 热键：12 个工具全部注册成功
   （其中 ocr / rename / archive 因默认键被别的程序占用，
     自动降级到 Win+Alt+N / Win+Alt+Z / Win+Shift+I）
```

**为什么是这 5 个内置**（用户拍板）：
主程序开箱就得能用；而剪贴板是**托盘与悬浮窗的支点**，
它若也是插件，不装插件时的行为会很别扭。这 5 个恰好覆盖"几乎每天都会用"的那类。

---

## 二、架构：三个部分

```
┌─────────────────────┐
│   Toolbox.exe       │  主程序（11 个内置工具 + 插件加载器）
│   + Contracts.dll   │  ← 契约，必须和 exe 放一起
└──────────┬──────────┘
           │ 加载
           ▼
   %LOCALAPPDATA%\桌面工具箱\plugins\<工具id>\Xxx.dll
           ▲
           │ 下载（多镜像 + SHA-256 校验）
           │
   GitHub Release 资产（每个插件一个 zip）
```

| 部分 | 说明 |
|---|---|
| **`Toolbox.Contracts.dll`**（52 KB） | 主程序与插件的**共享契约**：`IToolboxTool` / `ToolboxContext` / `Log` / `AppPaths` / `Settings` / `NativeMethods` / 剪贴板读写… |
| **`PluginLoader`** | 扫插件目录 → 读清单 → 校验兼容性 → 实例化 → 交给同一套工具注册表 |
| **插件包** | 一个几 KB 的 zip，里面就是插件的 dll |

**契约为什么必须独立成一个 dll**：
主程序是**单文件自包含 exe**，磁盘上没有 `Toolbox.dll` 可供插件引用。
插件与主程序必须引用**同一个**契约实体，类型身份才唯一 —— 否则主程序认不出插件实现的那个接口。

---

## 三、四条实测确认的硬约束

> 这几条都是**实际踩出来的**，不是查文档抄的。见 `DECISIONS.md` 坑 39。

### ① 契约必须留在磁盘上

`PublishSingleFile` 默认把**所有**托管程序集塞进 exe。
契约一旦被塞进去，插件就解析不到它。
→ 必须 `<ExcludeFromSingleFile>true</ExcludeFromSingleFile>`。

**代价要如实说**：交付从"一个 exe"变成"exe + 一个 52 KB 的 dll"。
这是插件化**无法回避**的代价。

### ② 必须挂程序集解析器

解析顺序：**宿主已加载 → 宿主目录 → 插件目录**。
第①步最关键：它保证插件拿到的是**宿主那份**契约，而不是自己带的一份。

### ③ 必须在 STA 线程上加载

WPF 的要求。主程序天然满足（UI 线程就是 STA），
但**任何后台线程里加载插件都会抛** `调用线程必须为 STA`。
加载器会主动检查并给出明确错误，而不是让它以看不懂的异常失败。

### ④ 宿主项目必须 `UseWPF=true`（这条最容易误诊）

否则 `PresentationFramework` 根本不进进程，插件里的 WPF 窗口一加载就报
**"找不到程序集 PresentationFramework"** —— 错误信息完全指不到"你没开 WPF"上去。

> 我在这条上花了半小时：先怀疑解析器写错了、再怀疑单文件发布的问题，
> 最后才发现是 POC 宿主的 csproj 里漏了 `UseWPF`。
> **教训：报"找不到 X"时，先确认 X 到底有没有被加载进进程，再怀疑查找逻辑。**

---

## 四、插件的生命周期

### 安装（工具管理窗口 → 安装 / 更新）

```
下载（多镜像 + SHA-256 校验）
  → 解压（只取 .dll，且防 Zip Slip）
  → 落到 plugins\<id>\
  → 热加载（ReloadPlugins）
  → 自动启用（写设置 + ApplyToolSwitches）
```

装完**默认启用** —— 用户点"安装"的意思就是要用它，
不自动开的话他还得再去设置里找一遍开关。

### 卸载

删掉 `plugins\<id>\`，并把开关关掉。

⚠️ **必须如实说明的限制**：已加载的程序集在 .NET 里**无法真正卸载**。
所以卸载后：文件没了（下次启动不再加载），但**本次运行期间它仍然可用**。
界面上写的是「重启工具箱后就彻底没有了」，而不是"已卸载"——
后者会让用户以为功能立刻消失，一看还在，就不再信任这个按钮。

---

## 五、安全

| 措施 | 说明 |
|---|---|
| **SHA-256 强制校验** | 下载后必须对得上发布者给的哈希，**不对就丢弃该源、换下一个** |
| **多镜像** | 直连 → ghproxy.net → gh-proxy.com → ghfast.top。用镜像不影响安全性：无论从哪来都要过哈希 |
| **Zip Slip 防护** | 解压时只取文件名、丢掉目录结构，并再次确认最终路径在插件目录内 |
| **只接受 .dll** | 插件包里的其它文件一律不落盘 |
| **契约版本校验** | 不兼容的插件**在实例化之前**就被拒绝，并说清原因 |
| **Id 冲突拒绝** | 插件不许顶掉内置工具，也不许互相顶 |
| **失败不拖垮启动** | 插件再坏也只是少一个工具，主程序照常起 |

> ⚠️ **只装你信任的插件**。插件是**能以你的身份运行的代码**，
> 哈希校验保证的是"你下到的和作者发布的一致"，
> **不是**"作者发布的是安全的"。这一点在界面上也写了。

---

## 六、做一个新插件

1. 在 `plugins-src\` 下新建工程（照抄 `WindowTopmost\` 那份 csproj）
2. 引用 `Toolbox.Contracts`，**`Private=false` + `ExcludeAssets=runtime`**
3. 写 `PluginManifest.cs`：`[assembly: ToolboxPlugin("id", "名称", Version=..., ContractsVersion="1.0")]`
4. 实现 `IToolboxTool`
5. 构建 → 打包成 zip → 发到 Release → 在 `tools.json` 里登记（含 SHA-256 与大小）

**关键**：业务源码用 `<Compile Include="..\..\src\Toolbox\Tools\Xxx\*.cs" Link="..."/>`
**链接**进来，而不是复制一份 —— 保证插件与主程序**永远同一份实现**，不会分叉。

---

## 七、还没做的（诚实清单）

| 项 | 状态 |
|---|---|
| 插件**更新提示** | 界面有"安装 / 更新"，但**不会主动告诉你"有新版本"**（没有版本对比） |
| 插件**自带第三方依赖** | 解析器会去插件目录找，但**没做过实测**（目前所有插件都只依赖契约） |
| 卸载后立即失效 | 做不到（.NET 限制：已加载的程序集无法卸载），需要重启 |
| 插件签名 | 只有 SHA-256 完整性校验，**没有代码签名**。哈希保证"和你下到的与作者发布的一致"，不保证"作者发布的是安全的" |

---

## 八、验证方式

```powershell
# 主程序自检（96 项，含插件机制 4 项）
dist\Toolbox.exe --selftest

# 插件装配可行性（独立工程，验证三条硬约束）
artifacts\probe-plugin\   # 三方 POC：宿主 + 契约 + 插件
```

**本轮实测记录**：

| 验证 | 结果 |
|---|---|
| 契约跨程序集类型匹配 | ✅ 插件回调宿主读到 `theme=dark` |
| 单文件自包含宿主 + WPF 插件 | ✅ 加载成功、窗口 Content 已加载 |
| 没装插件 | ✅ 11 个工具，功能不存在且无报错 |
| 装了插件 | ✅ 12 个工具，热键/开关/悬浮窗与内置一致 |
| 插件包下载（走镜像） | ✅ 4315 B，SHA-256 与清单一致 |
| 主程序自检 | ✅ **96/96** |
