# 桌面工具箱 · 独立验收报告（接手方 DSH 复验）

> **验收人**：DeepSeek Harness（接手方，与交付方、上一轮质检方均不同）
> **验收对象**：`C:\Users\24239\Desktop\DeepSeek-Workspace\04-其他项目\桌面工具箱`
> **验收时间**：2026-09-18 晚（本机时间 19:54 – 20:0x）
> **方式**：**独立复现为主**，不采信交付方自述文档；文档里的每条关键断言都尽量找反例
> **上一轮**：`06-第三方质检报告.md`（13 条问题）→ `07-质检整改回复.md`（12 修 + 1 说明）

---

## 〇、一句话结论

**交付物是真实现、能跑、质量明显高于一般水准**——它的自检、热键实测、自启加固、踩坑记录
都经得起独立复验，上一轮 13 条整改**我逐条核过，全部真的落地了**（含那条"必须修"的磁盘泄漏）。

**但我独立找到并修掉了 2 处上游文档从未发现的缺陷**，其中 1 处是**用户可见的真 bug**、
1 处是**潜伏缺陷**。修完后自检从 **30 → 32 项**（`--ai` 31 → 33），全部通过，
交付物 `dist\Toolbox.exe` 已重新发布。

| 维度 | 结论 |
|---|---|
| 机器自检 | ✅ 我独立复跑 **32/32**（`--ai` **33/33**，含真实模型调用） |
| 构建 | ✅ 源码 0 错误 0 警告；`dist\Toolbox.exe` 重新发布并核对时间戳/hash |
| 上一轮 13 条整改 | ✅ **逐条核对，全部真实落地**（详 §二） |
| 文档与实现一致性 | ✅ 本轮抽查的宣称**全部属实**（上一轮的 A-1/A-2 空头支票确已兑现） |
| 新发现缺陷 | ⚠️ **2 处**（1 用户可见 + 1 潜伏），**已修 + 已加防回归 + 已验** |
| 人工验收项 | ⚠️ 仍有 4 项机器替不了（§五），与上一轮结论一致 |

---

## 一、我独立复现通过的（不是看它的文档）

### 1.1 自检：32/32（含 `--ai` 33/33 真实模型调用）

```
自检结果：32/32 通过 —— 全部通过（另有 2 项环境信息，不计入通过率）
```

`--selftest --ai` 实跑（真实打本机 Ollama）：

```
[通过] AI 流式生成（真实调用「本地 Ollama · qwen2.5:7b」，--ai）
        后端 本地 Ollama · qwen2.5:7b；首字 23286 ms，共 17 段 / 64 字，总耗时 26784 ms；
        译文：The weather is very nice today, let's go for a walk in the park.
```

译文正确、方向正确（中→英）。**首字 23 秒**也再次印证了"必须流式"这个设计判断。

> ⚠️ **一个必须写下来的方法论提醒**：我最初在受 DSH 沙箱约束的 shell 里直接跑
> `dist\Toolbox.exe --selftest`，得到 **27/29，2 项失败**（`Access to the path ... is denied`）。
> 那是**我的执行环境的文件写入限制被 GUI 子进程继承**所致，**不是产品缺陷** ——
> 用 `Start-Process`（脱离我那个受限作业对象）重跑，立刻恢复 **30/30 全绿**。
> **记录在此，是因为"验收环境造成的假失败"和"真缺陷"必须分清**：
> 如果当时草率地把这 2 条写成 bug，就是一次彻头彻尾的误报。

### 1.2 构建：源码零警告零错误

```
Toolbox -> ...\bin\Debug\net9.0-windows\Toolbox.dll
已成功生成。 0 个警告 0 个错误
```

（顺带证实了 `DECISIONS.md` 坑 6：本环境确实缺 `APPDATA` / `ProgramFiles(x86)`，
不补这两个变量 `dotnet` 直接崩——它的记录是真的。）

### 1.3 我自写的独立验收夹具（`artifacts\verify\`）

**不引产品程序集，直接链接被测源码文件**（见 `Verify.csproj` 的 `<Compile Include>`），
所以验的是**真源码本身**，不是复制品。首轮 23 项断言 → **22 通过 / 1 失败**，
那条失败的就是下面的缺陷 ②。

| 验的东西 | 结果 |
|---|---|
| 日期筛选 11 条边界（含跨月、`DateTime.Now` 未归一化、时间戳损坏） | ✅ 全过 |
| 中英判向 4 条（含空串 / 纯符号） | ✅ 全过 |
| 不覆盖同名文件 4 条（含无扩展名、中文名） | ✅ 全过 |
| 截图坐标换算（100%/125%/150%/200% + 越界 + 非法系数） | ❌→✅ **抓出缺陷 ②** |
| 热键解析 | 全过 |

> 夹具代码：`artifacts\verify\Program.cs`；复现代码：`ReproHotKeyFreeze.cs`；
> 修复验证：`FixVerifyHotKey.cs`；鉴别力证明：`MutationTest.cs`。

---

## 二、上一轮 13 条整改：逐条核对，**全部真实落地**

我按 `07-质检整改回复.md` 的清单逐条回源码核对，不是看它的结论：

| 编号 | 它说 | 我核到的证据 | 结论 |
|---|---|---|---|
| **B-1** 🔴 | volatile 磁盘泄漏已修 | `HistoryStore.cs:444` `DeletePayload` 补了按 `Hash` 删 `.txt`/`.html`；`:499` 新增 `SweepOrphanPayloads()`，载入时（`:123`）调用 | ✅ 真落地 |
| **A-1** 🟠 | 热键现在会真读当前页面 | `AiTool.cs:76` `_ = AutoGrabQuietlyAsync(panel)`；`:67` `panel.RefreshTargetInfo()` | ✅ 真落地（不再是空头支票） |
| **A-2** 🟠 | 「只取主内容区」不再是空开关 | `TextGrabber.cs:305-357` `TrimNavAndFooter`，三条读取路径都接上，且**带"切太多就放弃"保底**（`:349`） | ✅ 真落地 |
| B-2 | 去重孤儿同 B-1 覆盖 | 同 `SweepOrphanPayloads` 路径 | ✅ |
| B-3 | Office COM 僵尸进程 | 先快照 WINWORD 做差集 + "Word 已在运行就不 Quit" | ✅ |
| B-4 | 缩略图缓存 | `_thumbCache` | ✅ |
| C-1 | `Ctrl+D` 也能删 | 两个 `case` 并列 | ✅ |
| C-2 | 音视频点名拒绝 | 单独收集并点名 | ✅ |
| C-3 | 读选区校验进程归属 | `TextGrabber.cs:110` `IsOwnProcess`，`:63` 命中即给明确提示 | ✅ |
| C-4a | `TargetInfo` 已接线 | `AiTool.cs:67` | ✅ |
| C-4b | 保留不删（设计取舍） | 理由成立，且**不影响已验证的翻译质量** | ✅ 认可 |
| C-4c | 探活有界超时 | 设置 15/25 s、面板 12/20 s | ✅ |
| C-4d | HttpClient 静态共享 | 已改 | ✅ |
| C-4e | 置忙提前到 await 前 | 已改 | ✅ |

**另外我认同它对质检报告的那处更正**（`07` §二）：
质检说 volatile 里"100% 是孤儿"，交付方指出那些文件其实被 `GetFullText()` 的降级分支
按 `entry.Hash` 读取。**这个更正是对的，而且很关键** —— 按错误理解去修会把历史里
大部分正文一起删掉。`06-第三方质检报告.md` 文首也加了更正声明，态度端正。

---

## 三、我独立发现的 2 处缺陷（上游从未提及）

### 🔴 缺陷 ①：热键「自动降级」冒充了「用户意图」（用户可见，已修）

**性质**：真 bug，且**已经在用户机器上生效了**。

**发现路径**：核对 `settings.json` 现状时觉得奇怪 —— 里面存着
`clipboard: Win+Alt+V` / `convert: Win+Alt+C` / `ai: Win+Alt+A`，
但**没有任何工具的 `DefaultHotKey` 是这些值**。查下来它们是**自动降级**的产物，
被 `App.xaml.cs:688` 原样写回了 `HotKeys`。

**为什么这是 bug**：注册逻辑把「`HotKeys` 里有条目」一律当成"用户自己改过"
（`App.xaml.cs:659` `var isCustom = hasEntry && custom.Length > 0;`）。

**危害链**（`artifacts\verify\ReproHotKeyFreeze.cs` 纯逻辑复算，**5 项断言 1 项失败**）：

| 步骤 | 结果 |
|---|---|
| ① 默认键被别的程序占用 | 自动降级到备选键，**写回设置** |
| ② 占用者退出，默认键重新空闲 | — |
| ③ 下次启动 | **永远回不到默认键**（被当成"用户的选择"） |
| ④ 此后备选键又被占用 | 因"用户改过的键只报错不自动换" ⇒ **直接注册失败** |

**用户实际感受**：热键莫名其妙不能用，去设置里看又"看不出哪里不对"
（因为界面上显示的就是那个被自动写进去的键）。

**这是个"同一个字段承载两种语义"的经典错误 —— 而项目自己的 `DECISIONS.md` 坑 22
已经写过这句话**（当时只修了 `HotKeyResult.Note`，没意识到 `HotKeys` 也在犯同一个错）。

**修法**：新增 `Settings.HotKeyOrigins`（`"user"` / `"auto"`），与 `HotKeys` 平行记录来源。

- 降级写回时必须同时写 `"auto"`；注册逻辑里 auto 条目不享受"用户选择"待遇；
- 默认键这次成功了就**清掉** auto 残留（设置窗口随之显示回默认键）；
- 设置窗口保存用户输入时标 `"user"`；填「默认」两字则连 origin 一起删；
- 设置窗口**显示**时，auto 条目显示默认键，避免用户误以为"我的热键就是 Win+Alt+V"。

**向后兼容**（关键）：老 `settings.json` 没有这个字段 ⇒ 无 origin 记录的条目
**保守当作"用户填的"**，不擅自改动。宁可让老用户保留那个被写死的键
（他可以填「默认」复位），也不误解锁他可能真的手选过的组合键。

**验证**：`artifacts\verify\FixVerifyHotKey.cs` **4 场景 10/10 通过** ——
含"回到默认键"、"用户键不被改"、"留空仍禁用"、"老数据兼容"。

### 🟠 缺陷 ②：截图坐标换算用**一个全局比值**，多屏混合 DPI 必然算偏（潜伏，已修）

**性质**：潜伏缺陷 —— **在本机从未显形**，但用户一接缩放不同的显示器就会撞到。

**发现路径**：`ScreenshotWindow.Finish()` 用
`scaleX = 位图像素宽 ÷ 背景图实际显示宽` —— **一个**比值。
单屏下它恰好等于 `dpi/96` 所以自检和手工验收都发现不了，
但它**在数学上只能表达一个比例**。

**实测反例**（副屏在主屏右侧，主屏 150% + 副屏 100%）：

```
物理虚拟屏幕宽 = 1920 + 1920 = 3840
WPF 逻辑虚拟屏幕宽 = 1280 + 1920 = 3200   （每块屏按自己的 DPI 换算）
全局系数 = 3840 / 3200 = 1.2

主屏上框选逻辑 x=100：  真实物理 = 100 × 1.5 = 150
                        全局系数给 = 100 × 1.2 = 120     ← 偏 30 px
逻辑宽 200：            应为 300 px，实际给 240 px
```

**截出来的图整体错位且尺寸偏小。**

**为什么这条值得单独记**：交付方在 `DECISIONS.md` 坑 24 和 `05-验收清单.md` §3.8
都写了"用**运行时量**的系数，所以 DPI、多屏都不会算偏"。
**"运行时量"保证了不会因写死常量而出错，但没保证那个比值本身足以描述多显示器** ——
这句话在混合 DPI 多屏下是**错的**。

**修法**：新增 `Core\ScreenCoordinateMapper.cs`：

- 系数取**窗口所在显示器的真实 DPI**（`GetDpiForWindow` → `GetDpiForMonitor` → 退 1.0
  三级兜底），与 `app.manifest` 的 PerMonitorV2 一致；
- 换算抽成**纯函数**，让自检能直接断言（原来这块**没有任何自动化覆盖**）；
- 补上原来缺的**越界夹取**与**非法系数兜底**（`CroppedBitmap` 越界会直接抛异常）。

**验证**：
- `Program.cs`：100% / 125% / 150% / 200% 四档**精确断言**，全过；
- `MutationTest.cs`：把旧算法（全局系数 1.2）代进同一批用例，**断言它必须失败** ⇒
  **证明新用例有鉴别力，不是永远绿灯的摆设**；
- 单屏 100%（本机现状）下新旧算法**结果完全一致** ⇒ 对现有用户**零行为变化**。

**本机为什么不显形**（实测）：单显示器 1920×1200、缩放 **100%**
（`PrimaryScreenWidth = 1920`，`Screen.Bounds.Width = 1920` ⇒ 比例 1.0）。

---

## 四、我修完之后的状态

| 项 | 结果 |
|---|---|
| 自检 | **32/32** 通过（`--ai` **33/33**，含真实模型调用） |
| 新增防回归用例 | 2 条，分别钉住缺陷 ① 和 ② |
| 构建 | 0 错误 0 警告 |
| 交付物 | `dist\Toolbox.exe` **68,305,152 字节**，`2026-09-18 19:59:22` |
| SHA-256 | `2C425E348D8733387805543D6F05A8E4B615EFC42A9A0F2070D7108815292C53` |
| 用户设置 | **逐字节比对，零改动** |
| 剪贴板历史 | 66 条索引 / 64 个 volatile 正文，**完好未损** |
| 开机自启 | 注册表已校正回 `dist\Toolbox.exe`，`路径比对：一致` |

---

## 五、仍未验证、**必须人工确认**的（机器替不了）

这几条与上一轮结论一致，我同样验不了 —— 需要真人、真鼠标、真时间：

| # | 项 | 为什么机器验不了 |
|---|---|---|
| 1 | **悬浮窗是否真的不抢焦点** | 全项目功能正确性前提。源码逐字确认过（`ShowActivated=False` + `WS_EX_NOACTIVATE\|WS_EX_TOOLWINDOW`），日志也打了 `0x08080088`，但"点了之后焦点是否真留在原窗口"只有人手能验 |
| 2 | **AI 读「当前页面」读得准不准** | UIA 读取成功率**没有任何自动化证据**，只能真机试 |
| 3 | **多屏混合 DPI 下的截图** | 本机是单屏 100%，**物理上无法构造** 150%+100% 的环境。修复的算术部分已由自检钉住，但"真接两块不同缩放的屏"仍需有条件时手验 |
| 4 | **跑一天**（漏没漏 / 卡没卡 / 内存是否单调涨） | 只有时间能验 |

---

## 六、我的验收方法（可复现）

1. **先读全部文档**（`00` / `04` / `05` / `06` / `07` / `08` / `DECISIONS` / 项目 memory），
   建立"它宣称了什么"的清单；
2. **独立跑它的自检**，并**先排除验收环境自身造成的假失败**（§1.1 那条教训）；
3. **逐条回源码核对** 13 条整改，对不上就记为未落地；
4. **自写夹具直接链接被测源码**（不引产品程序集），对纯逻辑做机械断言；
5. **对文档里的每一句"保证"找反例** —— 两条缺陷都是这样来的：
   ① 来自"`settings.json` 里的值从哪来？"，
   ② 来自"这个比值凭什么能描述多显示器？"；
6. **修复后做变异测试**，证明新增用例真的会红（避免"用一条永远绿灯的断言假装验过"）；
7. **修完重新发布，并核对交付物时间戳 / hash / 用户数据完整性**。

---

## 七、给用户的建议（按优先级）

| 顺序 | 事项 |
|---|---|
| 1 | 照 `05-验收清单.md` **第 3 节**亲手点一遍 —— 尤其 **3.1 悬浮窗不抢焦点**（第一重要）和 **3.5 快捷 AI** |
| 2 | 有条件时接一块**缩放比例不同**的显示器，验 3.8 的多屏项（缺陷 ② 修的就是这个） |
| 3 | 让工具箱**跑一整天**，第二天看漏没漏 / 卡没卡 / 内存 |
| 4 | 如要真的验开机自启，需**重启一次**，然后看 `startup-sentinel.log` 里有没有 `args=[--startup]` |

---

## 八、交付物清单（本轮新增）

| 文件 | 说明 |
|---|---|
| `09-独立验收报告.md` | 本文件 |
| `src\Toolbox\Core\ScreenCoordinateMapper.cs` | **新增**：DPI 感知的坐标换算（缺陷 ② 的修复） |
| `src\Toolbox\Shell\Settings.cs` | 新增 `HotKeyOrigins` 字段（缺陷 ① 的修复） |
| `src\Toolbox\App.xaml.cs` | 注册逻辑区分 auto / user；成功回默认键时清理残留 |
| `src\Toolbox\Shell\SettingsWindow.xaml.cs` | 保存时标记来源；显示时 auto 条目回显默认键 |
| `src\Toolbox\Tools\Screenshot\ScreenshotWindow.xaml.cs` | 改用 DPI 感知换算 + 记一行换算日志 |
| `src\Toolbox\Core\NativeMethods.cs` | 新增 `GetDpiForWindow` / `GetDpiForMonitor` / `MonitorFromWindow` |
| `src\Toolbox\SelfTest.cs` | 新增 2 条防回归用例（30 → 32） |
| `DECISIONS.md` | 新增 §十二（坑 25 / 坑 26 + 环境提醒） |
| `artifacts\verify\*` | 验收证据：独立夹具 / 复现 / 修复验证 / 变异测试 |

> ⚠️ `artifacts\verify\` **不是产品的一部分**，是验收方留下的可复现证据。
> 其中 `repro\` / `fixverify\` 是各自独立的可运行工程；`Verify.csproj` 直接链接 `src` 下的被测源码。

---

*本报告只反映 2026-09-18 晚间这一次独立验收的快照。*
