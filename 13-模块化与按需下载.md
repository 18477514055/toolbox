# 模块化 / 仓库 / 按需下载 —— 设计与现状

> 本轮（2026-09-18 第三批）做了四件事：**功能开关**、**工具模块化（按需下载通道）**、
> **GitHub 仓库**、**热键完全可选**。本文说明设计取舍与**当前能力边界**。

---

## 一、功能开关（总开关 + 每工具开关）

### 用户要的

> 「设置界面中有一个总开关，然后各类功能旁边都有一个开关，
> 这决定了悬浮窗上会显示哪些功能以及哪些功能将会运行。」

### 实现

| 项 | 说明 |
|---|---|
| 总开关 | `Settings.ToolsEnabled`。关掉后**所有**功能停止（不注册热键、不监听剪贴板、悬浮窗/托盘不显示入口），**但程序还在**，随时可重开 |
| 每工具开关 | `Settings.ToolEnabled` 字典 |
| 判定 | **全部走 `Shell\ToolGate.cs`** —— 这是"某工具是否启用"的唯一判据 |
| 生效 | **保存后立即生效**，不用重启 |
| 默认 | 常用 5 个开、其余 7 个关（用户选定） |

### 三条关键设计（都影响正确性）

**① 关掉的工具是"真的停"，不是"藏起来"**

`ToolRegistry.StartAll` 对关闭的工具**根本不调用 `Start()`**。
差别很重要：`Start()` 里做的是真事（注册剪贴板监听、订阅热键、挂事件回调）。
如果只是不显示按钮却照样 Start，那关掉的工具**仍在后台跑、仍占资源、仍响应热键**。

同理，`ApplyToolSwitches`（保存后应用）会**主动 `Stop()`** 被关闭的工具 ——
只做"启动新开的"、不"停掉新关的"，用户会发现"关了还在抓我的剪贴板"。

**② 总开关优先，且不覆盖单个选择**

总开关关掉时，`IsGateEnabled` 直接返回 false，**无视**单个开关。
理由：总开关的用途就是"一键安静"，如果它还要尊重每个工具的开关，
用户关掉后会发现"怎么还有几个在跑"，反倒要挨个关 —— 那就失去意义了。

但**不清空**单个选择：重新打开总开关后，用户原来的选择照常恢复。

**③ 只存"和默认不一样"的选择**

与默认一致 → 从字典移除；不一致 → 显式存 true/false。
这样以后调整默认策略时，**没表过态的用户会自动跟随新默认**，表过态的保持不动。

### 为什么要集中到一个 `ToolGate`

同时有 4 个地方要问"这个工具启用了吗"：悬浮窗（显示哪些按钮）、热键（注册哪些键）、
托盘菜单（列哪些工具）、`InvokeTool`（能不能调用）。
各写一份 `if (settings.XxxEnabled)` 迟早有一处漏改，
症状是"界面上看得到、点了没反应"或反过来 —— 极难查。

---

## 二、热键完全可选

### 用户要的

> 「快捷键不再强制要求留下点什么键，可以没有任何快捷键，
> 因为点击右侧弹窗同样很方便。」

### 现状

**这个能力其实一直都有**（留空 = 那个工具不占热键），本轮做的是**让它更好用**：

- 设置里明确写出「**热键是可选的，一个都不填也完全没问题**」
- 新增 **「清空全部热键」** 按钮（一键把 12 个框全清空）
- 新增 **「全部恢复默认」** 按钮
- 留空与"没配过"**分开存**（空串 vs 条目不存在），所以"禁用"不会被悄悄退回成"用默认键"

实测：把全部热键设为空串 → 程序照常启动、所有功能可用、日志只有中性的
`热键已禁用（用户设置为空）`，**不进 `[错误]`、不弹窗**。

---

## 三、GitHub 仓库

**https://github.com/18477514055/toolbox**（公开）

| 项 | 值 |
|---|---|
| 内容 | 226 个文件 / 1.8 MB（纯源码 + 文档，**不含任何构建产物**） |
| Release | `v1.0` —— `Toolbox-v1.0.zip`（68.8 MB，含 exe + 图标 + 说明 + 许可证） |
| 许可证 | MIT |

**为什么 exe 走 Release 而不是入库**：71 MB 的二进制会让仓库变得很重
（clone 慢、diff 无意义）。Release 附件有独立的下载 URL，正好也是"按需下载"要用的形态。

---

## 四、工具模块化 / 按需下载

### ⚠️ 先说清楚当前状态（别误解）

用户的两个需求是本轮**分开推进**的（已与用户确认按 **C 方案**：先 A 后 B）：

| 阶段 | 内容 | 状态 |
|---|---|---|
| **A（本轮）** | 功能开关体系 + **仓库与下载通道全链路打通并验证** | ✅ **已完成** |
| **B（下一轮）** | 把工具**真正拆成独立包**，核心只带剪贴板，其余从仓库拉 | ⏳ 未做 |

**所以现在**：12 个工具**都还在主程序里**，"工具管理"窗口的作用是
"让你看到清单、检查仓库连通性、以及（为下一轮准备的）下载能力"。
它**不会**让你下载到"主程序里没有的工具"——因为现在还没有那种工具。

**为什么先做 A 再做 B**：拆包是架构级重构，而网络通道是它的前置条件。
先把通道打通并验证（多镜像、哈希校验、离线回退），
下一轮就只需面对"拆包"一个问题 —— 否则两个问题混在一起很难定位。

### A 阶段实际做了什么

**`Tools\Store\ToolStore.cs`**

| 能力 | 说明 |
|---|---|
| 清单模型 | `tools.json`：工具 Id / 名称 / 说明 / 版本 / 资产名 / **SHA-256** / 大小 / 依赖说明 |
| **多镜像源** | 直连优先，然后依次 `ghproxy.net` → `gh-proxy.com` → `ghfast.top`；**全失败才报错** |
| **SHA-256 强制校验** | 下载后必须对得上发布者的哈希，**不对就丢弃该源、换下一个** |
| 断点友好 | 先下到 `.downloading`，校验通过才改名到正式路径（不会留下半个文件） |
| **离线回退** | 拉不到清单时用**内置清单**（随主程序打包）⇒ 界面不会是空白+报错 |

### 为什么镜像也要校验哈希

这是"用第三方镜像安全吗"的答案：**无论从哪个源下载，都必须对得上发布者的 SHA-256**。
镜像只影响"能不能下到"，不影响"下到的是不是对的"。
校验不过就丢弃并换源 —— 篡改过的内容根本进不来。

### 实测证据（`artifacts\probe-store\`）

```
① 内置清单           : 12 条目，Id 无重复
② 从仓库拉清单       : ✅ 成功（来自 GitHub 直连），12 条目
③ 下载 Release 附件  : ✅ 成功，68,832,295 字节，SHA-256 复核一致
④ 变异测试（错哈希） : ✅ 正确拒绝，且不留垃圾文件
```

国内镜像实测（三个都通）：
```
ghproxy.net   → 200
gh-proxy.com  → 200
ghfast.top    → 200
```

---

## 五、国内网络的一个实测坑（值得记）

本机实测：**`github.com` 与 `raw.githubusercontent.com` 会超时**，
而 `api.github.com` / `codeload.github.com` 正常。
⇒ 这正是"必须留国内镜像"的现实依据，不是过度设计。

（有意思的是 `git ls-remote` 反而通 —— 说明不同域名的可达性可以完全不同，
**不能因为"git 能推"就认为"网页/raw 能拉"**。）

---

## 六、下一轮（B 阶段）计划

1. 把每个工具拆成独立程序集（`Toolbox.Tools.Xxx.dll`）
2. 主程序只带核心（剪贴板）+ 插件加载器
3. 每个工具单独发 Release 资产，`tools.json` 填上 `AssetName` / `Sha256` / `Size`
4. 工具管理窗口的「下载」按钮即可真正安装工具

**已经铺好的路**（下一轮直接用，不用再改）：
- 清单格式与解析（`ToolPackage` 已有 `AssetName` / `Sha256` / `Size` 字段）
- 多镜像下载 + 哈希校验（已验证）
- 离线回退
- UI 里的下载按钮与进度条（`ToolStoreWindow`）
- `ToolGate` 开关体系（插件加载后照样受它管）
