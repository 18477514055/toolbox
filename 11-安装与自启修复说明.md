# 桌面工具箱 · 安装 / 启动 / 自启修复说明

> 处理时间：2026-09-18 晚
> 起因：用户反馈「开机启动失效」，并要求做一个桌面图标方便快捷启动。

---

## 一、先回答：开机自启到底为什么"失效"

### 结论：**程序的自启逻辑没坏，是 exe 放的位置太脆弱。**

我逐层查了，证据如下：

| 检查项 | 结果 |
|---|---|
| 注册表 `Run` 键里有条目吗 | ✅ 有：`"...\桌面工具箱\dist\Toolbox.exe" --startup` |
| 被任务管理器禁用了吗（`StartupApproved`） | ❌ 没有记录，**不是被禁用** |
| 被组策略拦了吗 | ❌ 无 `Disable` 策略 |
| 启动文件夹里有冲突快捷方式吗 | ❌ 没有 |
| 自启代码本身有 bug 吗 | ❌ 没有，代码是对的 |
| **注册表里那条命令实测能跑起来吗** | ✅ **能**（我用 `System32` 当工作目录模拟开机环境，成功启动） |

**真正的根因**——看启动哨兵 `startup-sentinel.log` 就一目了然：

```
2026-09-17 15:20:57	pid=20484	args=[--startup]	cwd=...\桌面工具箱
2026-09-17 15:22:21	pid=24356	args=[]          	cwd=...\桌面工具箱
...（后面 20 多条全是 args=[] 或 --settings / --open，没有一条 --startup）
2026-09-18 20:02:28	pid=32780	args=[]          	cwd=...\桌面工具箱
```

**整个日志里只有 1 条 `--startup`，而且是 9-17 15:20 开发时手工测的那一条。**
而这台机器**上次开机是 9-17 15:10，已经连续运行 29 小时没重启过**。

> ⇒ **它其实从来没有被真正开机触发过。** 你感觉"自启失效"，是因为那唯一一次真实机会（9-17 15:10 那次开机）之前，程序还没配置好；之后一直没再重启，所以自启一次都没机会表现。

**但位置脆弱这件事是真的隐患**，必须修：

- exe 原位置是 `C:\Users\24239\Desktop\DeepSeek-Workspace\04-其他项目\桌面工具箱\dist\Toolbox.exe`
  —— 一个**桌面工作区里很深的嵌套路径**，还带中文。
- `Run` 键存的是**绝对路径**。工作区一旦被清理、移动、改名（比如你整理项目目录），
  **自启就静默死亡**——而且因为进程根本没起来，主日志里连一行都不会有，你只会看到"没启动"。
- 这正是 `DECISIONS.md` **坑 13** 描述的失败模式（历史上真的发生过一次：
  注册表曾指向 Debug 构建，看起来能跑，实际指向的是会被 `dotnet clean` 清掉的目录）。

### 已做的修复

1. **把 exe 装到稳定位置**：`%LOCALAPPDATA%\桌面工具箱\Toolbox.exe`
   （数据目录本来就在这儿，路径稳定、不会被工作区清理影响、无需管理员权限）
2. **自启指向新位置**，并让程序自己确认过：`路径比对：一致 —— 自启会启动当前这个 exe`
3. **新增强力兜底**：程序**每次启动都会校正注册表路径**（`EnsurePointsAtCurrentExe`），
   所以即使以后你把 exe 挪了地方，只要它被启动过哪怕一次，自启就会自动跟过去。

---

## 二、安装结果（都已验证）

### 文件位置

```
C:\Users\24239\AppData\Local\桌面工具箱\
├─ Toolbox.exe      ← 程序本体（68,367,429 字节，已内嵌图标）
├─ Toolbox.ico      ← 图标（多分辨率 16~256）
├─ clipboard\       ← 剪贴板历史（原样保留，未动）
├─ logs\            ← 运行日志
├─ settings.json    ← 你的设置（原样保留，未动）
└─ startup-sentinel.log
```

把上面这个路径直接粘进资源管理器地址栏即可打开。

### 桌面快捷方式

| 快捷方式 | 位置 | 作用 |
|---|---|---|
| **桌面工具箱** | `C:\Users\24239\Desktop\桌面工具箱.lnk` | 主入口，双击启动（已在运行则唤起） |
| 快捷剪贴板 | `...\Desktop\桌面工具箱-直达\快捷剪贴板.lnk` | 直达该工具（等价热键 `Win+Alt+V`） |
| 图片裁剪 | `...\Desktop\桌面工具箱-直达\图片裁剪.lnk` | 等价 `Win+Shift+X` |
| 格式转换 | `...\Desktop\桌面工具箱-直达\格式转换.lnk` | 等价 `Win+Alt+C` |
| 快捷 AI | `...\Desktop\桌面工具箱-直达\快捷 AI.lnk` | 等价 `Win+Alt+A` |
| 快捷截图 | `...\Desktop\桌面工具箱-直达\快捷截图.lnk` | 等价 `Win+Alt+S` |
| 设置 | `...\Desktop\桌面工具箱-直达\设置.lnk` | 直接打开设置窗口 |

> 「桌面工具箱-直达」是桌面上一个文件夹，里面放 6 个直达入口。
> 你不用它们也可以——热键和悬浮窗才是主用法；这些是给"记不住热键"时用的。

### 验证过的三件事

| 测试 | 结果 |
|---|---|
| 双击桌面快捷方式 | ✅ 启动成功（272 MB 常驻） |
| 双击「直达\快捷 AI」 | ✅ 启动并直接打开 AI 面板 |
| 模拟开机自启（`--startup`，`System32` 工作目录） | ✅ 启动成功，哨兵留痕 `args=[--startup]`，日志零错误 |
| 从安装位置跑完整自检 | ✅ **32/32 通过** |

---

## 三、关于图标

图标是**按程序的视觉语言画的**，不是随便找的图：

- 外框深灰圆角方块 = 工具箱外壳
- 上方蓝色提手 = 工具箱把手
- 里面 **2×2 四个色块** = "一个外壳挂多个小工具"，四个颜色对应四个工具
- 配色取自程序自己的 UI 色板（主色 `#2970D1`，就是截图选区描边那个蓝）

**技术细节**：

- 一份 `.ico` 内含 **7 种尺寸**（16 / 24 / 32 / 48 / 64 / 128 / 256），
  Windows 会按场景自动挑（任务栏用 16、桌面用 32/48、大图标用 256）——
  只放一个大图会让小尺寸下糊成一团。
- 每帧都是 **PNG 压缩**（256×256 必须是 PNG 才规范）。
- 图标已**嵌进 exe 本身**（`<ApplicationIcon>`），所以：
  - 资源管理器里 `Toolbox.exe` 直接显示它；
  - 快捷方式即使不指定图标路径也能取到；
  - 拷给别人时**只拷一个 exe 就有图标**。
- 生成脚本留在 `artifacts\icon\MakeIcon.cs`，想改样式重跑即可。

> 已调用 `SHChangeNotify` 刷新图标缓存。如果桌面仍显示旧图标，
> 按 `F5` 刷新一下，或注销重登一次。

---

## 四、怎么确认自启真的生效了（下次开机后）

**唯一可靠的判据**是启动哨兵文件。重启后打开：

```
C:\Users\24239\AppData\Local\桌面工具箱\startup-sentinel.log
```

看**最后一行**有没有：

```
2026-09-19 08:xx:xx.xxx	pid=xxxxx	args=[--startup]	cwd=C:\Windows\System32
```

- **有 `args=[--startup]`** ⇒ 自启成功，进程确实被拉起来了。
- **没有这一行** ⇒ 自启没生效，再看下面两条。

**为什么必须看这个文件**：主日志（`logs\toolbox.log`）要程序启动到一定阶段才存在。
而"启动失败"恰恰可能发生在它之前——exe 根本没被拉起来。
那种情况下主日志一片空白，看起来像"程序从没启动过"。哨兵是**独立于主日志**的
append-only 记录，每次启动尝试都在最早期留一行，所以它是唯一可靠的答案。

**也可以随时用命令行只读查状态**（无副作用）：

```
"%LOCALAPPDATA%\桌面工具箱\Toolbox.exe" --autostart status
```

它会打印注册表里**实际那条命令**并做路径比对，应显示
`路径比对：一致 —— 自启会启动当前这个 exe`。

---

## 五、还能怎么启动

| 方式 | 操作 |
|---|---|
| **桌面图标** | 双击 `桌面工具箱` |
| **热键** | 直接按各工具热键（见 `10-已实现功能总清单.md` 总览表） |
| **悬浮窗** | 点桌面右边缘那条竖排小图标 |
| **托盘** | 右键托盘图标 → 选工具 |
| **命令行** | `Toolbox.exe --open clipboard` / `--settings` / `--autostart status` |

---

## 六、这次改动碰了哪些文件

| 文件 | 改动 |
|---|---|
| `%LOCALAPPDATA%\桌面工具箱\Toolbox.exe` | **新增**（安装的程序本体） |
| `%LOCALAPPDATA%\桌面工具箱\Toolbox.ico` | **新增**（图标） |
| `C:\Users\24239\Desktop\桌面工具箱.lnk` | **新增**（桌面快捷方式） |
| `C:\Users\24239\Desktop\桌面工具箱-直达\` | **新增**（6 个直达入口） |
| `HKCU\...\CurrentVersion\Run\桌面工具箱` | **改**：指向新的稳定位置 |
| `src\Toolbox\Assets\toolbox.ico` | **新增**（图标源文件） |
| `src\Toolbox\Toolbox.csproj` | **改**：加 `<ApplicationIcon>` |
| `artifacts\icon\MakeIcon.cs` | **新增**（图标生成脚本） |

**没有动的**：你的 `settings.json`（逐字节一致）、剪贴板历史（66 条完好）、
其余所有源码逻辑。

---

*相关问题请连同「哪一项 + 什么现象」一起反馈。*
