# 桌面工具箱 · 能不能分享给别人？—— 依赖说明与实测结论

> 问题：**能不能让它不依赖我电脑上的东西？要么一起打包，要么用别人电脑天生就有的。**
>
> 结论：**能，而且大部分早就做到了。** 本轮补齐了最后一环（便携模式），
> 并**实测修掉一个由此暴露出来的真 bug**。全部结论都有实测证据，不是读文档得出的。

---

## 一、直接回答：依赖分三层，只有第三层是真的

| 层 | 内容 | 谁提供 | 对方要做什么 |
|---|---|---|---|
| **① 运行时** | .NET 9 + WPF/WinForms | **打进 exe 里** ✅ | **什么都不用装** |
| **② 系统组件** | `user32` / `gdi32` / `shcore` / `advapi32` / `UIAutomationCore` 等 | **Windows 天生就有** ✅ | **什么都不用装** |
| **③ 可选增强** | LibreOffice、Ollama、Office | 对方自己决定 | **不装也能用，只是少一个格子** |

**没有任何第三方库**：全库 `using` 只有 .NET 和 Windows 命名空间；
`csproj` 里 **零 `PackageReference`**；`project.assets.json` 解析出的依赖只有
`Microsoft.NET.ILLink.Tasks`（SDK 自带的裁剪工具，不参与运行时）。

### ① 运行时是真打进去了（实测证据）

自包含 exe 的托管头里会嵌 runtimeconfig。实测读取本机 exe：

```json
"runtimeOptions": {
  "tfm": "net9.0",
  "includedFrameworks": [
    { "name": "Microsoft.NETCore.App",        "version": "9.0.19" },
    { "name": "Microsoft.WindowsDesktop.App", "version": "9.0.19" }
  ]
}
```

`includedFrameworks` 的意思是**运行时随 exe 一起分发**，不是"去系统里找"。

**更强的证据**——把 `dotnet` 从 PATH 里彻底拿掉再跑：

```
削后 PATH = C:\Windows\System32;C:\Windows
dotnet 还能找到吗: 找不到 ✅ 已隔离
--- 测试：自检（--selftest）在隔离环境下 ---
exit=0
结果: 全部通过  34/34
```

**连 .NET 都够不着，程序照样满血运行 34 项全过。**

---

## 二、第 ③ 层：不装会怎样？会不会整个程序废掉？

**不会。这是本项目从一开始就刻意设计的**（`IConverter.IsAvailable(out reason)`
是接口的一部分，界面上**先探测、再决定给你哪些格式可选**，不让你点了才报错）。

实测（我把产品源码的能力探测逻辑单独跑了一遍）：

```
--- 1. 格式支持矩阵 ---
        a.png  可选目标: png, jpg, bmp, tiff, gif, pdf
        a.docx 可选目标: pdf, docx, txt, html, odt
[通过] 图片有可选目标（不依赖任何外部程序，纯内置）
[通过] docx 有可选目标（需要 LibreOffice 或 Office）

--- 2. 依赖探测器在「缺依赖」时不抛异常 ---
[通过] OfficeComConverter.IsAvailable 不抛异常（缺 Office 也只是返回 false）
[通过] ImageConverter 永远可用（零依赖，不依赖任何外部程序）

--- 3. CanConvert 对任意输入都不抛异常 ---
        CanConvert("a.mp4","mp3") → 图片=False, Office=False
        CanConvert("","pdf")      → 图片=False, Office=False
        CanConvert("a.xyz","pdf") → 图片=False, Office=False
[通过] CanConvert 对空串 / 未知扩展名 / 音视频都不抛异常

结果：11/11 通过
```

**缺依赖的表现是"那个格子灰掉并写明缺什么"，不是崩溃、不是异常堆栈、更不是打不开。**

| 功能 | 依赖 | 对方不装会怎样 |
|---|---|---|
| 快捷剪贴板 | 无 | 正常 |
| 图片裁剪 | 无 | 正常 |
| **图片**格式转换 | 无（WPF 内置编解码器） | 正常 |
| 快捷截图 | 无（GDI `BitBlt`） | 正常 |
| **文档/PDF** 转换 | LibreOffice **或** Office | 该格子不可用，**图片转换完全不受影响** |
| 快捷 AI（**远端**后端） | **无**（只要有网） | 正常 |
| 快捷 AI（本地后端） | Ollama + 模型 | 换成远端后端即可，或不用 AI |

> **AI 这一条是本轮重点验证的**：我实测把后端切到「远端 API」（地址故意不带 `/v1`），
> 跑真实调用 —— **33/33 通过**，译文完全正确。
> 也就是说**对方只要有网 + 一个 Key，零安装就能用 AI**。
> 这也正是当初把 AI 抽成 `IAiClient` 双后端的原因。

---

## 三、本轮补齐的最后一环：**便携模式**

### 为什么需要它

前面的依赖分析都通过了，但有个**体验层面的问题**：
把工具箱拷给同事，他多半**不想让一个绿色小工具往自己 `%LOCALAPPDATA%` 里塞东西**
—— 那是"安装"，不是"试用"。他想要的是"不想要了整个文件夹删掉"。

### 现在的行为

数据放哪由**两个**条件决定：

| 情况 | 数据位置 |
|---|---|
| 默认（无标记、无参数） | `%LOCALAPPDATA%\桌面工具箱\`（常规，程序与数据分离） |
| exe 旁边有 **`portable.txt`** | **exe 同级的 `data\`**（便携，双击即生效） |
| 加 `--portable` 参数 | 同上（便携） |

### 实测三种场景

```
=== 场景A：--portable，全新环境 ===
  exit=0
  全部通过  34/34
  [数据目录模式] 常规→C:\Users\...\AppData\Local\桌面工具箱；便携→exe 旁边的 data\

=== 场景B：放 portable.txt，双击即便携 ===
  exit=0
  data\ 自动出现: True
  全部通过  34/34

=== 场景C：常规模式（无标记）===
  exit=0
  exe 旁边没有 data\: True
```

---

## 四、★ 便携模式暴露出来的一个真 bug（已修）

**这是本轮最有价值的发现 —— 它一直存在，但只在"全新环境"下才显形。**

### 现象

便携模式下跑自检，`volatile 孤儿回收` 那条**红了**：

```
[失败] volatile 孤儿正文文件在载入时被回收（B-1 防回归）
       孤儿文件还在 —— 磁盘泄漏会随使用时间无限增长
```

### 根因（`HistoryStore.Load()`）

```csharp
var path = AppPaths.IndexFile;
if (!File.Exists(path))
{
    Log.Line("剪贴板历史为空…");
    return;              // ← 这就是 bug
}
...
SweepOrphanPayloads();   // ← 写在 try 之后，被上面那个 return 跳过了
```

**`return` 把写在后面的收尾逻辑一起跳过了。**
而"索引文件不存在"恰恰就是**全新安装**（尤其便携模式首次运行）的状态 ——
也就是说，这个自愈机制**恰好在最需要它的场景里被关掉了**。

### 为什么以前没发现

原来那条用例跑在**已有历史**的环境里（`index.jsonl` 存在），走的是另一条分支，
所以一直是绿的。**换个环境（便携模式、空目录）立刻暴露。**
—— 这也说明"只在自己机器上测"有多容易漏掉东西。

### 修法

去掉早退，让唯一的那次 `SweepOrphanPayloads()` 在**所有**路径上都执行。
并且补了一条**专门针对这个 bug** 的回归用例（构造"索引文件不存在"的环境，
断言孤儿仍被回收），用例数 32 → **34**。

---

## 五、分享给别人：完整操作步骤

### 你要做的（一次）

把**整个 `share\` 文件夹**打包发过去即可：

```
C:\Users\24239\Desktop\DeepSeek-Workspace\04-其他项目\桌面工具箱\share\
├─ 桌面工具箱.exe     ← 程序本体（65 MB，运行时已内含，图标已内嵌）
├─ toolbox.ico        ← 图标（可选，exe 已内嵌一份）
└─ portable.txt       ← 有它 = 便携模式，数据留在文件夹里
```

**这三样就是全部**，压缩后发微信/网盘都行。

### 对方要做的（零）

1. 解压到任意文件夹（**别放 C:\Program Files**，那里要管理员权限）；
2. 双击 `桌面工具箱.exe`。

**就这样。** 不需要装 .NET、不需要装任何运行库。

### 对方能立刻用的

- ✅ 快捷剪贴板、图片裁剪、**图片**格式转换、快捷截图 —— **全部零依赖，开箱即用**；
- ✅ AI（只要他有任意 OpenAI 兼容的 Key）→ 设置里选「远端 API」，填地址+Key，零安装。

### 对方需要额外装才有的事（可选，不做也不影响）

- 📄 **文档/PDF 转换** → 装 LibreOffice（免费开源）或 Microsoft Office；
- 🔒 **完全本地的 AI**（数据不出机器）→ 装 Ollama + 拉模型（约 4.7 GB）。

### 卸载

**直接删掉那个文件夹。** 便携模式下数据全在 `data\` 里，不会有任何残留。

---

## 六、给你的建议（三选一）

| 方案 | 怎么做 | 适合 |
|---|---|---|
| **A. 直接发 `share\` 文件夹**（推荐） | 打包压缩发过去 | 对方只想试用 |
| **B. 只要一个 exe** | 只发 `桌面工具箱.exe` | 对方不在乎数据落在自己 `%LOCALAPPDATA%` |
| **C. 给对方装成正式应用** | 让他自己把 exe 放到一个固定目录，设置里勾「开机自启」 | 对方要长期用 |

> **A 和 B 的区别只有一个**：有 `portable.txt` 时数据在文件夹里（删文件夹=卸载）；
> 没有时数据在 `%LOCALAPPDATA%\桌面工具箱`（和你的本机一致）。

---

## 七、本轮改了哪些代码

| 文件 | 改动 |
|---|---|
| `Core\AppPaths.cs` | `Root` 由 `static { get; }` 改为可初始化；新增 `InitializeMode()` / `IsPortable`；支持 `portable.txt` 标记与 `--portable` |
| `App.xaml.cs` | **启动第一行**调用 `InitializeMode`（必须早于任何 AppPaths 访问）；日志里标明便携模式 |
| `Tools\Clipboard\HistoryStore.cs` | **修 bug**：索引文件不存在时不再早退，保证 `SweepOrphanPayloads()` 一定执行 |
| `SelfTest.cs` | 新增 2 条用例（便携模式判定 + 索引缺失时清扫孤儿）→ **32 → 34 项** |

---

## 八、验证汇总

| 测试 | 结果 |
|---|---|
| 常规模式自检 | ✅ **34/34** |
| 便携模式自检（`--portable`） | ✅ **34/34** |
| 便携模式自检（`portable.txt`） | ✅ **34/34** |
| 模拟"别人电脑"（削掉 dotnet + 空目录） | ✅ **34/34**，主程序常驻运行正常 |
| 缺依赖降级（LibreOffice/Office 不在） | ✅ 11/11，只降级不崩 |
| 远端 AI 后端真实调用 | ✅ 33/33，译文正确 |
| 便携模式未污染本机 `%LOCALAPPDATA%` | ✅ 已核对时间戳未变 |

---

*所有结论均为实测。复核命令：`dist\Toolbox.exe --selftest`（预期 34/34）。*
