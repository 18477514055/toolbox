# 快捷剪贴板 · P1 设计稿（还没动手，等 P0 真实使用结论）

> 状态：**设计**。P0 探针已在真机上验证抓取链路（自检 3/3 + 活体验证），本文件定 P1 的做法。
> 前置结论全部来自 `DECISIONS.md`，那里有证据。

---

## 一、P1 的目标（只做这些，不做别的）

一个托盘常驻的小窗口，按热键唤出，能搜历史、能把选中的内容放回剪贴板。

**验收标准**（一条条能勾）：

1. 托盘图标常驻，右键有「打开历史 / 暂停监听 / 退出」。
2. `Win+Shift+V` 唤出面板（再按一次或按 Esc 收起）。
3. 面板里能按关键词过滤历史（文本内容、文件名都参与匹配）。
4. 列表分三类显示：文本 / 图片（带缩略图）/ 文件（带图标或扩展名）。
5. 选中后按回车 → 内容回到剪贴板，面板收起，提示"已复制，Ctrl+V 粘贴"。
6. 重启程序后历史还在。
7. 全程不吃 CPU：空闲时 0% 占用。

**明确不做**（留给 P2/P3）：自动粘贴、置顶/删除、类型筛选、开机自启、设置界面。

---

## 二、工程结构

```
04-其他项目\桌面工具箱\
├─ 00-先看我.md
├─ DECISIONS.md
├─ 03-快捷剪贴板设计.md          ← 本文件
├─ run-probe.cmd                 P0 探针启动器
├─ src\
│  ├─ ClipboardProbe\            P0 探针（保留，回归测试用）
│  └─ Toolbox\                   ★ P1 主程序
│     ├─ Toolbox.csproj          net9.0-windows, UseWPF + UseWindowsForms
│     ├─ App.xaml / App.xaml.cs  应用生命周期、单实例、托盘
│     ├─ Shell\
│     │  ├─ TrayIcon.cs          NotifyIcon 封装
│     │  ├─ HotKeyManager.cs     RegisterHotKey + WM_HOTKEY
│     │  └─ ToolRegistry.cs      ★ 工具注册机制（见 §四）
│     ├─ Core\
│     │  ├─ ClipboardListener.cs 从探针搬过来（已验证的实现，别重写）
│     │  ├─ ClipboardReader.cs   从探针搬过来（含读取顺序与重试）
│     │  └─ ClipboardWriter.cs   写回剪贴板（带重试 + 自污染标记）
│     └─ Tools\Clipboard\
│        ├─ ClipboardTool.cs     实现 IToolboxTool
│        ├─ HistoryStore.cs      JSONL + blobs（从 ProbeStore 演进）
│        ├─ HistoryPanel.xaml    面板界面
│        └─ HistoryViewModel.cs  MVVM，搜索与列表
└─ artifacts\                    运行产出（探针与主程序分目录）
```

**关键：`ClipboardListener` / `ClipboardReader` 从探针**原样搬过来**，不要重写。** 那两个文件里的每个分支都是踩坑换来的（见 `DECISIONS.md` §二）。

---

## 三、数据模型（在探针格式上扩展）

P0 的 JSONL 字段已经够用，P1 增加四个：

| 新增字段 | 用途 |
|---|---|
| `Pinned` (bool) | 置顶，永不淘汰 |
| `LastUsedAt` | 复用时间，用于排序（同一内容再复制一次就顶到最前） |
| `Thumb` (string?) | 缩略图相对路径（列表用它，避免加载原图） |
| `Origin` (string) | `user` = 用户自己复制的；`tool` = 工具箱自己写的。**见下方"通道与档案分离"** |

其余沿用：`Seq` / `Time` / `Kind` / `Formats` / `Hash` / `Bytes` / `Files` / `ImageWidth` / `ImageHeight` / `TextPreview` / `TextLength`。

**存储布局**：

```
%LOCALAPPDATA%\桌面工具箱\clipboard\
├─ index.jsonl            追加写，启动时全量读进内存
├─ blobs\<hash>.png       图片原图（按内容哈希命名 → 天然去重）
├─ thumbs\<hash>.png      缩略图（长边 ≤ 320px）
└─ volatile\<hash>.txt    超长内容的正文（不在历史里留全量，见下）
```

> 为什么放 `%LOCALAPPDATA%` 而不是工作区：这是**运行时数据**，不属于项目源码。工作区里的 `artifacts\` 只放测试产出。

---

## 三·五、两条必须在这里就做掉的设计（用户明确要求）

### 3.5.1 剪刀原则：剪贴板是「通道」，历史是「档案」

> 用户原话："如果浏览器把整页文本放进剪贴板的话，那剪贴板会被污染啊。我要用的东西都被挤到下面去。"

**结论**：档案里只应出现**用户自己有意复制的东西**。工具箱内部为了搬数据而写的剪贴板内容，一律**不归档**。

实现（两条一起做才可靠）：

1. **写入时打标记**：`ClipboardWriter` 写剪贴板前，把内容哈希记入「自己写的」集合（带时间窗，比如 5 秒内有效）；命中比对 `Origin = tool`，**跳过 Append**。
   - 同一进程内完全可控——**不要去尝试识别"来源进程"**（`GetClipboardOwner` 常常返回 0，第三方进程不可信）。
2. **`Origin = tool` 的条目默认不显示在列表里**（设置里可开"显示工具箱写入记录"用于排错）。

### 3.5.2 超长内容不进档案

复制一整篇文章、复制一个巨大文件夹时，历史里不该出现巨型条目：

| 情况 | 档案里存什么 | 正文放哪 |
|---|---|---|
| 文本 ≤ 阈值（建议 10 KB） | 全文 | 直接进 index.jsonl |
| 文本 > 阈值 | **只有一行摘要**："文章（3200 字）· 08:20" + `TextLength` | `volatile\<hash>.txt` |
| 文件列表 | 路径清单（**永远只存路径，绝不复制文件本体**） | — |
| 图片 | 原图 blob + 缩略图 | `blobs\` / `thumbs\` |

**选中超长条目时**的行为：显示预览 + 提供"完整复制到剪贴板"按钮（用户主动要，才写剪贴板；这次写入同样标记为 `Origin = tool`，**不进档案**）。

> 这条的直接收益：历史列表永远不会被一篇文章挤爆，翻找真正要用的东西时不用滚很远。**它也是 AI 工具"读整页"能安全落地的前提。**

---

## 四、工具箱外壳：工具注册机制（这是"工具箱"三个字的落点）

外壳只做四件事：**托盘、主窗口、热键分发、设置**。每个小工具实现统一接口：

```csharp
interface IToolboxTool
{
    string Id { get; }            // "clipboard"
    string Name { get; }          // "快捷剪贴板"
    string Icon { get; }          // 托盘/列表用
    string DefaultHotKey { get; } // "Win+Shift+V"
    void Start(ToolboxContext ctx);   // 注册监听、订阅热键
    void Stop();
    object CreatePanel();             // 返回面板 UserControl
}
```

`ToolRegistry` 在启动时扫描已注册工具（P1 阶段先硬编码一个列表，够用；工具多了再改反射扫描）。

**这么设计的目的**：以后加「取色器」「常用短语」「快速计算」「OCR 取字」，只需要新增一个 `Tools\Xxx\` 目录 + 实现接口，**不动外壳一行代码**。第一个工具做完，第二个是半价。

---

## 五、P1 的界面草图

```
┌─ 快捷剪贴板 ────────────────────────── ✕ ┐
│ 🔍 [搜索历史…                    ]  [全部▾] │
├────────────────────────────────────────────┤
│ 📄 会议纪要要点…              08:20  置顶  │
│ 🖼 [缩略图] 截图 1920x1080    08:18        │
│ 📁 hosts, win.ini  (2 个文件)  08:15        │
│ 📄 多行文本验证…               08:12        │
├────────────────────────────────────────────┤
│ 共 128 条 · 已选第 1 条                    │
│ 回车=复制  Esc=关闭  ↑↓=选择               │
└────────────────────────────────────────────┘
```

- 唤出位置：鼠标所在屏幕的**右下角**附近（比居中更不容易挡住视线焦点）。
- 窗口属性：`Topmost`、无边框、失焦自动收起（`Deactivated` 事件）。
- 列表用 `VirtualizingStackPanel`（`ListBox` 默认虚拟化），1000 条也不卡。

---

## 六、实现顺序（每步都能单独验收）

| 步骤 | 内容 | 完成标志 |
|---|---|---|
| 1 | 建 `Toolbox` 工程 + 托盘图标 + 单实例 | 托盘出现图标，右键能退出 |
| 2 | 搬 `ClipboardListener`/`ClipboardReader`，接 `HistoryStore` | 复制东西后 JSONL 增长（与探针一致） |
| 3 | `HotKeyManager` + 空面板唤出/收起 | `Win+Shift+V` 能弹出收起空白面板 |
| 4 | 列表面板 + 缩略图 + 搜索框 | 能搜到刚才复制的内容 |
| 5 | 选中回填剪贴板（`ClipboardWriter`）**+ `Origin` 标记机制** | 回车后任意程序 Ctrl+V 能粘上，**且历史里不新增条目** |
| 6 | 容量配额 + LRU 淘汰 + 重启保留 | 灌 3000 条后不炸，重启历史还在 |
| 7 | **超长内容摘要化**（`volatile\` + 一行摘要） | 复制一篇文章后，列表里是一行而不是一整屏 |
| 8 | 用自检思路给 P1 补一组自动化检查 | 关键路径有回归验证 |

**第 5 步的坑（提前记下）**：`ClipboardWriter` 写回时会触发我们自己的监听，产生一条新记录。
→ 写入前记下内容哈希，事件到达时比对，**命中就跳过**（`DECISIONS.md` §3.4 的去重机制正好复用）。
→ 这一步**必须先于 AI 工具做完**：AI 工具要靠这套标记机制保证"读页面不污染档案"。见 §三·五。

**验收时要专门测的两条**（对应 §三·五）：

- 用 AI 工具读一次页面后，剪贴板里**仍是**用户调起前的内容，且历史里**不出现**新条目；
- 复制一篇 5000 字文章后，列表里是一行摘要（"文章（5000 字）· 时间"），而不是一整屏正文。

---

## 七、P2 / P3 备选（不急）

- **P2**：置顶/删除、类型与时间筛选、纯键盘操作流、多选批量复制、拖拽出去。
- **P3**：开机自启（注册表 `Run`）、热键可改、容量与隐私设置（排除某些程序）、发布打包。
- **再往后**：自动粘贴（要 `SetForegroundWindow` 还原焦点 + 模拟 `Ctrl+V`，是本项目唯一"有点脏"的地方，留到最后做且做成可选）。

---

## 八、明确不做的事（免得接手方"顺手"做出来）

1. **不模拟 `Ctrl+A`/`Ctrl+C` 自动抓取页面**——污染档案、抢走用户剪贴板、抢焦点。见 §三·五。
2. **不靠"来源进程"过滤剪贴板**——`GetClipboardOwner` 常返回 0，第三方进程不可信。只做"自己写的自己标记"。
3. **不复制文件本体进历史**——文件条目只存路径。
4. **不覆盖源文件**——一切另存。

---

*写于 P0 探针验证通过之后。动手前请先读 `DECISIONS.md`。*
